# T5-02 Audit — tensor/tidal polarization gravity attribution

Benchmark ID: `KMQGB-T5-M02-TENSOR-POLARIZATION`  
Concrete observable ID: `SPIN012-TIDAL-NETWORK-001`  
Role: operationalize the spin/helicity distinction left by T5-01 and defeat static scalar/vector quantum-force mimics  
State: **TERMINAL — `PASS_RQIR_GATE`**  
Objective: **`TENSOR_POLARIZATION_ATTRIBUTION_ESTABLISHED`**

## Terminal question

T4-05 showed that a quantized scalar can exactly mimic the Newtonian static phase. T5-01 showed that massless spin-2 consistency forces universal soft coupling.

Can the spin assignment be tied to an **operational source-detector response** rather than inferred only from a formal soft theorem?

Yes: the tidal/radiative polarization response supplies a concrete cross-regime discriminator.

## Frozen detector object

For nearby freely falling detector masses, geodesic deviation is governed by the electric-type tidal tensor

`ddot xi^i = - R^i_(0j0) xi^j`.

For a plane gravitational disturbance the observable detector response can be decomposed into polarization tensors.

In four-dimensional metric theories the standard polarization basis contains

- tensor spin-2: `+`, `x`;
- vector spin-1: `x_vec`, `y_vec`;
- scalar spin-0: breathing `b` and longitudinal `l`.

GR contains only the two transverse tensor modes in its massless radiative sector.

A detector/network output has the schematic form

`h_I(t)=sum_A F_I^A(n,psi) h_A(t-n.x_I)`

where the antenna patterns `F_I^A` differ for tensor, vector and scalar polarizations.

A sufficiently nondegenerate detector network or a controlled tidal detector geometry can therefore identify the polarization subspace rather than only the total force magnitude.

## Cross-regime attribution vector

Freeze

`I_spin2={Theta_static,kappa_soft,T_mn universality,R_0i0j tensor pattern,F_+ and F_x,response phase/lag,absence/bounds on scalar-vector modes}`.

The key demand is that **one parent coupling** links

1. the static/near-zone gravitational phase;
2. the universal soft spin-2 coupling;
3. the radiative/tidal tensor response.

This is much harder for an ordinary scalar/vector quantum mediator to mimic than the static `1/r` law alone.

## Scalar adversary

A massless scalar with tuned charges can reproduce

`V(r)=-G m_A m_B/r`

in a nonrelativistic sector.

But a propagating scalar excitation generically produces scalar tidal/polarization structure rather than a pure helicity-2 `+`,`x` response.

Thus the scalar's static degeneracy is broken by a matched radiative/tidal measurement unless additional degrees of freedom are added and tuned.

Any such enlarged comparator must then reproduce the **same universal source coupling and Ward/contact relations** as well, not merely fit antenna patterns independently.

## Vector adversary

A massless vector can carry a long-range quantum force and quantum information, but its charge structure and vector polarization response differ from universal massless spin-2 gravity.

Again the relevant distinction is a linked source-response vector, not the existence of radiation per se.

## Current observational-method support

Modern gravitational-wave tests use networks precisely because detector antenna patterns respond differently to tensor, vector and scalar modes. Living-review analyses identify the `+`,`x` modes as spin-2, vector modes as spin-1, and breathing/longitudinal modes as spin-0.

Recent 2026 space-based network studies explicitly quantify separation of vector and scalar stochastic backgrounds from the tensor sector.

KMQGB uses this literature as proof of **operational distinguishability in principle**, not as a claim that a future laboratory KG experiment must literally use LIGO/LISA-scale waves.

## F0-F7 terminal map

| Gate | State | Reason |
|---|---|---|
| F0 response object | PASS | tidal tensor / detector antenna response explicit |
| F1 static limit linkage | PASS_SCOPED | matched to T4/T5 branch-phase source sector |
| F2 spin/helicity classification | PASS | tensor/vector/scalar response subspaces distinct with adequate detector geometry |
| F3 operational readout | PASS | network/null-stream/antenna methods exist |
| F4 scalar/vector attribution | **PASS_RQIR_GATE** | static quantum-force mimics do not automatically reproduce pure spin-2 tidal sector |
| F5 full KG rigidity | DEFERRED | must link this response to non-EB/commutator/Ward vector from one KG dynamics |
| F6/F7 | DEFERRED | concrete model/resources absent |

## Terminal status

`PASS_RQIR_GATE`

objective:

`TENSOR_POLARIZATION_ATTRIBUTION_ESTABLISHED`.

## Candidate Gravity design lesson

Permanent rule:

> **A Newtonian-looking quantum force is not enough; gravity attribution should be cross-regime.**

The future KG model should make the same gravitational coupling control

`{Theta_static, chi_R/tidal response, soft spin-2 factor, tensor polarization, Q_channel, A_comm, Ward/contact}`.

This creates a rigidity test against scalar/vector C4 mediators: adding a new field to mimic one component must not be allowed to retune all others independently.

## Scope caveat

Polarization alone is not global theory identification: modified gravity can contain the two tensor modes plus additional polarizations, and detector networks have finite degeneracies/sensitivity.

The methodological PASS is that polarization/tidal structure provides an **independent gravity-attribution coordinate** capable of breaking the T4 scalar static degeneracy.

## Terminal completion

**100% — tensor/tidal attribution coordinate established.**

## Sources

1. Living Reviews in Relativity, *Gravitational-wave tests of general relativity with ground-based detectors and pulsar-timing arrays* (2024 update), polarization/antenna-pattern review.
2. Phys. Rev. D 113, 044046 (2026), space-detector networks for separating tensor/vector/scalar stochastic backgrounds.
3. General E(2)/geodesic-deviation polarization-classification literature.
