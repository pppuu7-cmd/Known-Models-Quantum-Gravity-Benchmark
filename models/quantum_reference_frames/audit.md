# T4-04 Audit — quantum reference frames / relational localization

Benchmark ID: `KMQGB-T4-M04-QRF-RELATIONAL`  
Concrete authority ID: `QRF-SEMICLASSICAL-SPACETIME-HOLE-2025-001`  
Role: red-team for Candidate Gravity claims phrased as branchwise metric/event superposition or absolute localization  
State: **TERMINAL — `PASS_RQIR_GATE`**  
Objective: **`QRF_REDUNDANCY_QUOTIENT_ESTABLISHED`**

## Terminal question

Which apparently quantum-geometric features are invariant physical statements, and which can change under a quantum reference-frame / quantum-coordinate transformation?

The 2025 quantum-coordinate analysis gives a concrete answer:

- whether configurations in different coherent branches are identified as “the same” or “different” can depend on the QRF;
- therefore descriptions of superposition and entanglement can be QRF-dependent;
- event localization across branches can be QRF-dependent;
- the number/identity of manifold points used to describe an event is not by itself a physical invariant;
- relational/dressed observables constructed with physical reference fields provide the correct invariant target.

Therefore future Candidate Gravity cannot use representation-dependent branch language as its novelty residual.

## Frozen redundancy test

Let a branchwise partial observable be

`O^(i)[g^(i),Psi^(i)](p)`.

Choose four physical scalar reference fields `chi_A` that coordinatize the relevant region. The corresponding relational/dressed observable is schematically

`O_|chi=chi*^(i)(x) = O^(i)((chi^(i))^{-1}(x))`.

Candidate Gravity comparisons must be performed on the **relational observable** or another QRF-invariant operational object, not on bare coordinate labels `p` across branches.

## Examples of claims rejected as sufficient

The following phrases are not accepted as KG-specific evidence without an invariant operational completion:

- “the event is at two different spacetime points”;
- “the metric is in a superposition at coordinate x”;
- “there are four events rather than two” merely because branch coordinate labels differ;
- “local/nonlocal interaction” defined only through a QRF-dependent point identification;
- branchwise entanglement/superposition statements that change under a QRF transformation while all relational predictions remain unchanged.

## What survives the quotient

Useful KG observables include quantities such as

- curvature evaluated relative to physical scalar/clock fields;
- branch proper-time differences tied to operational events;
- detector coincidences/worldline intersections;
- relational source-detector separation;
- QRF-invariant causal-order/process witnesses;
- channel/noise/response statistics defined by physical detectors rather than manifold-point labels.

The exact invariant object depends on the model; the methodological quotient does not choose the KG dynamics for us.

## Relation to T4-02

T4-02 found that indefinite causal order alone does not attribute the effect to quantum geometry.

T4-04 explains part of the reason: descriptions such as “delocalized events” versus “superposed metrics” can be transformed into one another under changes of quantum coordinates/reference frame, while the invariant causal-order observable remains the same.

Therefore geometry attribution must use a **relational gravitational observable**, not a preferred branch-coordinate narrative.

## F0-F7 terminal map

| Gate | State | Reason |
|---|---|---|
| F0 transformation framework | PASS | explicit QRF/diffeomorphism comparison-map framework |
| F1 physical reference fields | PASS_SCOPED | scalar-field coincidence construction provides relational coordinate map |
| F2 invariance principle | PASS | QRF-equivalent descriptions must not be counted as distinct physics |
| F3 observable construction | PASS_SCOPED | relational/dressed observables explicit |
| F4 novelty quotient | **PASS_RQIR_GATE** | representation-dependent localization/superposition directions removed |
| F5 model-specific residual | DEFERRED | requires an actual KG parent dynamics |
| F6/F7 | N/A | methodology target |

## Terminal status

`PASS_RQIR_GATE`

objective:

`QRF_REDUNDANCY_QUOTIENT_ESTABLISHED`.

This is not a claim that quantum reference frames explain away quantum gravity. It establishes which directions are **representational nuisance directions** that cannot count as novelty.

## Candidate Gravity design lesson

Permanent rule:

> **KG novelty must be relational before it is quantum.**

Before testing a proposed KG residual against C0–C6, first quotient quantum-coordinate/reference-frame redundancy. Only QRF-invariant/dressed source-detector observables enter the physical comparator vector.

The current preferred KG vector should therefore be understood entirely in operational/relational form:

`{Theta_rel,Q_channel,A_comm,S^+/-_ij,chi_R,N,Ward/contact,constraint invariants,mediator attribution,geometry attribution}`.

## Reopen/extension condition

When a concrete KG parent dynamics exists, this audit should be instantiated as an actual QRF/diffeomorphism transformation test on that model's source-detector observables.

## Terminal completion

**100% — methodological redundancy quotient established.**

## Sources

1. V. Kabel et al., Communications Physics 8, 185 (2025), *Quantum coordinates, localisation of events, and the quantum hole argument*.
2. A.-C. de la Hamette et al., Phys. Rev. Lett. 135, 141402 (2025), indefinite causal order and quantum coordinates.
3. Quantum-reference-frame literature cited by those works.
