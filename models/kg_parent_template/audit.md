# T6-05 Audit — minimal pre-ansatz Candidate Gravity parent template

Benchmark ID: `KMQGB-T6-M05-PREANSATZ-CTP`

State: TERMINAL METHODOLOGY / NO KG ANSATZ PROMOTED

## Purpose

Convert the accumulated benchmark lessons into the smallest mathematical container in which a future Candidate Gravity model could be written and immediately compared to C5 and the stronger comparator registry.

This file deliberately stops **before** choosing new dynamics or parameters.

## Critical correction — Gaussian is only a null subsector, not the full C5 comparator

T6-02 froze the Gaussian linearized-graviton influence functional as a useful exact C5 **subsector**. It must not be mistaken for the whole perturbative quantum-GR comparator.

Ordinary perturbative GR is nonlinear and already generates

- graviton three- and higher-point functions;
- non-Gaussian graviton correlations;
- higher source/CTP vertices from graviton self-interactions and matter/graviton loops;
- higher-curvature EFT corrections and their correlated contact/exchange structures.

Therefore

`higher CTP vertex != automatically beyond C5`.

The parent decomposition must be

`Gamma_parent[T_r,T_a] = Gamma_C5,full_matched[T_r,T_a] + DeltaGamma_KG[T_r,T_a]`,

where `Gamma_C5,full_matched` means the largest perturbative-QG/EFT C5 parent structure available and valid in the same declared kinematic/order domain, not merely its Gaussian truncation.

The Gaussian object from T6-02 remains a valuable analytic null/control because it fixes the two-point retarded/noise structure exactly.

## Variables

Use relational/dressed conserved source histories and Schwinger-Keldysh average/difference variables

`T_r=(T_+ + T_-)/2`,

`T_a=T_+ - T_-`.

## Prospective novelty sector

`DeltaGamma_KG` denotes only the part left **after** matching/profiling the full applicable C5 parent, including the known higher-point/EFT directions at the perturbative order being tested.

It may contain higher-point, nonperturbative, state-dependent or controlled-nonlocal structures, but none of those labels constitutes novelty by itself.

## Mandatory CTP / physical constraints before any ansatz promotion

Any concrete `DeltaGamma_KG` must satisfy, in its declared domain:

1. **normalization/unitarity:** `Gamma[T_r,T_a=0]=0`;
2. **reality/hermiticity:** the doubled-history complex-conjugation relation appropriate to the influence functional;
3. **positivity/noise consistency:** the imaginary sector must define an admissible reduced evolution in the scoped approximation;
4. **causal response:** `a-r...` kernels obey retarded/largest-time support appropriate to the chosen local or explicitly controlled-nonlocal theory;
5. **spin-2 Ward/contact identities:** contractions with gauge/longitudinal directions vanish or combine into the required contact terms for conserved/dressed stress-energy sources;
6. **universal gravitational coupling / helicity-2 attribution:** the same infrared coupling anchors static, soft and tidal/radiative sectors;
7. **relational/QRF invariance:** final observables use physical clocks/worldlines/separations, not bare coordinate branches;
8. **GR/C5 infrared limit:** ordinary perturbative quantum GR is recovered where it is the valid low-energy theory;
9. **full common-domain comparator quotient:** C0-C6, C3b, scalar/Yukawa/ST, field-redefinition classes, full applicable C5 nonlinear/EFT directions, and stronger benchmark adversaries are evaluated in the same domain/order;
10. **rigidity:** the surviving observable relations are forced by the parent dynamics rather than independently tunable.

## Compact observable map

The parent functional must generate both

`K_rel={N_ij,C_ij,chi_R,causal support,proper-time geometry,QRF invariance}`

and

`G_attr={Theta_rel,Q_channel,helicity2/tidal response,kappa_soft,T_mn universality,Ward/contact,mediator nulls}`.

The target novelty condition is a relation

`F(K_rel,G_attr,supporting statistics)=0`

whose parameter/function freedom is smaller than the comparator span and which yields at least one nonzero profiled residual.

## Exact promotion gate for a future KG ansatz

Do **not** create/promote a Candidate Gravity ansatz merely because this template exists.

Promotion requires a concrete `DeltaGamma_KG` such that

1. it is nonzero after matching the **full applicable C5 parent at the same perturbative/kinematic order**, not only the Gaussian C5 subsector;
2. it is not removable by analytic field redefinition or ordinary EFT coefficient matching;
3. it satisfies the constraints above;
4. it produces a finite observable residual after the full comparator/nuisance quotient;
5. the residual links at least two attribution layers (for example ordered quantum structure and spin-2 gravity attribution) rather than living in one easily mimicked channel;
6. the same coupling/parent structure predicts the linked sectors without independent retuning.

Until such a `DeltaGamma_KG` exists, Candidate Gravity remains **uninstantiated at this design layer**.

## Terminal classification

`PASS_RQIR_GATE`

Objective: `MINIMAL_PREANSATZ_PARENT_TEMPLATE_ESTABLISHED`.

This PASS closes a methodology construction only. It does not change external Candidate Gravity readiness.

## Candidate Gravity lesson

The benchmark has identified the precise place where genuine model building can begin: propose a constrained `DeltaGamma_KG` beyond the **full matched C5 parent**, while preserving the infrared massless-spin-2 gravity sector, and immediately subject it to the frozen attribution stack.

## Supporting references for the correction

Perturbative quantum gravity has nonzero three- and higher-graviton interactions/correlation functions and a quantum effective action containing higher-curvature and higher-point structures; therefore non-Gaussianity/higher vertices cannot be used as an automatic beyond-C5 label. Current examples include perturbative graviton three-point calculations, quantum-effective-action scattering constructions, and 2026 perturbative-QG detector energy correlators.
