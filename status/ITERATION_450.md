# Iteration 450 — SOURCE-DEFINED CAUSAL INVARIANT CONTRACTION QUALIFICATION

## Purpose
Before any new contraction-level numerical production, determine prospectively whether the repository's source-backed material uniquely specifies an executable complete coupled magnetic/intertwiner invariant contraction for the direct causal vertex. Iter448/449 only established scoped coherent Toller collision behavior and MUST NOT be promoted to a full-vertex result.

## Frozen scientific question
Does the current repository contain source-backed, internally traceable material sufficient to identify **one unique complete contraction object** without adding fitted coefficients, guessed phases, preferred sequential order, invented normalization, or a surrogate model?

## Frozen lanes (independent audits)
1. `formula_identity`: locate an explicit complete direct-causal-vertex formula/equation object and its source citation/immutable provenance.
2. `index_domains`: verify that all magnetic/intertwiner/spin summation or integration labels appearing in that object have explicit domains/ranges and closed contractions.
3. `weights_phases_norms`: verify that source-defined weights, phases/branch factors and normalizations are explicit enough to execute without fitting or convention invention.
4. `controls_conventions`: verify orientation/order/permutation conventions and an exact EPRL/noncausal control or source-defined comparison needed to distinguish contraction mistakes from physics.

Each lane audits repository text/source metadata independently and emits the exact supporting file paths and anchor lines it found. Mere keyword presence is not a PASS: evidence must be tied to one consistent candidate formula object.

## Frozen aggregate outcomes
- `SOURCE_CAUSAL_INVARIANT_CONTRACTION_OBJECT_QUALIFIED` only if all four lanes provide mutually consistent evidence tied to one unique complete formula object with source provenance and no unstated executable convention.
- `BLOCKED_SOURCE_CAUSAL_INVARIANT_CONTRACTION_OBJECT_INCOMPLETE` if required evidence is absent/incomplete.
- `BLOCKED_SOURCE_CAUSAL_INVARIANT_CONTRACTION_OBJECT_AMBIGUOUS` if multiple incompatible candidate formula objects/conventions remain and the repository does not prospectively select one.
- `INVALID_ITER450_AUDIT` only for malformed/missing lane output or implementation/infrastructure failure.

## Scope/claim guards
A qualification PASS is **not** a causal-vertex finiteness PASS, D7-S2 closure, D7 terminal classification, or authorization of `EXISTING_SUFFICIENT`, `ADAPT_EXISTING`, `HYBRID_REQUIRED`, `NEW_REQUIRED`. It only authorizes a later prospectively frozen numerical/symbolic contraction audit.

No thresholds, required evidence classes, or interpretation rules may be weakened after production results are inspected. If this gate is BLOCKED, do not manufacture a surrogate contraction.

## Dependency
Iter449 terminal result must already be consumed and durably recorded before this gate is implemented. That dependency is satisfied by `results/ITER449_TOLLER_COHERENT_SHARED_SCALE_MULTIPAIR_TERMINAL_2026-09-13.md`.
