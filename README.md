# Known Models / Quantum Gravity Benchmark (KMQGB)

**Repository status:** **100% ready as benchmark/methodology infrastructure** under **RQIR Core v1.0 FROZEN**.  
**Scientific Paper-IV status:** **ACTIVE / NOT_YET_AUTHORIZED**.  
**Repository:** `pppuu7-cmd/Known-Models-Quantum-Gravity-Benchmark`  
**Recovery entrypoint:** `recovery/RESTORE_FROM_NEW_CHAT.md`.

## What “100% ready” means

KMQGB now has a complete declared infrastructure for:

- frozen-core governance;
- known-framework adapters and comparator evidence;
- recovery/provenance/versioning;
- executable methodology and negative controls;
- prospective Candidate Gravity scaffolding;
- fail-closed same-realization / Beyond-C5 / promotion discipline;
- deterministic reproducibility packaging;
- article-facing claim/evidence/limitation control.

This is **not** a claim that quantum gravity is solved, that all known frameworks are excluded, or that a Candidate Gravity model has been established.

Authority: `protocol/READINESS_100_COMPLETION_CONTRACT.md`.

## Frozen architecture

KMQGB is the active known-model proving ground for Paper IV. RQIR Core v1.0 is the independent frozen judge; Candidate Gravity is a separate scientific programme and cannot tune the judge.

Permanent rule:

`framework-native objects -> KMQGB adapter -> frozen RQIR observable/comparator -> terminal or BLOCKED model status`.

A model-specific translation belongs here. A demonstrated Core defect must go through RQIR change control; model failure or blockage never modifies the judge.

## Repository / methodology completion

Current stable metrics:

- R1 repository readiness: **100%**;
- R2 KMQGB methodology/material readiness: **100%**;
- R3 external Candidate Gravity scientific readiness: **24%**;
- legacy parent-search R4: **45% paused/conditional**;
- post-freeze PF1 regression: **5/5 terminal = 100%**;
- Closure Wave 02: **0/3 terminal = 0%**;
- Paper IV: **`NOT_YET_AUTHORIZED`**.

See `protocol/READINESS_METRICS.md` and `recovery/state.json`.

## Executable methodology

Critical controls are frozen in:

- `protocol/EXECUTABLE_TEST_REGISTRY.json`;
- `code/methodology_orchestrator.py`.

The orchestrator fail-closes on missing executables, duplicate registry IDs, command/path mismatch, timeouts, or nonzero test exit status.

The repository-completion cross-check is:

`python code/repository_completion_validator.py --require-100`

The prospective Candidate Gravity scaffold is:

- `schemas/candidate_gravity_record_v1_3.schema.json`;
- `templates/candidate_gravity_record_v1_3.template.json`;
- `code/kg_candidate_record_v13_validator.py`.

A scientifically BLOCKED record is valid infrastructure state. Promotion, heavy compute and article wording remain separately gated.

## Deterministic reproducibility bundle

Bundle policy:

`release/BUNDLE_CONTENTS.json`.

Build and byte-determinism check:

`python code/build_release_bundle.py --verify-deterministic`

GitHub Actions uploads the resulting `kmqgb-reproducibility-bundle` together with the methodology-orchestrator summary after validation.

The pre-score completion run `34412786245` / job `102670803282` passed all 36 steps and produced artifact id `10127884011`.

## Post-freeze benchmark status

Historical benchmark denominators remain immutable. The post-freeze representative PF1 regression is **5/5 terminal** under the same RQIR Core v1.0.

This demonstrated that CFS, GR/EFT, string/dual-resonance, LQG/spinfoam and asymptotic-safety adapters can all be evaluated without changing frozen Core semantics. Terminal here includes scientifically legitimate BLOCKED states.

## Paper-IV global decision

Authority:

- `paper_iv/PAPER_IV_FROZEN_CORE_DECISION_LEDGER.md`;
- `paper_iv/PAPER_IV_FROZEN_CORE_DECISION_LEDGER.json`;
- `post_freeze_paper_iv_closure_wave_02/README.md`.

Current verdict:

**`NOT_YET_AUTHORIZED`**.

`BLOCKED` is never exclusion evidence by itself, and `NEW_REQUIRED` is not authorized while decisive known-framework objects remain completion/attribution blocked.

## Closure Wave 02

### O-AS

Programme-level Lorentzian gravitational contact evidence exists, but the exact terminal object remains:

`STABLE_REPRODUCIBLE_SAME_REALIZATION_A4_PLUS_FULL_ERROR_COMPARATOR_CERTIFICATE`.

Re-audit on new stable contact authority/data rather than repeatedly recording the same absence.

### O-LQG — active analytic front

The current KMQGB multiscale gamma fingerprint has executable relations for:

- gamma/Delta structural identifiability;
- RG tangency of the gamma-duality surface;
- area-metric birefringence gamma inversion;
- observable-space multiscale transport.

The fixed observable relation after common-scale transport is

`Delta_gamma = 2 cot(8 psi) - q`,

with

`beta_Delta = -16 csc^2(8 psi) beta_psi - beta_q`.

What remains physical rather than infrastructural is the same-realization chain

`EPRL/spinfoam -> area-metric continuum/RG -> gamma_EFT`

and same-parent `beta_rho / beta_Delta` authority.

### O-CFS

The exact open target remains:

`FIRST_EXPLICIT_NORMALIZED_CFS_GRAVITY_CORRECTION_TENSOR_PLUS_COMPARATOR`.

## Candidate Gravity

The repository is ready to evaluate a future candidate, but **no Candidate Gravity ansatz is promoted here by Iter178**. External scientific readiness remains 24%.

The end-to-end future decision procedure is:

`protocol/BEYOND_C5_PARENT_PRINCIPLE_DECISION_PROCEDURE.md`.

It links realization provenance, functional freedom, escape-door taxes, physical reduction, comparator quotient, residual geometry, rigidity/holdout, RG transport, compute authorization and publication authorization.

## Publication discipline

Article-facing synthesis:

- `publication/KMQGB_METHODS_EVIDENCE_MATRIX.md`;
- `publication/claim_evidence_matrix.json`.

The repository explicitly distinguishes `ESTABLISHED_EXTERNAL`, `DERIVED_KMQGB`, `REPRODUCED_EXECUTABLE`, `OPEN_BLOCKED`, `HYPOTHESIS_ONLY`, and `FORBIDDEN_OVERCLAIM`.

## Heavy compute

Heavy compute remains **IDLE** while the active CW2 blockers are structural/provenance/matching problems. Numerical precision cannot substitute for a missing same-realization map, normalization, parameter identity or physical correction tensor.
