# PF2-01B1/B2 — Nonlocal Weyl-basis eikonal causality controls

**KMQGB iteration:** 185  
**RQIR Core:** v1.0 FROZEN  
**Parent family:** `NONLOCAL_QG`  
**Observable:** eikonal phase / Shapiro time delay for high-energy weakly coupled gravitational scattering.

## Prospective domain

The benchmark is restricted to the regime used in the source analysis,

`ell_P << b << ell_Lambda`,

with Regge/eikonal kinematics (`t/s << 1`) and the exact Weyl-basis form-factor realization declared below. The result is not extrapolated to strong coupling, arbitrary backgrounds or the whole nonlocal-gravity family.

The applicable RQIR consistency requirement is the controlled-nonlocal replacement in the attribution/physical-consistency stack: a candidate nonlocal dynamics cannot claim a causal physical sector if its frozen physical propagation/scattering observable contains a Shapiro time advance in the declared regime.

## Declared Weyl-basis realization

Use the nonlocal Weyl/scalar-curvature action studied by Giaccari & Modesto, arXiv:1803.08748,

`L_W ~ (2/kappa_D^2)[ R + C gamma_C(Box) C + R gamma_S(Box) R ]`,

with

`gamma_C = (D-2)/(4(D-3)) * (exp(H_2)-1)/Box`,

`gamma_S = -(D-2)/(4(D-1)) * (exp(H_0)-1)/Box`.

The source explicitly separates different entire/form-factor choices, so `H_K` and `H_T` are treated as distinct RQIR realizations rather than averaged into one family row.

## B1 — `H_2 = H_0 = H_K`

The published eikonal analysis states that this Weyl-basis realization develops a causality-violating time advance. The Riemann-square content generated by the form-factor expansion is identified as the relevant dangerous structure.

### Comparator

Einstein gravity in the identical eikonal kinematic setup supplies positive Shapiro time delay. The RQIR physical discriminator is therefore the sign of the nonlocal correction/total delay for the same external states and impact-parameter regime.

### Classification

`FAIL_RQIR_GATE__SCOPED_EIKONAL_CAUSALITY_TIME_ADVANCE`

This is a real negative scientific result for the declared `H_K` Weyl realization, not an infrastructure or missing-object failure.

It is **not** family-level exclusion evidence because a distinct admissible form-factor realization has a different causal outcome.

## B2 — `H_2 = H_0 = H_T`

For the `H_T` entire-function choice considered in the same source, the dangerous Shapiro time advance is absent in the stated regime.

### Classification

`PASS_RQIR_GATE__SCOPED_EIKONAL_CAUSALITY_CONTROL`

This is a causality control only. It does not establish full quantum-gravity sufficiency, family-level viability, or a comparator-orthogonal scattering residual.

## Matched-control significance

The B1/B2 pair is stronger than a one-sided claim because it proves that the causality decision is **form-factor-realization dependent** inside the nonlocal Weyl family. Therefore:

- `NONLOCAL_QG` cannot receive a family-wide causality FAIL from B1;
- `NONLOCAL_QG` cannot receive a family-wide causality PASS from B2;
- the form-factor prescription must remain part of every same-realization vector;
- family-level closure requires a census/equivalence argument over materially distinct viable branches.

## Independent 2026 boundary authority

Zhao, Modesto & Bambi, EPJC 86, 713 (2026), arXiv:2605.01413, show that a particular but large class of nonlocal form factors admits exact vacuum Gödel-type solutions with closed timelike curves. This independently demonstrates that renormalizability by itself cannot be used as a universal causality certificate. It is retained as PF2-01B4 scope evidence and is not silently combined with B1/B2.

## Remaining nonlocal-family gate

The next highest-value object is

`PF2_01B3_RIEMANN_WEYL_AMPLITUDE_RESIDUAL`.

Donà et al., JHEP 08 (2015) 038, establish that an independent Riemann-sector form factor can alter tree-level graviton amplitudes. The remaining RQIR task is not to prove merely that the amplitude differs from GR, but to determine whether a **normalized residual survives the full allowed local higher-curvature EFT comparator quotient** for one declared weakly-nonlocal realization.

Required payload:

`{fixed Riemann/Weyl form factor, external states/helicities, normalized amplitude vector, identical-order GR plus local higher-curvature EFT comparator basis, form-factor parameter incidence, unitarity/pole prescription, causality status, approximation/error ledger, comparator-orthogonal residual}`.

## Global impact

B1 is a scoped scientific FAIL and B2 is a scoped causality PASS. Neither changes the parent `NONLOCAL_QG = PARTIAL_SUBFAMILY_ONLY`, D2, D4-global, D7 or Candidate Gravity activation.
