# KMQGB Research Log — Iterations 036–050

## Iter036–038 — predictive/minimal-suite/machine pipeline

- predictive holdout rigidity frozen;
- minimal discriminating test suite frozen;
- machine-readable candidate record schema and fail-closed validator frozen;
- critical validator rule fixed: scientifically `BLOCKED` is structurally valid; contradictory PASS/promotion is invalid.

## Iter039–044 — Beyond-C5, quartic-first and projection gates

- cheap standalone DeltaGamma seeds screened out;
- Beyond-C5 escape doors E1–E5 frozen;
- nonlocal E1 structural skeleton frozen but parent kernel left blocked;
- CEMZ causality red-team shifted preferred minimal branch from candidate-specific cubic deformation to four-point/higher;
- quartic four-graviton helicity and spectral-moment rigidity frozen;
- local vs nonlocal dispersion-class attribution made mandatory;
- kinematic projection-rank completeness derived from external RQIR Iter611 pattern.

## Iter045–047 — string, radiative protection and parent-principle audit

- type-II string frozen as direct structural adversary to quartic-first rigidity;
- pure-GR two-loop cubic-curvature counterterm corrected naive all-orders `C3=0` language;
- preferred branch became `full matched C5 through 3-point + first candidate-specific independent deformation at 4-point/higher`;
- parent-principle audit classified bootstrap/string, supersymmetry/duality, asymptotic-safety RG, causal/discrete microstructure and positivity/spectral constraints.

## Iter048–049 — readiness metrics and amplitude saturation

- stable R1/R2/R3/R4 reporting rubric introduced;
- amplitude parent-principle saturation frozen:
  - universal double copy too broad;
  - self-dual gravity too scoped;
  - minimal Regge zeros select string;
  - controlled trajectory/zero deformations enter known dual-resonance families;
- cross-representation rigidity protocol and reference code frozen.

## Iter050 — cross-representation/background coverage and fresh 2026 comparator

Stable scores:

- R1 repository readiness `90%`;
- R2 KMQGB methodology/material readiness `86%`;
- R3 external Candidate Gravity scientific readiness `24%` at RQIR Iter611.

Waves 25–29 terminally classified.

Cross-representation comparator coverage:

- full C5 = primary strong null;
- C4/C6 = strong ordinary-quantum-mediator null;
- C2/C3/C3b = strong stochastic/CQ real-time nulls;
- string/AS = strong subobjects but some exact multi-representation KMQGB links remain blocked.

Cross-background rigidity frozen; entropy/extremality consistency retained as shared-parameter overconstraint, not standalone KG novelty.

Fresh comparator audited:

G. M. Rhythm, 2026 state-dependent infinite-derivative gravity with curvature-history fields `{g,Phi,Delta,C,chi}`.

Scoped Bianchi/on-shell conservation PASS; kernel rigidity, ghost/spectrum and quantum completion blocked.

Exact next front:

search for a minimal parent principle that fixes rather than parameterizes a low-dimensional hierarchy and survives C5/C4/C6/string/AS/nonlocal comparators across scattering, CTP/relational and allowed curved-background blocks.
