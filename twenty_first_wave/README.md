# KMQGB Wave 21 — Kinematic Projection-Rank Completeness

**Frozen denominator:** 5 methodology targets.  
**Status:** terminal 5/5.  
**Purpose:** generalize the RQIR Iter611 obstruction into a reusable pre-comparator gate for any future Candidate Gravity observable map.

| # | Target | Terminal result |
|---|---|---|
| T21-01 | parent-vs-target invariant dimension audit | `PASS_RQIR_GATE` |
| T21-02 | required auxiliary-constraint count | `PASS_RQIR_GATE` |
| T21-03 | unique lift/trajectory before derivative or cut pullback | `PASS_RQIR_GATE` |
| T21-04 | projected root/Jacobian authority | `PASS_RQIR_GATE` |
| T21-05 | post-hoc trajectory/zero-fill prohibition | `PASS_RQIR_GATE` |

## Rank rule

Let parent kinematics have dimension `n`, target/native coordinates dimension `m<n`, and target map `s=Phi(x)` with local rank `m`.

A unique local lift requires exactly `n-m` additional independent same-parent constraints, or an equivalent prospectively frozen embedding `x=gamma(s)`, such that the stacked target+constraint Jacobian has full rank `n`.

Without this, target derivatives, pole/cut support and root Jacobians are not uniquely defined.

## External RQIR example

Iteration 611 depends on three independent source invariants `(u,a_s,a_a)` while the native hard-channel protocol supplies one scalar `s`. Therefore two auxiliary same-parent constraints are required. Their absence is a legitimate `BLOCKED` state, not zero contribution and not a physical FAIL.

Authoritative protocol: `protocol/KINEMATIC_PROJECTION_RANK_COMPLETENESS.md`.

No Candidate Gravity readiness or ansatz is promoted by this methodology result.