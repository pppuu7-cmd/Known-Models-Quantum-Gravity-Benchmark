# SYNTHESIS-016 — Black-Hole Monodromy / Wave-Equation Spectral Parent

**Status:** A2-positive gravity-native spectral control / rejected as standalone P4 parent.  
**KMQGB iteration:** 127.  
**Purpose:** test the strongest available template in which finite gravitational geometric data genuinely determine a non-rational spectral function rather than merely constraining it.

## 1. Parent capsule

Take a fixed black-hole background and a specified bulk fluctuation operator with

- its finite differential equation/operator coefficients fixed by the geometry and field content;
- boundary normalization/source convention;
- ingoing/causal horizon condition;
- the appropriate boundary/normalizability condition.

The retarded Green function and spectral density are then obtained from the resulting boundary-value/monodromy problem.

Recent 2026 holographic work provides a strong realization of this template: exact/large-frequency thermal spectral functions factor into perturbative/OPE and nonperturbative pieces, with the latter derived by exact WKB/monodromy of the black-hole wave equation and carrying information about the horizon/interior/singularity.

## 2. A1 — genuine finite physical generator

Unlike KMS, finite moment constraints or a hand-selected form factor, the wave equation plus causal boundary conditions is a **dynamical generator**.

Once the background/operator/state prescription is fixed, it does not require an independently fitted value of the spectral density at every frequency.

Classification:

`A1 PASS_CONTROL__FINITE_GRAVITY_BACKGROUND_DYNAMICS_GENERATES_SPECTRAL_OBJECT`.

## 3. A2 — continuous/non-rational spectral data can be derived

The resulting retarded spectral function can contain

- quasinormal poles;
- branch/continuum structure;
- nonperturbative high-frequency corrections;
- transseries/monodromy information.

Thus this architecture demonstrates the key mathematical possibility sought after Iter123–126:

`finite gravity-related differential law + physical boundary conditions -> unique non-rational spectral envelope`.

No arbitrary positive-frequency function is inserted after the fact.

Classification:

`A2 PASS_CONTROL__SPECTRAL_ENVELOPE_DERIVED_FROM_BOUNDARY_VALUE_PROBLEM`.

## 4. A3 — comparator containment

The known realizations are already contained in strong registered architectures:

- ordinary QFT on black-hole backgrounds;
- Einstein/supergravity black-hole perturbation theory;
- AdS/CFT/holographic thermal correlators;
- black-hole quasinormal-mode/Green-function methods.

Therefore the mechanism is a **positive blueprint**, not a novelty certificate.

Classification:

`A3 FAIL_AS_NOVEL_PARENT__BLACK_HOLE_LINEAR_RESPONSE_HOLOGRAPHY_CONTROL`.

## 5. A4 — probe/background G2 is not intrinsic quantum-gravity K4

The crucial limitation is attribution/order.

The construction starts from a specified background geometry and a linearized/probe operator. It derives a two-point/linear-response spectral object. It does not prospectively derive

- a comparator-orthogonal intrinsic flat-space physical four-graviton irreducible hard kernel;
- the nonlinear backreacting graviton interaction hierarchy;
- an all-point gravitational S-matrix from one new parent;
- a new same-parent CTP hierarchy beyond the known theory used to define the wave operator.

In KMQGB notation:

`derived G2 on chosen gravity background != derived new quantum-gravity K4`.

Classification:

`A4 BLOCKED__FINITE_SPECTRAL_GENERATOR_IS_BACKGROUND_PROBE_CONTROL_NOT_NEW_INTRINSIC_4G_DYNAMICS`.

## 6. Strong lesson for Candidate Gravity

This is nevertheless one of the most useful positive controls in the search. It shows exactly what a successful origin law should look like structurally:

`finite operator/dynamics`

`+ state/boundary/causal prescription`

`-> complete analytic spectral object`,

rather than

`constraints -> choose remaining function`.

The future KG analogue must elevate the finite generator from a **linear probe equation on an already chosen geometry** to a **finite interacting gravitational law** that simultaneously determines

1. the physical background/asymptotic state sector;
2. the primitive irreducible `4g` hard datum;
3. its all-point nonlinear continuation;
4. the same-parent retarded/CTP hierarchy.

## 7. Reopen condition

A monodromy/wave-equation-inspired P4 branch becomes serious only if the finite operator law is itself new and gravity-native and derives

- interacting spin-2 rather than probe `G2` dynamics;
- absolute physical amplitude normalization;
- comparator-orthogonal hard `4g`/higher structure;
- all-point and CTP observables;
- and survives GR/holography/string/matrix/AS/nonlocal/discrete comparators.

## 8. Score consequence

A2-positive control only. No P4/P5/P6 deliverable is closed. R4 remains 45%.
