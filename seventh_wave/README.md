# KMQGB Seventh Wave — Candidate DeltaGamma Residual Taxonomy

**Frozen denominator:** 5 targets.  
**Historical waves 1–6:** terminal and immutable.  
**Seventh-wave terminal coverage:** **5/5 = 100%**.  
**Purpose:** test candidate locations for `DeltaGamma_KG` only after the full applicable C5 parent is matched.

| # | Candidate residual class | Terminal status | Result |
|---|---|---|---|
| T7-01 | higher-point / non-Gaussian graviton structure | `OPERATIONALLY_DEGENERATE` | perturbative quantum GR/C5 already contains nonlinear graviton interactions, loops, higher-point correlators and EFT corrections |
| T7-02 | squeezed / exotic graviton state | `OPERATIONALLY_DEGENERATE` | striking signatures can be state effects within ordinary quantized gravity; profile over admissible `rho_h` |
| T7-03 | memory / non-Markovian kernel | `OPERATIONALLY_DEGENERATE` | ordinary graviton influence dynamics already generates colored noise and non-Markovian memory |
| T7-04 | parity / chiral ordered structure | `OPERATIONALLY_DEGENERATE` | parity-violating EFT/extra-pseudoscalar/state comparators can generate chirality/birefringence |
| T7-05 | nonanalytic / cross-regime structure | `OPERATIONALLY_DEGENERATE` | C5 massless loops already generate universal nonanalytic structure; extra fields/UV states generate ordinary thresholds/cuts |

## Seventh-wave rollup

`5 OPERATIONALLY_DEGENERATE`.

No candidate qualitative residual class survived as a standalone location for `DeltaGamma_KG`.

This does **not** mean that all possible dynamics are absorbed by C5. It means that labels such as

- higher-point;
- squeezed;
- memory;
- chiral;
- nonanalytic

are too broad to define novelty.

## Full-C5 guardrail retained

Every target is evaluated against

`Gamma_parent = Gamma_C5,full_matched + DeltaGamma_KG`.

A feature is not assigned to `DeltaGamma_KG` merely because it is absent in a Gaussian, vacuum, Markovian, parity-even or tree-level truncation.

## Stronger lesson — move from feature classes to matched identities

Future Candidate Gravity work should no longer ask

`Which qualitative feature should DeltaGamma_KG have?`

Instead ask

`Which explicit relation among relational source/channel/ordered/analytic observables is violated by every full comparator parent?`

Examples of admissible stronger objects include

- same-normalization cross-order residuals `R_n=Gamma_obs^(n)-Gamma_C5,full^(n)`;
- state-independent combinations after profiling `rho_h`;
- linked spectral/temporal identities rather than memory alone;
- parity-odd relations tied simultaneously to spin-2 Ward/universality rather than chirality alone;
- origin-resolved analytic-structure vectors
  `I_an={location,type,residue/discontinuity,spin/helicity,source coupling,IR coefficient,origin,Ward/contact}`.

## Candidate Gravity consequence

The seventh wave exhausts the most obvious feature-level places to hide a new parent term. The next useful stage should therefore test **cross-order / cross-attribution identities** and the geometry of the comparator span itself, rather than inventing another qualitative effect.

No Candidate Gravity ansatz is authorized by this wave.
