# SOURCE_J1_K5_EQ4_IMPROPER_DISTRIBUTIONAL_EXISTENCE_E1E2_REPAIR_GATE — prospective preregistration

Date: 2026-09-15
Status: `PREREGISTERED; NO RESULT YET`
Lane: KMQGB Research / Closure

This is a prospective repair/supersession of the dependency/control logic in `research/SOURCE_J1_K5_EQ4_IMPROPER_DISTRIBUTIONAL_EXISTENCE_PREREG_2026-09-15.md` (commit `1983e9f28b45af33cf15d297b3ebcc293c82e8fb`). The historical preregistration remains immutable. No substantive result or implementation was produced under that stale contract before the Critic correction below.

## HYPOTHESIS

For the exact fixed source-order `j=1`, nonzero-real-rho, channel-`00000`, full-K5-collision object, the already validated repository record is sufficient to decide whether the published Eq. (4) is presently **established to exist as a controlled distribution** without introducing a new joint regulator/extension rule.

The null/blocked alternative is not nonexistence: if the published source supplies only one-wedge distributions, the standard sufficient Hörmander multiplication criterion is unavailable at an allowed full-collision witness, and no validated necessary nonexistence theorem is among the frozen inputs, then the correct terminal status is `BLOCKED_SCOPED` rather than scientific FAIL.

## exact OBJECT

The published source-order causal Eq. (4) integrand/product in the fixed realization already used by the validated collision theorems:

- fixed `j=1`;
- fixed nonzero real rho convention;
- fixed intertwiner channel `00000`;
- gauge-fixed `SL(2,C)^4` group variables;
- full K5 collision stratum `N=SU(2)^4`;
- ten source-defined one-wedge Toller distributions, not a reordered ten-spectral surrogate and not an auxiliary regulator model.

This gate asks only whether distributional existence of this exact object is established by the frozen validated premises. It does not ask uniqueness.

## DEPENDENCY

Frozen validated premises, all predating this preregistration:

1. `research/SOURCE_J1_K5_PUBLISHED_IEPSILON_SCOPE_RESULT_2026-09-15.md`, result commit `8c6582371d6e763e4f37b30fbe42f67684edcdb7`:
   `SOURCE_CAUSAL_VERTEX_PUBLISHED_IEPSILON_IS_ONE_WEDGE_NOT_JOINT_COLLISION_REGULATOR_SCOPED`.
   This is the authoritative E1 premise; the published i-epsilon defines individual Toller matrices but does not itself provide a ten-wedge joint collision regulator/removal theorem.
2. `research/SOURCE_J1_K5_COHERENT_CONTACT_HORMANDER_RESULT_2026-09-15.md`, result commit `051d0c71382b55f2ce37994a95aa704b8725a599`:
   `SOURCE_J1_K5_COHERENT_CONTACT_HORMANDER_CRITERION_FAILS_SCOPED`, including exact nonzero conormals with `dB_12 + dB_23 - dB_13 = 0`.
3. `research/SOURCE_J1_K5_ALIGNED_CONTACT_CYCLESPACE_RESULT_2026-09-15.md`, result commit `397a55009e88e4a43453e07c015bc566c9c9412c`:
   `SOURCE_J1_K5_ALIGNED_CONTACT_CYCLESPACE_HORMANDER_OBSTRUCTION_SCOPED`, with six-dimensional K5 cycle-space relations at an allowed aligned-spinor witness.
4. `research/SOURCE_J1_FULL_COLLISION_ABSOLUTE_DIVERGENCE_THEOREM_2026-09-15.md`, result commit `bf43183b12799e217e452c6335edc1114e389352`, independently Critic-confirmed by commit `a4d775ff799e629ccb6970dc587cd91cd8f6810b`: ordinary local absolute-Haar divergence in this strict scope. This is a scope/control premise only; it is not distributional nonexistence.
5. Latest Critic correction `recovery/CRITICAL_REVIEW_SOURCE_J1_K5_EQ4_ADDITIONAL_JOINT_SELECTION_AUTHORITY_V2_2026-09-15.md`, commit `2fda454298a3d2594e24242c49fc96a91df76958`: the Eq4 joint-selection-authority V2 executable result is `INVALID_IMPLEMENTATION` as a validated downstream premise.

The invalid V1/V2 joint-selection-authority results are explicitly EXCLUDED from the scientific decision. Narrow source facts independently validated by item 1 remain admissible.

## SOURCE / REALIZATION AUTHORITY

Authority is exactly the immutable repository records listed above plus the current RQIR/recovery governance. No web summary, chat memory, guessed regulator, or post hoc source reinterpretation may enter the classifier.

The one-wedge identity `T+ + T- = D` and source-defined one-wedge distributions remain upstream facts. This gate does not manufacture a joint product from that identity.

## FROZEN INPUTS

The implementation must consume a structured manifest containing the five premises above with exact file path, commit SHA, expected terminal classification/fact, and role. The workflow must source-lock all five paths/commits as ancestors and verify that the exact expected facts are present in the checked-out records.

The executable scientific decision must be a generic decision function over structured booleans derived from those locked records, not a literal terminal label.

Required derived predicates:

- `E1_SOURCE_JOINT_PRESCRIPTION_ESTABLISHED`: true only if a frozen validated premise establishes a joint K5 collision product/regulator/removal prescription for Eq. (4). The published i-epsilon result listed above implies `false` for the current manifest.
- `E2_SUFFICIENT_PRODUCT_CRITERION_PASSES`: true only if a frozen validated sufficient product/pullback criterion passes for the exact object. The coherent-contact and aligned-contact results force `false` because an allowed collision witness violates the standard Hörmander sufficient criterion.
- `E2_SUFFICIENT_PRODUCT_CRITERION_FAILS_AT_ALLOWED_WITNESS`: derived from the exact conormal relation/cycle-space evidence; current manifest implies `true`.
- `E4_NECESSARY_NONEXISTENCE_THEOREM_ESTABLISHED`: true only if a frozen validated premise is a necessary theorem excluding every controlled distributional extension of this exact object. None of the frozen results has that claim; current manifest implies `false`.
- `INVALIDATED_AUTHORITY_USED`: must remain `false`.

No missing object may be encoded as zero and failure of a sufficient criterion may not be promoted to nonexistence.

## POSITIVE / NEGATIVE CONTROLS

The implementation must run outcome-sensitive in-memory fixtures through the same decision function before evaluating the frozen manifest:

1. Positive existence control: set `E1_SOURCE_JOINT_PRESCRIPTION_ESTABLISHED=true` with all invalid flags false; classifier must reach `SOURCE_J1_K5_EQ4_DISTRIBUTIONAL_EXISTENCE_ESTABLISHED_SCOPED`.
2. Positive sufficient-criterion control: set `E2_SUFFICIENT_PRODUCT_CRITERION_PASSES=true`; classifier must reach the same existence label.
3. Positive nonexistence control: set `E4_NECESSARY_NONEXISTENCE_THEOREM_ESTABLISHED=true` while E1/E2 existence predicates are false; classifier must reach `SOURCE_J1_K5_EQ4_DISTRIBUTIONAL_NONEXISTENCE_ESTABLISHED_SCOPED`.
4. Negative/blocked control: E1=false, E2-pass=false, E2-fails-at-witness=true, E4=false; classifier must reach `SOURCE_J1_K5_EQ4_DISTRIBUTIONAL_EXISTENCE_BLOCKED_SCOPED`.
5. Invalid-control: if `INVALIDATED_AUTHORITY_USED=true`, classifier must reach `INVALID_IMPLEMENTATION` regardless of otherwise favorable flags.
6. Contradiction-control: simultaneous validated existence and necessary nonexistence predicates must reach `INVALID_IMPLEMENTATION`, not an arbitrary scientific label.

All fixture branches are method controls only; they are not model evidence.

## PASS

`SOURCE_J1_K5_EQ4_DISTRIBUTIONAL_EXISTENCE_ESTABLISHED_SCOPED` iff either:

- a frozen validated source-defined joint prescription establishes the exact Eq. (4) distributional object; or
- a frozen validated sufficient product/pullback theorem passes for the exact object;

and no invalid/contradictory premise is used.

## FAIL

`SOURCE_J1_K5_EQ4_DISTRIBUTIONAL_NONEXISTENCE_ESTABLISHED_SCOPED` iff a frozen validated **necessary** obstruction excludes every controlled distributional extension of the exact object and no existence premise simultaneously holds.

This is intentionally a high bar. Absolute divergence, failure of Hörmander's sufficient criterion, collision-supported ambiguity, or absence of an explicit joint regulator are individually insufficient for FAIL.

## BLOCKED / INVALID

`SOURCE_J1_K5_EQ4_DISTRIBUTIONAL_EXISTENCE_BLOCKED_SCOPED` iff:

- E1 does not establish a source-defined joint collision prescription;
- E2 does not establish existence and an allowed witness defeats the standard sufficient Hörmander criterion;
- E4 supplies no necessary nonexistence theorem;
- invalidated V1/V2 authority results are not used.

`INVALID_IMPLEMENTATION` iff the executable uses an invalidated authority premise, violates source/object identity, cannot reach all preregistered fixture branches, hard-codes the terminal scientific label, or encounters logically contradictory validated existence/nonexistence premises.

`INFRASTRUCTURE_FAILURE` applies only before a scientific classifier is valid.

## INTERPRETATION CEILING

A BLOCKED result means only: distributional existence of the exact published Eq. (4) full-collision product is **not established by the frozen validated routes**, while nonexistence is also not established.

It does NOT prove:

- Eq. (4) is meaningless or nonexistent;
- no source-compatible extension can be constructed;
- a common/independent auxiliary regulator is source-equivalent;
- uniqueness or physical/global nonuniqueness;
- failure of every spin/channel/family;
- D7-S2, D7-S3 or D7-S4 closure;
- any terminal D7 selector;
- Candidate Gravity activation;
- `QUANTUM_GRAVITY_SOLVED`, `ALL_KNOWN_MODELS_FAIL`, `NEW_THEORY_REQUIRED`, or `NEW_PHYSICS_FOUND`.

## Downstream rule

If this gate returns BLOCKED, the next admissible high-information gate is a **separately prospectively frozen** `SOURCE_J1_K5_ADDITIONAL_JOINT_REGULATOR_EQUIVALENCE_GATE`, which may construct an auxiliary common/independent regulator only as a candidate extension and must then test source equivalence/path-order independence. This gate itself does not perform that construction.

Governance remains: `RQIR Core v1.0 = FROZEN`; `D7-S2 = NOT_CLOSED`; `D7-S3 = NOT_CLOSED`; `D7-S4 = PARTIAL_GLOBAL_NOT_CLOSED`; terminal selectors forbidden; Candidate Gravity inactive.