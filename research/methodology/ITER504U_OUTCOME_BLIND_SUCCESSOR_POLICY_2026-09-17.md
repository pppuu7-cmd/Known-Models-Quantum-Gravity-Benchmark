# Iter504U outcome-blind successor policy

Date: 2026-09-17
Status: `FROZEN_WHILE_ITER504U_RUN_35246605860_IS_NONTERMINAL`

This policy is frozen without consuming partial held-out slope/drift/certification values from active Iter504U run `35246605860`.

## Parent gate

`ITER504U_HELDOUT_LOCAL_D_GENERALIZATION_FIREWALL`

Prospective preregistration: `05b8e9354c9a7805f0fce18b904346a986a0787f`.

## If terminal PASS

If and only if the independently checked terminal class is

`ITER504U_HELDOUT_LOCAL_D_GENERALIZES_SCOPED`,

then the next admissible step is **campaign design**, not a claim of full-domain closure:

`ITER504V_BROADER_DOMAIN_CAMPAIGN_DESIGN`

Required before any broader execution:

1. inventory the actual authorized parent-state domain and exact state identities;
2. define deterministic sharding and complete-cover rules;
3. freeze computational resource limits and resume semantics;
4. preserve the exact no-refit local-D scientific contract;
5. preserve exact-Arb decision transport and explicit leaf/per-rho binding;
6. prospectively freeze aggregation and independent Critic rules;
7. distinguish implementation failure, INCONCLUSIVE and scientific certification;
8. prohibit promotion from held-out PASS to model/family/global conclusion without the broader execution itself.

A held-out PASS authorizes design of this broader campaign only.

## If terminal INCONCLUSIVE

If and only if the independently checked terminal class is

`ITER504U_HELDOUT_LOCAL_D_INCONCLUSIVE_SCOPED`,

then **do not** increase depth, alter thresholds, substitute cases or remove difficult held-out cases.

The next admissible gate is:

`ITER504V_HELDOUT_RESIDUAL_LOCALIZATION`

It must operate only on the prospectively frozen unresolved case/cell identities from the terminal Iter504U payload and must prospectively discriminate among already-authorized residual mechanisms such as:

- insufficient local derivative enclosure contraction;
- max-channel competition/switching;
- fixed-channel local nonstationarity;
- mixed mechanism.

Any additional partition/depth or direct-function certificate must be prospectively frozen before its new values are consumed.

INCONCLUSIVE is not model failure.

## If terminal INVALID

If the terminal class is

`ITER504U_INVALID`, or the workflow fails before producing a valid terminal classification, no scientific inference follows.

The only admissible next step is minimal implementation/provenance defect localization and an execution-only repair that preserves the frozen held-out science/cohort/classifier. Do not change cases, thresholds, depth, R/rho grids, precision or channel count in response to the invalid execution.

## If cross-environment disagreement occurs

Treat it as implementation/provenance invalidity until localized. Do not average, majority-vote, select one environment or classify from display floats.

## Universal ceiling

No branch of this policy by itself authorizes all-Iter504 closure, all 1888 states, D7 closure, model/family failure, a selector decision, Candidate Gravity, Paper IV, `QUANTUM_GRAVITY_SOLVED`, `ALL_KNOWN_MODELS_FAIL`, `NEW_THEORY_REQUIRED`, or `NEW_PHYSICS_FOUND`.
