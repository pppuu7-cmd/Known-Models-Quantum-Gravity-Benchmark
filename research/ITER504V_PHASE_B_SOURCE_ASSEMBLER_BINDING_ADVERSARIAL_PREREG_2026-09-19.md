# ITER504V Phase-B source-assembler binding adversarial gate — preregistration

Date: 2026-09-19
Lane: KMQGB Research / Closure
Gate: `ITER504V_PHASE_B_SOURCE_ASSEMBLER_BINDING_ADVERSARIAL_GATE`
Status: PROSPECTIVELY FROZEN BEFORE RESULT

## HYPOTHESIS

The exact Phase-B source assembler used by authoritative source run `35405065903` accepts at least one structurally/provenance-invalid synthetic inventory while preserving a nominal scientific classification, specifically either or both of:

A. a malformed serialized parent-inclusion child/parent dyadic identity with inclusion booleans and leaf decisions held fixed;
B. a frozen shard/artifact placement mismatch in which case payloads are physically exchanged between two quartile shard directories while their internal state IDs and the complete 768-state union remain unchanged.

This is a verifier-binding hypothesis only. It does not assert that the active producer emitted either defect.

## EXACT OBJECT

The tested object is the exact source-launch assembler blob:

- source run: `35405065903`;
- immutable source launch head: `31fcdacffcc394e96b73917083281edb90d6753c`;
- `code/iter504v_phase_b_assemble.py` blob: `f93bea2c32ed383a054beebb4d4b1c64fcbbd2c1`;
- Phase-B preregistration: `e42caf47f9201d79cd89b79dc72b90f696e3ec5d`;
- Phase-B execution authority: `caf67585a9fad990dd90948df51839e6ed7cf891`;
- campaign manifest blob: `d3b8821e08243016bd475f3faf9f2161deed13d8`;
- preterminal audit dependency: commit `20cf389c1a85a6aba4f49820b945a6dca78cc654`.

No active source-run case, leaf, slope, drift, certification, assembly, aggregate or counterexample payload is part of this object.

## DEPENDENCY

This closure gate depends only on the frozen Phase-B contract and source code/provenance above. It is admissible while source run `35405065903` is nonterminal because it consumes no scientific output and launches no competing same-object scientific execution.

## SOURCE / REALIZATION AUTHORITY

The synthetic corpus must reproduce the exact source assembler schema and frozen complete-q1 identity:

- causals `0to5,1to4,2to3`;
- blocks `0..3`;
- signed paths `0..3`;
- boxes `0..15`;
- 768 unique canonical state IDs;
- canonical sequence SHA256 `3cac282175830e795394dca5202f5368b27120817f2d4abd9eba54fc6c9f1e6f`;
- R `6,8,10,12`;
- rho `0.35,0.9,1.6,2.7`;
- precision 384;
- 243 channels;
- threshold `1/20`;
- robust floor `1`;
- `MAX_DEPTH=3`;
- exact dyadic midpoint partition tags;
- exact leaf/per-rho certification binding;
- local derivative recomputation tags.

Synthetic numerical values are not interpreted physically. They exist only to exercise the exact structural validator path.

## FROZEN INPUTS

The gate constructs one deterministic complete 768-case baseline arranged in exactly 192 quartile shard directories for one environment, four states per shard. Every shard contains a `shard.json` declaring its frozen `(causal, block, path, quartile)`, exact four boxes and exact four state IDs.

Baseline cases are structurally valid under the exact assembler. One designated state contains a coherent depth-1 two-child tree with two parent-inclusion records so candidate A can be mutated without changing leaf certification. All other states may use one certified depth-0 leaf.

Candidate A mutation is frozen as: change only one designated inclusion record's serialized parent interval/depth identity so it is not the exact dyadic parent of the child; keep inclusion booleans, leaf intervals, leaf certification and unresolved counts unchanged.

Candidate B mutation is frozen as: exchange exactly one case file between q0 and q1 shard directories for the same `(causal,block,path)` while leaving both case payloads, their internal state IDs and both original `shard.json` declarations unchanged. The complete 768-state union remains exact.

## POSITIVE CONTROLS

1. The coherent 768-case baseline must be accepted by the exact source assembler with `errors=[]`, `total_cases=768`, zero unresolved leaves and classification `ITER504V_BROADER_DOMAIN_LOCAL_D_CERTIFIES_COMPLETE_Q1_SCOPED`.
2. An independent oracle must confirm the baseline parent-inclusion identity.
3. An independent shard oracle must confirm all 192 shard declarations each match exactly the four physically contained internal state IDs and that the full frozen shard cover is exact.

## NEGATIVE / SENSITIVITY CONTROLS

1. Candidate A oracle must detect the malformed child/parent dyadic relation.
2. Candidate B oracle must detect the shard/content mismatch.
3. A duplicate/internal-state mutation must be rejected by the exact assembler as `ITER504V_BROADER_DOMAIN_INVALID`; this proves the gate is not merely demonstrating universal acceptance.
4. A leaf/per-rho certification contradiction must be rejected by the exact assembler as invalid.

## PASS / FAIL / BLOCKED / INVALID

`ITER504V_PHASE_B_SOURCE_ASSEMBLER_BINDING_DEFECTS_VERIFIED_SCOPED` iff:

- all positive and sensitivity controls pass;
- candidate A is independently structurally invalid but the exact assembler still returns nominal PASS with no validation error;
- candidate B is independently shard/topology invalid but the exact assembler still returns nominal PASS with no validation error;
- both Python 3.11 and 3.13 gate lanes produce byte-identical canonical decision payloads.

`ITER504V_PHASE_B_SOURCE_ASSEMBLER_BINDING_DEFECT_PARTIAL_SCOPED` iff all controls pass and exactly one of A/B survives the exact assembler.

`ITER504V_PHASE_B_SOURCE_ASSEMBLER_BINDING_CANDIDATES_REFUTED_SCOPED` iff all controls pass and the exact assembler rejects both A and B for the intended contract violations.

`ITER504V_PHASE_B_SOURCE_ASSEMBLER_BINDING_BLOCKED_SCOPED` iff a faithful complete synthetic corpus or exact-source invocation cannot be established without consuming active scientific payload.

`INVALID_IMPLEMENTATION` iff source/blob/provenance locks fail, the baseline is not accepted, sensitivity controls fail, the gate does not execute the exact source assembler, or cross-environment canonical decision payloads disagree.

The criteria above are frozen and must not be changed after any gate result is observed.

## INTERPRETATION CEILING

This gate can establish only a scoped verifier/authority-path fact about the exact source assembler. Even a defect-verified result does not mean the active Phase-B producer emitted malformed science, does not classify source run `35405065903`, does not alter frozen scientific PASS/INCONCLUSIVE semantics, and does not authorize any model-family, D7, Candidate Gravity, Paper IV or global quantum-gravity conclusion.

If either defect is verified, the separately frozen terminal independent Critic must explicitly close that binding against exact immutable source artifacts before any terminal Phase-B authority may be accepted.
