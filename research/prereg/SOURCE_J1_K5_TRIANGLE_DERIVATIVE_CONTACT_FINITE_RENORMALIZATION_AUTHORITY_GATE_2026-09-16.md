# Preregistration — SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_FINITE_RENORMALIZATION_AUTHORITY_GATE

Date: 2026-09-16
Status: PROSPECTIVELY_FROZEN_BEFORE_SOURCE-AUDIT_IMPLEMENTATION_OR_RESULT

## HYPOTHESIS

The six-dimensional finite local freedom left by terminal V8 may be reduced, quotiented, or fixed only by an affirmative same-realization source authority: an explicit finite normalization/renormalization condition, gluing/composition identity, RG/scale identity, or explicitly authorized equivalence quotient. Mere absence of a source selector is a BLOCKED/source-authority fact, not a scientific/model failure.

## EXACT OBJECT

Consume exactly the terminal V8 local highest-contact `delta''` triangle object and no broader object:

- local normal roots `B12=x`, `B23=y`, `B13=x+y`;
- fixed `j=1`, fixed-tangent/channel-`00000` V4/V5 realization;
- complete S3-invariant collision-supported jet class through derivative order 7;
- V8 exact scheme-equivalence affine solution with rank `2`, augmented rank `2`, nullity `6`;
- V8 basis count `8` and nullspace basis exactly as stored in the locked V8 canonical JSON.

The gate does not retest V7 scheme dependence or V8 counterterm absorbability. It tests only source authority acting on the V8 finite-renormalization freedom.

## DEPENDENCY

Terminal parent: `SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_COUNTERTERM_EQUIVALENCE_GATE`, result commit `3e6e069d728ccef2724c26f374c298f29d591043`, current-front commit `57b647b177cd84a0b3162c289883019e6f195244`.

Historical V7 `INVALID_IMPLEMENTATION` remains preserved; this gate consumes only repaired V7 authority through terminal V8. V5 remains QUALIFIED with exact `00000=11/24` source/realization fact confirmed scoped and no broad-support promotion.

## SOURCE / REALIZATION AUTHORITY

The companion immutable ledger `inputs/source_j1_k5_triangle_derivative_contact_finite_renormalization_authority_v9.json` freezes the complete structured source directory present at start-of-gate (`sources/*.json`) by blob SHA, together with the exact V4/V5/V7/V8 same-realization authority chain and V8 canonical object.

Decision-critical selector evidence must be affirmative and same-realization. A missing prose literal or generic discussion is not a negative theorem. Only explicit selector data or an explicit source identity whose action on the frozen V8 coefficient space can be represented exactly may reduce the six-dimensional freedom.

## FROZEN INPUTS

- start-of-gate main `57b647b177cd84a0b3162c289883019e6f195244`;
- `sources/arxiv_2202_04360_sl2c_haar_cartan.json` blob `4a8eb5762745f4b483c8aff87c0c9fb8b74cc236`;
- `sources/arxiv_2601_23162v1_causal_vertex.json` blob `f0b520fc04904e038f252dc5b45d23893cb020ed`;
- `sources/arxiv_2604_24945v1_toller_cartan.json` blob `8dc58f3c9c191289f2f148469f8df3ea95f77ad5`;
- V8 canonical JSON blob `49e248993d0a23d85663aaa198cd5d87b6200b6e`;
- V8 terminal/result and V4/V5/V7 authority files listed in the companion ledger.

No new external source may be inserted after observing the audit result. A later source-expansion gate, if needed, must be prospectively frozen separately.

## FROZEN SELECTOR GRAMMAR

The structured source audit must recursively inspect every key/value in the three frozen `sources/*.json` files. A decision-relevant selector is admissible only if it is explicit and machine-identifiable in one of these prospectively frozen categories:

1. `FINITE_COEFFICIENTS`: explicit numerical values/equations for collision-supported finite counterterm coefficients in the same local realization;
2. `FINITE_NORMALIZATION`: explicit renormalization/normalization condition that fixes the common finite normalization or one/more V8 affine coordinates;
3. `GLUING_COMPOSITION`: explicit gluing/composition identity accompanied by enough same-realization data to induce exact linear constraints on V8 coefficients;
4. `RG_SCALE`: explicit RG/scale identity accompanied by enough same-realization data to induce exact linear constraints on V8 coefficients;
5. `AUTHORIZED_QUOTIENT`: explicit equivalence/quotient statement identifying specified V8 finite directions as physically redundant.

Keyword presence without coefficient/constraint/quotient data is `MENTION_ONLY` and cannot reduce nullity. Unrelated Haar/Cartan or representation normalization is not automatically finite collision-renormalization authority.

## POSITIVE CONTROLS

1. Reproduce from the locked V8 canonical JSON: classification `SOURCE_UNFIXED_FINITE_LOCAL_FREEDOM_SCOPED`, basis count `8`, rank `2`, augmented rank `2`, nullity `6`, and the six exact nullspace vectors.
2. Verify all frozen source blob SHAs and authority-chain blob SHAs before classification.
3. Synthetic UNIQUE fixture: six independent exact selector equations plus one common-normalization condition must reduce the V8 affine freedom to a unique coefficient vector and classify `SOURCE_FINITE_RENORMALIZATION_UNIQUELY_FIXED_SCOPED`.
4. Synthetic QUOTIENT fixture: an explicit six-direction quotient with a fixed observable class must classify `SOURCE_FINITE_RENORMALIZATION_QUOTIENT_FIXED_SCOPED`.
5. Synthetic BLOCKED fixture containing relevant vocabulary but no actionable coefficient/constraint/quotient data must classify `SOURCE_FINITE_RENORMALIZATION_AUTHORITY_BLOCKED_SCOPED`.
6. Synthetic INCONSISTENT fixture with exact contradictory same-realization selector equations must classify `SOURCE_FINITE_RENORMALIZATION_CONSTRAINT_INCONSISTENT_SCOPED`.
7. Exact selector-rank/nullity arithmetic must use rational/symbolic decisions only.

## NEGATIVE / ADVERSARIAL CONTROLS

- A generic occurrence of `normalization`, `scale`, `RG`, `gluing`, `composition`, or `renormalization` without an exact selector must not reduce nullity.
- A selector tied to a different object/channel/representation must be rejected as `WRONG_REALIZATION`.
- The V8 canonical representative must not be mistaken for a source-selected coefficient vector.
- A new condition invented by this gate (for example setting scheme A counterterm action to zero) is forbidden.
- Missing authority must never be recoded as coefficient zero or model failure.

## PASS / BLOCKED / FAIL / INVALID

`SOURCE_FINITE_RENORMALIZATION_UNIQUELY_FIXED_SCOPED` iff affirmative frozen same-realization source selectors reduce the V8 affine freedom to one unique coefficient vector and fix the common finite normalization, with all controls passing.

`SOURCE_FINITE_RENORMALIZATION_QUOTIENT_FIXED_SCOPED` iff the frozen source explicitly authorizes a quotient that removes the residual V8 freedom and leaves a unique source-defined equivalence class/observable, with all controls passing.

`SOURCE_FINITE_RENORMALIZATION_AUTHORITY_BLOCKED_SCOPED` iff the frozen source corpus does not provide enough actionable same-realization selector data to fix/quotient the V8 freedom. This is BLOCKED, not FAIL.

`SOURCE_FINITE_RENORMALIZATION_CONSTRAINT_INCONSISTENT_SCOPED` iff affirmative same-realization source constraints exist but are exactly inconsistent with the terminal V8 affine solution space. This is a scoped source/object inconsistency only, not a model/family/global failure.

`INVALID_IMPLEMENTATION` iff source/blob locks, V8 object reconstruction, selector grammar, exact arithmetic, outcome-sensitivity controls, or independent-lane agreement fail.

## INTERPRETATION CEILING

This gate can establish only whether the currently frozen durable structured source corpus fixes, quotients, leaves unfixed, or contradicts the six-dimensional V8 finite-renormalization freedom for the local highest-contact `delta''` triangle. It cannot prove all-mollifier extension existence/nonexistence, full-K5 closure, physical model/family failure, D7 closure, a terminal selector, new physics, or Candidate Gravity authority.

Governance remains `RQIR Core v1.0 = FROZEN`; `D7-S2 = NOT_CLOSED`; `D7-S3 = NOT_CLOSED`; `D7-S4 = PARTIAL_GLOBAL_NOT_CLOSED`; terminal D7 selectors forbidden; Candidate Gravity inactive.