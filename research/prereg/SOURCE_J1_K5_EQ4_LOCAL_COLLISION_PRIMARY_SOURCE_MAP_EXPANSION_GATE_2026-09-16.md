# SOURCE_J1_K5_EQ4_LOCAL_COLLISION_PRIMARY_SOURCE_MAP_EXPANSION_GATE — prospective freeze

Date: 2026-09-16
Status: PROSPECTIVELY_FROZEN_BEFORE_PRIMARY_SOURCE_EXTRACTION_OR_RESULT

## HYPOTHESIS

The frozen same-realization primary-source corpus may contain enough explicit local equations/passages to construct the missing Eq. (4) highest-contact triangle test-jet map that the terminal parent gate left undefined. The gate tests source/object availability and derivability, not whether an absent map should be treated as zero.

## exact OBJECT

Parent object: terminal `SOURCE_J1_K5_EQ4_LOCAL_COLLISION_TEST_JET_MAP_GATE`, classification `EQ4_LOCAL_COLLISION_TEST_JET_MAP_SOURCE_BLOCKED_SCOPED`, terminal result commit `d9b050b638153b7042b8e1ace55952231d936854`.

Required same-realization map fields, frozen exactly as the parent blocker:

1. `LOCAL_COLLISION_CHART`: an explicit local coordinate chart for the source Eq. (4) highest-contact triangle realizing the three collision conormals used by V8 (`B12=x`, `B23=y`, `B13=x+y`) or an explicit source-faithful change of variables to that chart, including Jacobian/normalization authority.
2. `SMOOTH_REMAINDER_OBJECT`: an explicitly isolated smooth test/remainder factor multiplying the singular three-contact distribution in the same Eq. (4) realization, not a synthetic tensor or unrelated one-wedge object.
3. `JET_ORDER_7_COMPLETENESS`: a derivation contract sufficient to determine every derivative of that smooth remainder required by the terminal V8 S3-invariant collision-jet basis through total derivative order 7, with missing derivatives remaining unknown rather than zero.
4. `PERMUTATION_COORDINATE_CONSISTENCY`: an explicit or derivable S3 permutation action/transport on the local chart and smooth remainder sufficient to compare the V8 invariant jets without changing realization.

The gate may record partial fields, but PASS requires all four.

## DEPENDENCY

- terminal V8 local counterterm object: 8 invariant collision jets, V8 rank/augmented rank `2/2`, affine nullity `6`;
- terminal V10 full-primary source expansion: no finite-renormalization selector in the frozen two-source corpus;
- terminal V11 physical-observable quotient: nullspace action rank undefined because local map absent;
- terminal parent V12: Eq. (4) parent identity, Toller insertion identity and parameter/normalization lock confirmed; the four map fields above remain missing.

Historical BLOCKED/FAIL/INVALID/QUALIFIED results remain historical and are not rewritten.

## SOURCE / REALIZATION AUTHORITY

Frozen primary-source corpus only:

- `arXiv:2601.23162v1`, causal vertex / Eq. (4) parent; immutable PDF SHA256 already durably recorded by V10: `cc533af3c84de8c0de57523a795ba4357523712f1746ce55fa78eb521cd51713`.
- `arXiv:2604.24945v1`, Toller/Cartan reconstruction; immutable PDF SHA256 already durably recorded by V10/V12 authority chain.

Repository authority locks:

- `sources/arxiv_2601_23162v1_causal_vertex.json` blob `f0b520fc04904e038f252dc5b45d23893cb020ed`;
- `sources/arxiv_2604_24945v1_toller_cartan.json` blob `8dc58f3c9c191289f2f148469f8df3ea95f77ad5`;
- V10 full-primary audit records;
- `inputs/source_j1_k5_eq4_local_collision_test_jet_map_authority.json` blob `a9b4fdd1da929de9b2e366cc082b9d21511598ca`.

External literature outside these two frozen primary sources cannot decide this gate. Any newly extracted passage must be durably committed with source id, version, page/section/equation anchor and exact interpretation before it can enter the classifier.

## FROZEN INPUTS

- exactly the four required map fields above;
- source Eq. (4) parent identity and Eq. (3)/(7) Toller insertion identity from the parent gate;
- highest-contact local triangle object from V7/V8;
- V8 invariant jet order cap `7`;
- no synthetic tensor substitution;
- no assignment of missing derivatives or map components to zero;
- no post-hoc widening of source corpus or relaxation of required fields.

## POSITIVE CONTROLS

1. Parent terminal locks reproduce exactly: Eq. (4) identity, Toller insertion identity, parameter/normalization identity, V8 basis count 8 and nullity 6.
2. Every actionable extracted field must cite exact primary-source page/section/equation anchors and a deterministic source-record digest.
3. A complete synthetic control containing all four required fields must classify PASS.
4. A partial synthetic control missing exactly one field must classify BLOCKED, not PASS.
5. A coordinate-change control must carry an explicit nonzero Jacobian/normalization transformation rather than silently identifying charts.

## NEGATIVE / ADVERSARIAL CONTROLS

1. Lexical mentions of `local`, `contact`, `normalization`, `composition`, `coordinate`, `permutation`, or `Taylor` without same-realization mappability are non-authoritative.
2. One-wedge contact formulas alone cannot satisfy `SMOOTH_REMAINDER_OBJECT` or `JET_ORDER_7_COMPLETENESS` for the triple-contact Eq. (4) object.
3. Synthetic Iter453 tensors or any nonphysical mock observable are rejected as wrong-realization evidence.
4. A chart without source-authorized Jacobian/normalization cannot satisfy `LOCAL_COLLISION_CHART`.
5. Symmetry language without an explicit/derivable coordinate transport cannot satisfy `PERMUTATION_COORDINATE_CONSISTENCY`.
6. Missing source material must classify BLOCKED and must never be interpreted as zero action, zero residual or scientific FAIL.

## PASS

`EQ4_LOCAL_COLLISION_PRIMARY_SOURCE_MAP_EXPANDED_SCOPED` iff all four required fields are source-authorized and mappable on the same Eq. (4) highest-contact realization, and the order-7 jet construction is complete under the frozen derivation contract.

## FAIL

No physical-model FAIL is authorized by this source-expansion gate. An explicit contradiction in the frozen sources that makes the four-field map mutually inconsistent may classify `EQ4_LOCAL_COLLISION_PRIMARY_SOURCE_MAP_INCONSISTENT_SCOPED`, but this remains a scoped source/object inconsistency, not model/family falsification.

## BLOCKED / INVALID

- `EQ4_LOCAL_COLLISION_PRIMARY_SOURCE_MAP_PARTIAL_BLOCKED_SCOPED` iff at least one but fewer than four required fields becomes actionable and no inconsistency is found.
- `EQ4_LOCAL_COLLISION_PRIMARY_SOURCE_MAP_AUTHORITY_BLOCKED_SCOPED` iff the frozen primary sources provide no complete same-realization map and no actionable new field sufficient to change the parent blocker.
- `INVALID_IMPLEMENTATION` for wrong source versions/digests, wrong parent object, synthetic substitution, post-hoc criterion changes, treating missing values as zero, or classifier/control failure.

## INTERPRETATION CEILING

This gate can only establish source/object availability for the local Eq. (4) collision test-jet map. PASS would authorize a separately prospectively frozen physical-observable nullspace-action/rank gate. BLOCKED does not imply zero action, distributional nonexistence, full-K5/model/family failure, D7 closure, a terminal selector, or Candidate Gravity activation. `RQIR Core v1.0` remains FROZEN.
