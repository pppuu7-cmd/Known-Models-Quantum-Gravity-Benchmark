# SOURCE_J1_K5_EQ4_PHYSICAL_TRANSVERSE_QUOTIENT_PRIMARY_AUTHORITY_ESCALATION_GATE

Date: 2026-09-16
Status: PROSPECTIVELY PREREGISTERED — no expanded-authority outcome consumed

## Parent terminal fact

`SOURCE_J1_K5_EQ4_COMMON_LOCAL_LIE_PULLBACK_TRANSVERSE_RANK_GATE` terminalized as `EQ4_COMMON_LOCAL_LIE_PULLBACK_TRANSVERSE_RANK_BLOCKED_SCOPED`: the raw local Lie map is exact/full on its 12-dimensional gauge-fixed domain, but `physical_transverse_rank=null` because the same-realization physical quotient/projection and downstream normalization/S3 transport are not defined by the previously frozen authority.

## Single question

Does an admissible expanded **primary** authority explicitly define the physical transverse quotient/projection for the same Eq.(4) triangle realization `(12,23,13)` strongly enough to map the raw tangent variables/contact constraints into a physical quotient without guessed conventions?

## Prospectively allowed authority

Only:

1. arXiv:2601.23162v1, PDF SHA256 already frozen as `cc533af3c84de8c0de57523a795ba4357523712f1746ce55fa78eb521cd51713`, including appendices/source package;
2. the published primary article, Phys. Rev. D 113, 126020 (2026), DOI `10.1103/fwql-t4yr`, published 2026-06-15, including its appendices and any publisher-linked official supplementary material;
3. author-provided derivation/code only if explicitly linked from one of those primary records or an official author publication page as material for this paper.

Secondary summaries, textbooks, unrelated spinfoam quotient conventions, historical V8 auxiliary charts, and later independent papers are forbidden as authority for the missing object.

## Required identity fields

A PASS requires an explicit same-realization source definition of all of:

- `PHYSICAL_TRANSVERSE_QUOTIENT_OBJECT`;
- `QUOTIENT_OR_PROJECTION_MAP_FROM_EQ4_LOCAL_TANGENT_VARIABLES`;
- `CONTACT_CONSTRAINT_PULLBACK_TO_QUOTIENT`.

Jacobian/Haar/contact-normalization and S3/orientation transport are recorded if present but are not silently inferred; they remain distinct downstream objects.

## Decision rules

- PASS: all three required quotient identity fields are explicitly present in admissible primary authority with unambiguous variable identity and convention.
- BLOCKED: at least one required field is absent after the complete frozen primary-authority search. Record the minimal missing field; do not substitute a standard quotient.
- INVALID: source set enlarged post hoc, secondary/textbook formula used as authority, variable classes conflated, historical V8 relation reintroduced, or wording is interpreted beyond an explicit source definition.

This is an authority/object-definition gate only. No new rank is computed here.

## Claim ceiling

PASS only authorizes a later normalization/physical-rank gate. BLOCKED is an object-definition ceiling, not transversality FAIL. No D7 closure, model/family failure, selector, Candidate Gravity, or new-theory claim is authorized.
