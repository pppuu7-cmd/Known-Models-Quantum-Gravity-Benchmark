# SOURCE_J1_K5_CONTACT_REPRESENTATION_DERIVATION_V2 — protocol supplement

Date: 2026-09-15
Status: `PROSPECTIVELY_FROZEN_BEFORE_IMPLEMENTATION_OR_RESULT`
Parent preregistration: `research/prereg/SOURCE_J1_K5_CONTACT_REPRESENTATION_DERIVATION_V2_2026-09-15.md`, commit `cd7b4134b1dc465ed5e85f5e749cdf1ef61ac53a`.
Frozen authority-corpus commit: `e7c2546236fea1b6913a253fc354efc769dc544a`.

This supplement does not alter the parent scientific question or terminal taxonomy. It makes the source-authority extraction, frozen inputs, controls, and interpretation ceiling executable and auditable before implementation.

## HYPOTHESIS

For the source-defined `j=1`, real nonzero-`rho`, source-order K5 Eq. (4) object in fixed channel `00000`, the frozen repository authority either contains/derives an exact coherent-contact -> magnetic/intertwiner representation bridge sufficient to contract the dangerous contact component, or it does not. A source-authoritative bridge must be established from parsed mathematical semantics, not from a hand-authored conclusion flag or null.

## OBJECT

The exact object is the coherent-spinor highest contact component used in the frozen coherent-contact Hörmander witness, transferred—if source-authorized—into the same magnetic/intertwiner realization and conventions used by the exact fixed channel `(0,0,0,0,0)` K5 contraction. The independent cubic-pole matrix `Q=diag(1,-2,1)` and coefficient `11/24` remain controls only and are forbidden as substitutes for a coherent-contact coefficient.

## DEPENDENCY

- Parent V2 preregistration at `cd7b4134b1dc465ed5e85f5e749cdf1ef61ac53a`.
- Latest independent Critic at registration: `671596eb4f00d4a8bcc9bfae42453fb0450ff39e`, verdict `INVALID_IMPLEMENTATION` for the historical conclusion-preloaded extraction.
- Frozen repository authority corpus is the immutable Git tree at commit `e7c2546236fea1b6913a253fc354efc769dc544a` under the exact selectors in `FROZEN INPUTS` below.
- Iter504 run `34907349374` and Iter461 run `34748503239` are independent and non-terminal at registration; no partial substantive values may be consumed.

## SOURCE / REALIZATION AUTHORITY

Only mathematical statements durably present in the frozen authority corpus may establish the bridge. The extraction must distinguish:

1. coherent-contact source mathematics (`B(z,g)`, `Theta`, `delta^(rho,1)`, derivatives such as `delta''(B)`);
2. magnetic/spin-1 operator mathematics (`D^1`, spherical magnetic indices/basis, exact rank-2 tensor/matrix/operator);
3. the `rho` convention/map;
4. spherical-basis phases or an exact basis transform sufficient to determine them;
5. edge orientation/action (`g_ab`, left/right or equivalent);
6. normalization/intertwiner convention;
7. an explicit distribution-valued identity/derivation connecting item 1 to item 2, plus enough information to execute the fixed K5 channel-`00000` contact contraction.

A document containing coherent-contact mathematics and another containing an unrelated cubic-pole magnetic operator does not satisfy item 7. Cross-document chaining is admissible only where an explicit equation/definition/reference in the frozen semantics identifies the same distribution-valued object and conventions.

## FROZEN INPUTS

Authority-corpus commit: `e7c2546236fea1b6913a253fc354efc769dc544a`.

Enumerate **all** tracked files at that commit matching at least one of these exact selectors:

- `research/SOURCE_J1*.md`;
- `research/prereg/SOURCE_J1*.md`;
- `code/source_j1*.py`;
- `inputs/source_j1*.json`;
- `recovery/CRITICAL_REVIEW_SOURCE_J1*.md`.

No later file may enter the actual-source evidence. No file matching these selectors may be silently omitted. The certificate must emit the complete enumerated path list and Git blob SHA for every corpus member.

Semantic extraction must split Markdown by headings and Python/JSON by syntactic records, normalize whitespace/case only, and derive bridge candidates from the conjunction of the seven source/realization requirements above. It may emit missing requirements, candidate sections, conflicts, and exact supporting snippets/hashes. It may not consume any pre-existing `actual_*`, manual `source_pinned`, terminal classification, or conclusion-bearing null/boolean as evidence for the actual-source outcome.

Exact arithmetic is required for any contraction. Numerical fallback is not authorized in this gate.

## POSITIVE / NEGATIVE CONTROLS

All controls must pass through the same extraction and classifier used for the actual corpus:

1. `POSITIVE_SURVIVAL`: a synthetic semantic record containing all seven requirements and an exact nonzero contact contraction must classify `CONTACT_SURVIVES_SCOPED`.
2. `ADVERSARIAL_CANCELLATION`: a synthetic semantic record containing all seven requirements with a nonzero pre-contraction contact term but exact contracted coefficient `0` must classify `CONTACT_CANCELS_SCOPED`.
3. `WRONG_CONVENTION`: a near-match with a deliberately wrong `rho` map, spherical phase/basis, normalization, or edge orientation must not qualify; it must classify `INVALID_IMPLEMENTATION` for the fixture.
4. `MISSING_BRIDGE`: coherent-contact evidence plus unrelated cubic-pole magnetic evidence, with no distribution-valued bridge identity, must classify `SOURCE_DERIVATION_BLOCKED_SCOPED`.
5. `LEADING_POLE_GUARD`: a record containing `Q=diag(1,-2,1)` and `11/24` but no coherent-contact bridge must not yield a coherent-contact coefficient.
6. `CONCLUSION_PRELOAD_GUARD`: adding hand-authored `actual_contact_map=null`, `source_pinned=false`, or a terminal label to otherwise unchanged evidence must not change the extracted actual-source predicates.

Any control failure makes the implementation `INVALID_IMPLEMENTATION` and forbids a scientific result.

## PASS

Classify `CONTACT_SURVIVES_SCOPED` iff all controls/source locks pass, the exhaustive frozen-corpus extraction establishes all seven bridge requirements in mutually consistent conventions, the exact fixed channel-`00000` coherent-contact contraction is executed, and the relevant exact coefficient is nonzero.

## FAIL

Classify `CONTACT_CANCELS_SCOPED` iff all controls/source locks pass, the exhaustive frozen-corpus extraction establishes all seven bridge requirements in mutually consistent conventions, the exact fixed channel-`00000` coherent-contact contraction is executed, and the relevant exact coefficient is exactly zero.

This FAIL is only exact cancellation of the tested scoped contact component; it is not a model/family FAIL.

## BLOCKED / INVALID

Classify `SOURCE_DERIVATION_BLOCKED_SCOPED` iff all controls/source locks pass but the exhaustive frozen-corpus semantic extraction lacks at least one of the seven bridge requirements, finds no source-authoritative distribution-valued bridge identity, or cannot execute an exact coherent-contact channel-`00000` contraction without adding model data.

Classify `INVALID_IMPLEMENTATION` if a source lock/corpus selector/control fails, a corpus member is omitted, conventions conflict without a source-authoritative resolution, actual-source evidence is conclusion-preloaded, the classifier is not mechanically connected to parsed/derived evidence, or a cubic-pole control is promoted to the coherent-contact coefficient.

`SOURCE_DERIVATION_BLOCKED_SCOPED != FAIL`; missing map/coefficient != zero coefficient.

## INTERPRETATION CEILING

`CONTACT_SURVIVES_SCOPED` would establish only exact survival of the tested coherent-contact component in this fixed `j=1`, real nonzero-`rho`, channel-`00000`, full-K5-collision scope. It would not by itself prove Eq. (4) distributional nonexistence, global nonuniqueness, family failure, D7 closure, or any terminal selector.

`CONTACT_CANCELS_SCOPED` would establish only exact cancellation of this tested component; it would not prove distributional existence or ordinary convergence.

`SOURCE_DERIVATION_BLOCKED_SCOPED` would establish only that this exhaustive frozen GitHub authority corpus is insufficient to source-authoritatively execute the representation transfer. It would not prove that no bridge exists outside the corpus, that the contact coefficient is zero, or that the model fails.

Governance remains frozen: `RQIR Core v1.0 = FROZEN`; `D7-S2 = NOT_CLOSED`; `D7-S3 = NOT_CLOSED`; `D7-S4 = PARTIAL_GLOBAL_NOT_CLOSED`; terminal D7 labels/selectors remain forbidden; Candidate Gravity remains inactive.