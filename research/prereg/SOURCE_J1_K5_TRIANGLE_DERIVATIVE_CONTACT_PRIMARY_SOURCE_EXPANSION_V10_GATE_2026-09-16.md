# Preregistration — SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_PRIMARY_SOURCE_EXPANSION_V10_GATE

Date: 2026-09-16
Status: PROSPECTIVELY_FROZEN_BEFORE_PRIMARY-SOURCE RETRIEVAL, EXTRACTION, OR RESULT

## HYPOTHESIS

The six-dimensional finite-renormalization freedom left by terminal V8 and source-authority BLOCKED by terminal V9 may be reduced only if the full immutable primary sources defining the same causal/Toller realization contain affirmative same-realization finite-normalization, gluing/composition, RG/scale, extension-prescription, or authorized-equivalence conditions that were omitted from the narrow structured snapshots. Absence of such authority after the frozen exhaustive full-source audit is SOURCE BLOCKED, not scientific/model FAIL.

## EXACT OBJECT

Consume exactly the terminal V8/V9 local highest-contact `delta''` triangle object:

- local normal roots `B12=x`, `B23=y`, `B13=x+y`;
- fixed `j=1`, fixed-tangent/channel-`00000` V4/V5 realization;
- complete S3-invariant collision-supported jet class through derivative order 7;
- exact V8 scheme-equivalence affine solution: basis count 8, rank 2, augmented rank 2, affine nullity 6;
- exact V8 nullspace basis as locked in the terminal V8 canonical JSON;
- terminal V9 current structured-source selector rank 0 and remaining affine nullity 6.

This gate does not retest V7 scheme dependence, V8 absorbability, or V9 structured-source scanning. It tests whether full primary-source text adds actionable authority on the same six-dimensional freedom.

## DEPENDENCY

- terminal V8 scientific result commit: `3e6e069d728ccef2724c26f374c298f29d591043`;
- terminal V9 result commit: `7d3d4de295f3d5b3b5968621ffcb8fd35bfd2bd6`;
- start-of-gate current main: `7be8a59f4937e1de368e9d0bd713ef59badff374`.

Historical FAIL/BLOCKED/INVALID/QUALIFIED records remain preserved. In particular, V9 `SOURCE_FINITE_RENORMALIZATION_AUTHORITY_BLOCKED_SCOPED` is the parent blocker to be challenged only by new affirmative primary-source evidence.

## SOURCE / REALIZATION AUTHORITY

Frozen full-primary-source corpus for this gate consists of exactly:

1. `arXiv:2601.23162v1` — immutable version 1 full primary source (PDF), causal-vertex source already represented narrowly by `sources/arxiv_2601_23162v1_causal_vertex.json`.
2. `arXiv:2604.24945v1` — immutable version 1 full primary source (PDF), Toller/Cartan source already represented narrowly by `sources/arxiv_2604_24945v1_toller_cartan.json`.

No later arXiv version, secondary paper, review, blog, search snippet, or uncited external source may affect the terminal classification of this gate. A later gate may separately prospectively freeze additional sources.

The retrieved PDF bytes must be hashed and the durable source records must include immutable identifier/version, retrieval URL, PDF SHA256, exact page/section/equation anchors, short verbatim excerpts within copyright limits, and a paraphrased authority interpretation.

## FROZEN INPUTS

- terminal V8 canonical object and terminal V9 canonical decision projection;
- exact two-source primary corpus above;
- frozen selector categories inherited from V9:
  - `FINITE_COEFFICIENTS`
  - `FINITE_NORMALIZATION`
  - `GLUING_COMPOSITION`
  - `RG_SCALE`
  - `AUTHORIZED_QUOTIENT`
- frozen search families used only to locate candidate passages, never as authority by themselves:
  `renormal`, `regular`, `normaliz`, `counterterm`, `extension`, `distribution`, `contact`, `gluing`, `composition`, `projector`, `idempot`, `scale`, `RG`, `running`, `finite part`, `scheme`, `equivalent`, `quotient`, `ambigu`, `prescription`.

Every candidate hit must be read in local mathematical context. Lexical occurrence alone is non-actionable.

## ACTIONABLE SELECTOR DEFINITION

A source record is actionable only if all are true:

1. PRIMARY: it is directly supported by one of the two frozen immutable primary PDFs.
2. SAME_REALIZATION: it applies to the same causal/Toller object used by the V4/V5/V7/V8 chain, not generic representation theory or an unrelated one-edge/Haar diagnostic.
3. EXPLICIT: it gives an equation, normalization prescription, composition/gluing law, scale/RG relation, finite coefficient condition, or explicit equivalence/quotient statement.
4. MAPPABLE: its action on the frozen V8 affine coefficient space can be represented as an exact linear constraint or an explicit quotient direction/subspace without inventing extra physics.
5. INDEPENDENT: it is not merely a restatement of V4/V5/V7/V8/V9 inputs.

If mapping requires an unstated convention, missing coefficient identification, or new physical assumption, classify that record as non-actionable and preserve the blocker.

## POSITIVE CONTROLS

- Both frozen PDFs must be retrieved at the exact version and hashed.
- Each durable excerpt record must round-trip to its cited page/section/equation.
- V8 exact affine nullity 6 and V9 selector rank 0 must be reproduced from locked repository inputs before applying any expanded-source selector.
- A synthetic six-independent-constraint fixture must reach unique fixing.
- A synthetic explicit six-direction quotient fixture must reach quotient fixing.
- A synthetic rank-3 selector fixture must reach partial constraint with remaining nullity 3.

## NEGATIVE / ADVERSARIAL CONTROLS

- Generic uses of words such as `normalization`, `scale`, `composition`, `distribution`, or `projector` must not reduce V8 freedom absent SAME_REALIZATION + MAPPABLE authority.
- A selector from the wrong arXiv version must be rejected.
- A selector acting only on a one-edge/Haar object must be rejected.
- A contradictory exact selector fixture must classify inconsistent, not unique.
- Search snippets or secondary descriptions must never become decision-critical authority.

## PASS / PARTIAL / BLOCKED / INCONSISTENT / INVALID

`SOURCE_EXPANDED_FINITE_RENORMALIZATION_UNIQUELY_FIXED_SCOPED` iff the frozen full-primary-source corpus supplies consistent actionable constraints whose exact rank removes all 6 V8 affine directions and fixes common finite normalization.

`SOURCE_EXPANDED_FINITE_RENORMALIZATION_QUOTIENT_FIXED_SCOPED` iff an explicit source-authorized physical quotient removes all remaining V8 freedom, with any residual representative freedom proven quotient-only.

`SOURCE_EXPANDED_FINITE_RENORMALIZATION_PARTIALLY_CONSTRAINED_SCOPED` iff consistent actionable source authority has exact rank 1..5 on the V8 nullspace, leaving remaining affine nullity 5..1.

`SOURCE_EXPANDED_FINITE_RENORMALIZATION_AUTHORITY_BLOCKED_SCOPED` iff both exact primary PDFs are exhaustively audited under the frozen protocol and no actionable same-realization mappable selector is found; missing selector is not zero residual and not FAIL.

`SOURCE_EXPANDED_FINITE_RENORMALIZATION_CONSTRAINT_INCONSISTENT_SCOPED` iff two or more actionable same-realization source constraints are mutually inconsistent on the frozen V8 affine object.

`INVALID_IMPLEMENTATION` for wrong source/version, incomplete frozen-source audit, unverifiable page anchors, altered selector criteria, failure to reconstruct V8/V9 locks, or use of non-primary/secondary evidence as authority.

## INTERPRETATION CEILING

This gate can only establish whether the two frozen full primary sources add finite-renormalization authority for the exact V8 local triangle realization. It cannot prove that no such authority exists in all literature, cannot establish distributional existence/nonexistence, cannot promote the local triangle to full K5/model/family closure, cannot close D7-S2/S3/S4, cannot authorize terminal selectors, and cannot activate Candidate Gravity.
