# Preregistration — source j=1 K5 Eq. (4) additional joint-selection authority v2 repair gate

Date: 2026-09-15
Lane: KMQGB Research / Closure
Status: `PROSPECTIVE_FROZEN`
Gate: `SOURCE_J1_K5_EQ4_ADDITIONAL_JOINT_SELECTION_AUTHORITY_V2_GATE`

## HYPOTHESIS

Within the exact frozen repository-native source-authority corpus below, at least one genuinely joint Eq. (4) selection rule exists whose source/realization identity is pinned and whose action on the Critic-confirmed full-collision witness `Delta(kappa)=c(kappa) delta_N` is explicit or derivable without adding new model data.

This v2 gate repairs only the implementation incompleteness identified by the latest independent Critic. It does not assume the historical v1 classification and does not silently repair that immutable result in place.

## EXACT OBJECT

Exactly the same scoped object as the Critic-confirmed repaired witness:

- source-order gauge-fixed K5 causal vertex;
- fixed `j=1`;
- fixed nonzero real edge-`rho` convention;
- intertwiner channel `00000`;
- full K5 collision stratum `N=SU(2)^4` inside gauge-fixed `SL(2,C)^4`;
- homogeneous witness `Delta(kappa)=c(kappa) delta_N`, with `c=+i` on the 16 constrained causal K5 edge-sign patterns, `c=-i` on their 16 global negatives, and `c=0` on the other 992 independent edge-sign patterns.

The witness is a discriminator of joint selection authority only. No base joint extension `E` is assumed to exist.

## DEPENDENCY

Validated immutable upstream dependencies frozen before this preregistration:

1. `results/SOURCE_J1_K5_JOINT_EXTENSION_CONJUGATION_COUNTEREXAMPLE_REPAIR_RESULT_2026-09-15.md`, result commit `9e4627fd02204b27be3f211660db4c689776d808`;
2. `recovery/SOURCE_J1_K5_JOINT_EXTENSION_CONJUGATION_COUNTEREXAMPLE_REPAIR_HANDOFF_2026-09-15.md`, handoff commit `2eb0da9c987eb706df29971ccea29f65928005d7`;
3. `recovery/CRITICAL_REVIEW_SOURCE_J1_K5_JOINT_EXTENSION_CONJUGATION_REPAIR_2026-09-15.md`, Critic commit `97a4821f85f13e6f5c6d41356612c88939d45f2c`, verdict `CONFIRMED_SCOPED`;
4. `research/SOURCE_J1_K5_PUBLISHED_IEPSILON_SCOPE_RESULT_2026-09-15.md`, source-audit commit `8c6582371d6e763e4f37b30fbe42f67684edcdb7`;
5. `research/SOURCE_J1_K5_ONE_WEDGE_UNIQUENESS_VS_JOINT_EXTENSION_RESULT_2026-09-15.md`, result commit `4b8abb10d89e36801012972ec84f34eb722a5b49`;
6. `research/SOURCE_J1_FULL_COLLISION_SCALING_EXTENSION_AUDIT_2026-09-15.md`, commit `048692cb497ec91a6aa7709e221dc5051a10225f`;
7. `code/lqg_iter331_source_jet_normalization_audit.py`, commit `a59b6bdd08c9fd416dd81df32dfe1d612b5f0d9b`;
8. `code/lqg_iter331_aggregate.py`, commit `8c0502b7e892354a89d5387c5e789514e1d2a9d9`;
9. latest independent Critic of v1, `recovery/CRITICAL_REVIEW_SOURCE_J1_K5_EQ4_ADDITIONAL_JOINT_SELECTION_AUTHORITY_2026-09-15.md`, commit `fcc22dcf4f186b2de3557507242d5186e4e96caa`, verdict `INVALID_IMPLEMENTATION`.

Recovery/current-front reconciliation before this preregistration is current `main` `3cf2ebc3f17ffabee2c1c547e4c3200a002f516a`.

## SOURCE / REALIZATION AUTHORITY

Scientific source authority for this gate is **only** the nine frozen repository records above. No new paper, external search result, private note, chat assertion, or post-freeze source record may be introduced into the v2 decision.

The v2 audit MUST implement a one-to-one matrix for exactly six scientific authority classes, with no row merging and no replacement of a broad class by narrower preselected indicators:

1. **correlated/common regulator-removal** — an Eq. (4)-level common or correlated multiwedge regulator family plus a source-pinned removal/limit prescription;
2. **independent regulator plus path/order theorem** — independent wedge regulators together with a source-pinned theorem establishing path/order independence sufficient to define the joint collision object;
3. **microlocal product/pullback/extension theorem** — a theorem applying to the correlated ten-wedge object and full collision that defines or selects the joint product/extension;
4. **vertex-level normalization or boundary condition** — any source-pinned vertex-level normalization, distributional identity, or boundary condition that fixes collision-supported additions, without reducing the search to preselected K4/K3 jet flags;
5. **nested/forest gluing** — a source-pinned nested-stratum/forest compatibility or gluing rule whose action reaches the full-collision extension;
6. **other genuinely joint source theorem** — any other explicit theorem in the frozen corpus that is genuinely joint and whose action on `Delta` is stated or derivable without new model data.

The implementation MUST expose explicit evidence predicates for all six rows. For class 6 it MUST trace every frozen dependency record and cannot assign absence unconditionally. For class 4 it MUST inspect the complete frozen record text for vertex-level normalization/boundary/distributional-selection authority and may use Iter331 jet flags only as supporting evidence, never as an exhaustive proxy. For class 2 it MUST have a separate path/order-independence predicate distinct from common-regulator authority.

One-wedge `i epsilon`, one-wedge Ruhl/Toller uniqueness, generic scaling-degree extension theory, the independent-sign EPRL sum rule, the already-tested fixed-channel conjugation covariance, and coefficient/sign-pattern K5 relabeling are not admissible as new joint selection authority by themselves.

## FROZEN INPUTS

The audit must consume exactly the nine dependency records above plus this v2 preregistration at the production HEAD. The source-lock job must verify the listed dependency commits are ancestors and hash every frozen input.

The terminal audit record must also contain fresh status/conclusion/head-SHA fields for independent registered runs:

- Iter504 run `34907349374`;
- Iter461 run `34748503239`.

Those workflow statuses are provenance/control inputs only. If either run is non-terminal, **no partial substantive values** from it may be consumed. If either becomes terminal during this gate, that terminal status still does not become a scientific input to the six-class matrix unless a new prospectively frozen gate explicitly adds its result.

No candidate authority may be added after this preregistration. Discovery of genuinely new authority outside the frozen corpus makes this gate `BLOCKED_NEW_SOURCE_OUTSIDE_FROZEN_CORPUS`/invalid for classification and requires a new preregistration.

## POSITIVE CONTROLS

The v2 implementation must recover all of these already validated facts:

1. the published Eq. (3) `i epsilon` is one-wedge authority and the audited displayed Eq. (4) contains no pinned common positive epsilon, vector epsilon, regulator-removal path, or order of ten regulator limits;
2. the companion Toller-paper audit found no ten-wedge correlated positive-regulator family and no regulator path/order-independence theorem resolving K5 collision products;
3. one-wedge Toller uniqueness is insufficient for joint extension and a genuinely joint source condition is required for uniqueness;
4. the fixed-channel full-collision object has `sd_N=30` in codimension `12`, so off-collision data alone leave local same-scaling ambiguity;
5. the Critic-confirmed repaired `Delta` witness is nonzero and survives the already-tested conjugation, sign-pattern K5 permutation, independent-sign EPRL zero-sum, support, and scaling constraints;
6. the latest Critic verdict on v1 is `INVALID_IMPLEMENTATION` specifically because six frozen scientific classes were implemented as five rows, class 2 was not independent, class 4 was narrowed, and class 6 was hard-coded absent.

Failure of any positive control yields `SOURCE_J1_K5_EQ4_ADDITIONAL_JOINT_SELECTION_AUTHORITY_V2_INVALID_IMPLEMENTATION`.

## NEGATIVE CONTROLS

The implementation must reject as sufficient new joint authority:

1. one-wedge `i epsilon` alone;
2. one-wedge Ruhl/Toller uniqueness alone;
3. generic extension-theory existence/nonuniqueness statements that do not select the coefficient;
4. the Iter331 detector/rank/required-jet statements themselves;
5. a synthetic auxiliary regulator or normalization invented by the implementation;
6. a deliberately injected text fixture containing `common regulator` without a removal rule;
7. a deliberately injected text fixture containing `independent regulators` without a path/order-independence theorem;
8. a deliberately injected text fixture containing `normalization` only in a claim-ceiling/nonexistence sentence.

If any negative control is promoted to source-pinned outcome-determining authority, the gate is invalid.

## PASS

Return

`SOURCE_J1_K5_EQ4_ADDITIONAL_JOINT_SELECTION_V2_FORCES_DELTA_ZERO_SCOPED`

iff at least one of the six frozen classes is source-pinned for the same object, its action on collision-supported additions is explicit or derivable without new model data, and applying it to the confirmed witness forces the coefficient of `Delta` exactly to zero, with all controls passing.

## FAIL

Return

`SOURCE_J1_K5_EQ4_ADDITIONAL_JOINT_SELECTION_V2_PRESERVES_NONZERO_DELTA_SCOPED`

iff at least one of the six frozen classes is source-pinned for the same object, its action is outcome-determining without new model data, and it permits a nonzero multiple of the confirmed `Delta`, with all controls passing.

This is a failure only of the hypothesis that the added source authority removes this scoped witness. It is **not** a model/family FAIL.

## BLOCKED / INVALID

Return

`SOURCE_J1_K5_EQ4_ADDITIONAL_JOINT_SELECTION_AUTHORITY_V2_NOT_PINNED_SCOPED`

iff all six one-to-one rows are validly audited and none is source-pinned strongly enough to act on `Delta`. This is a source/object-definition blocker, not scientific falsification.

Return

`SOURCE_J1_K5_EQ4_ADDITIONAL_JOINT_SELECTION_AUTHORITY_V2_ACTION_UNDETERMINED_SCOPED`

iff at least one genuinely joint rule is source-pinned for the same object but its action on `Delta` cannot be determined without adding model data.

Return

`SOURCE_J1_K5_EQ4_ADDITIONAL_JOINT_SELECTION_AUTHORITY_V2_INVALID_IMPLEMENTATION`

if source-lock/provenance fails, the six-row one-to-one contract is violated, any class is narrowed/merged/hard-coded absent, positive or negative controls fail, the frozen corpus changes during execution, partial non-terminal workflow values are consumed, or a candidate authority is invented after freezing.

## INTERPRETATION CEILING

No outcome of this gate may by itself establish:

- existence or nonexistence of a base Eq. (4) joint extension;
- unconditional physical/global nonuniqueness of the published causal vertex;
- source authorization of `Delta`;
- absence of qualifying authority outside the frozen repository corpus;
- impossibility of constructing a new source-compatible joint regulator/product/normalization theorem;
- any result for other spins, channels, model families, lower collision strata, or arbitrary boundary recouplings;
- closure of `D7-S2`, `D7-S3`, or `D7-S4`;
- any terminal D7 selector, including `EXISTING_SUFFICIENT / ADAPT_EXISTING / HYBRID_REQUIRED / NEW_REQUIRED`;
- Candidate Gravity activation;
- `QUANTUM_GRAVITY_SOLVED`, `ALL_KNOWN_MODELS_FAIL`, `NEW_THEORY_REQUIRED`, or `NEW_PHYSICS_FOUND`.

Historical FAIL/BLOCKED/INVALID records remain preserved.

## EXECUTION CONTRACT

The deterministic certificate must:

1. emit exactly six candidate rows with stable unique IDs matching the six classes above;
2. expose per-row evidence predicates and per-record trace entries;
3. separately test common/correlated regulator authority and independent-regulator path/order authority;
4. inspect all frozen dependency record text for class-4 vertex-level normalization/boundary/distributional-selection candidates and distinguish positive authority statements from explicit absence/open-question/claim-ceiling statements;
5. trace class 6 over every frozen dependency record and forbid unconditional absence assignment;
6. run the positive and negative controls before classification;
7. emit hashes for every frozen input and fresh Iter504/Iter461 status metadata supplied by the workflow;
8. classify only after the workflow is terminal; green CI is provenance, not a scientific verdict.

A workflow may use separate `source-lock` and `authority-audit` jobs. This gate is deterministic and bounded; `fail-fast` is not scientifically relevant because no matrix fan-out is required.