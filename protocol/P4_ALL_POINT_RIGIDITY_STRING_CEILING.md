# P4 All-Point Rigidity / String Ceiling

**Status:** permanent P4 comparator boundary.  
**KMQGB iteration:** 088.  
**Purpose:** record that complete amplitude rigidity is mathematically achievable, while preventing an all-point bootstrap rule from being mislabeled as a novel gravity parent when its unique solution is already a string/dual-resonant comparator.

## 1. Positive rigidity control

Recent analytic bootstrap work demonstrates that a small set of global scattering assumptions can become extraordinarily rigid.

In particular, ultrasoft Regge behavior plus a minimal-zero condition can uniquely recover Veneziano/Virasoro-Shapiro-type amplitudes; related five-point Regge-zero plus cyclic/crossing constraints can uniquely determine the corresponding five-point string residue/amplitude up to overall normalization.

Thus the KMQGB problem is **not** that a finite physical principle can never determine an infinite hard hierarchy.

The real requirement is stronger:

`finite principle -> unique all-point hierarchy`

must also satisfy

`hierarchy not already a registered comparator architecture`.

## 2. String ceiling

The following ingredients are now high-pressure string/dual-resonant comparator signals:

- linear Regge spectra selected by ultrasoft high-energy behavior;
- level truncation;
- minimal mandatory Regge zeros;
- hidden-zero/splitting structures that collapse higher-point residues;
- q-deformed dual-resonant recursion;
- worldsheet/monodromy/product representations.

A future P4 synthesis using these structures must prove an independent physical origin and a comparator-orthogonal observable. Merely rediscovering the same unique amplitude in different notation fails A3.

## 3. General lesson from Iter086–087

There are now three distinct failure modes for amplitude-only selection:

1. **dispersion without microscopic spectral law:** functional freedom survives in discontinuities/partial waves;
2. **finite higher-point locus constraints:** null contact directions survive away from the constrained loci;
3. **sufficiently strong global rigidity conditions:** known positive examples often collapse to string/dual-resonant amplitudes.

Therefore a successful parent must derive the global hard data from an independently motivated gravitational object, not merely choose a stronger bootstrap checklist until a unique known amplitude appears.

## 4. Updated first-hard-datum target

The preferred same-spectrum target is now

`finite gravitational parent object`

`-> irreducible nonlocal/nonanalytic 4g datum with fixed normalization`

`-> all-point factorization/crossing completion`

`-> same-parent retarded/CTP continuation`.

If new poles/states are introduced instead, the proposal moves to E2 and must survive string/massive-spin2/tower/ordinary-mediator comparators.

If factorization/probability rules are modified, the proposal moves to E4/E5 and must survive no-signalling/composition/positivity tests.

## 5. Fresh comparator note

The 2026 covariant-LQG UV-fixed-point/spinfoam-stack program provides a strong recent example of a microscopic/discrete parent reducing triangulation ambiguity to finite boundary data. It remains a registered LQG/spinfoam comparator and does not by itself supply the required flat-space hard four-graviton plus same-parent CTP hierarchy.

This raises the comparator bar for future finite-boundary-data proposals.

## 6. Score consequence

No P4 credit. R4 remains 45%.