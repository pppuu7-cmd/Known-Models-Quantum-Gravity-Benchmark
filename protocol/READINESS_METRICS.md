# Stable Readiness Metrics for Candidate Gravity Research

**Status:** frozen reporting methodology.  
**Updated:** 2026-09-09.  
**Current KMQGB authority:** Iter122.  
**Purpose:** keep repository readiness, KMQGB methodology readiness and scientific Candidate Gravity readiness separate and stable across chats/iterations.

These percentages measure different objects and must never be merged.

## R1 — Repository readiness

Frozen component scores:

- recovery/provenance/versioning `20/20`;
- benchmark/comparator corpus `15/15`;
- permanent protocols `20/20`;
- executable layer `12/15`;
- candidate/pre-ansatz scaffold `7/10`;
- automated reproducibility/tests/CI/artifact packaging `8/10`;
- external firewall/synchronization `10/10`.

**R1 = 92%.**

Iter117–122 add recovery/provenance and hard-origin controls but do not close the remaining end-to-end executable candidate-pipeline/artifact gaps. The post-116 ID-race correction is required provenance hygiene, not a new rubric point.

## R2 — KMQGB methodology/material readiness

Frozen component scores:

- benchmark/comparator evidence `15/15`;
- comparator/attribution taxonomy `15/15`;
- completeness/constraints/projection `15/15`;
- residual/identifiability/rigidity `19/20`;
- Beyond-C5 escape/parent-principle methodology `10/15`;
- executable methodology `8/10`;
- consolidated playbook/article-ready synthesis `7/10`.

**R2 = 89%.**

Last score change remains Iter060: `+1` from consolidated playbook/synthesis `6/10 -> 7/10`.

Iter099–116 already established the primitive-hard-datum/complexity-law front. Iter117–122 are score-neutral because

- Iter117/118/119 independently corroborate earlier zero/infinity/multi-shift gates;
- Iter120 adds a GR↔string complex-growth ceiling inside already-scored parent-selection methodology;
- Iter121 reframes the existing black-hole entropy blocker as a density-of-states problem;
- Iter122 shows that adding ETH/random-matrix statistics still leaves smooth graviton transition envelopes, higher non-Gaussian cumulants/correlations and phases.

These sharpen fail-closed methodology but do not produce a novel parent survivor or close a full frozen rubric point.

## R3 — Candidate Gravity scientific readiness

Controlled **only by external RQIR authority**. KMQGB cannot promote this value.

Latest directly observed authority remains **RQIR Iter675** on 2026-09-09.

External stable rubric remains

- comparator foundation `24/25`;
- robust unique residual `0/20`;
- frozen parent dynamics/ANSATZ `0/20`;
- consistency/positivity/Ward/causality `0/15`;
- identifiability/Fisher `0/10`;
- resource/experiment closure `0/10`.

**R3 = 24%.**

Iter675 certifies a present prerequisite-exhaustion state under committed authority. Missing authorities:

- M1 same-parent physical observable bridge;
- M2 matched same-parent Source/Born/contact completion in that physical observable;
- M3 robust nonzero comparator-subtracted residual;
- M4 actual `Tr U1` if required by a future C5 path.

No active promotable ansatz and no robust residual exist. No independent compute gate is currently authorized.

Before every future R3/heavy-compute report, refresh external RQIR authority again.

## R4 — Current research-task completion

Current task: `MINIMAL_NOVEL_PARENT_PRINCIPLE_SEARCH`.

Frozen score:

- P1 known-principle saturation `15/15`;
- P2 multi-representation/background rigidity `15/15`;
- P3 spectral-origin/dispersion requirements `15/15`;
- P4 explicit novel low-freedom parent principle `0/25`;
- P5 immediate comparator survival `0/20`;
- P6 pre-ansatz machine record for a genuine survivor `0/10`.

**R4 = 45%.**

No Iter099–122 negative gate, failed synthesis, positive/corroborative control, recovery action or provenance repair may substitute for P4.

### Current scientific front

The required object is

> a finite gravity-native origin law that fixes **both** the primitive comparator-orthogonal hard datum and the complexity/state/transition data that make it unique.

Main remaining high-value routes:

1. **zero/growth:** derive zero locus + multiplicity + degree/growth/order + normalization from one physical law;
2. **boundary-at-infinity:** derive a non-GR boundary law rather than merely reconstructing it with extra shifts;
3. **intermediate growth:** derive complex order/indicator/Regge/zero-density data physically rather than choosing a minimality objective; GR and string are strong endpoint controls;
4. **E3 nonperturbative:** derive action + measure + state + Lorentzian contour/thimbles + Stokes/transseries data and then a normalized intrinsic hard amplitude;
5. **modified quantum/factorization:** complete probability/composition/no-signalling law plus hard/CTP prediction;
6. **E2 new-state:** derive the entire required tower/spectrum and couplings from finite parent data;
7. **black-hole/strong-gravity:** derive microscopic phases and transition amplitudes; if ETH is used, derive the smooth asymptotic-graviton envelopes and higher cumulants/correlations from the same finite parent.

Positive controls show hard rigidity is possible: MHV gravity hidden-zero plus fixed degree and GR bonus large-z constructibility. The remaining problem is a new physical origin for the rigidity law.

Every serious candidate must also provide all-point generation or a direct route to it, same-parent Lorentzian CTP/retarded continuation, absolute normalization and immediate comparator survival.

## Reporting format

Every future iteration must report

- `Repository readiness (R1): XX%`;
- `KMQGB methodology/material readiness (R2): XX%`;
- `Candidate Gravity scientific readiness (R3): XX%`;
- `Current task completion (R4): XX%`.

Any score change must name the exact frozen rubric component that changed and why.
