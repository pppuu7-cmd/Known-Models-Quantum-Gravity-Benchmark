# Frozen RQIR Protocol Reference Ledger

Purpose: point to literal Candidate Gravity/RQIR authority. This file is a ledger, not a redefinition of the protocol.

## Authority snapshot

- Original benchmark branch base in RQIR: `02ad31e89f1df0d5515779e6b7526e8eb5505667` (Candidate Gravity Iteration 561 at branch creation).
- Migrated source benchmark HEAD: `782df9af3af1ca55ca4f9bf143e2723a7a8f18bc`.
- External RQIR repository: `pppuu7-cmd/Relativity-Quantum-Interface-Reconstruction`.
- Known moving-front path there: `candidate_gravity/recovery/CURRENT_QG_FRONT.md`.
- KMQGB repository: `pppuu7-cmd/Known-Models-Quantum-Gravity-Benchmark`.

## Recovered high-level frozen facts

1. Comparator policy is frozen and cannot be enlarged after observing a candidate/model residual.
2. Known-model benchmark must distinguish exact comparator identity from downstream blocking.
3. KMQGB must not reconstruct Q1–Q7, comparator, quotient, or acceptance rules from chat memory; all such mappings require literal path+commit/blob authority from the external RQIR repository.

## Required literal definitions still to recover before closing GR

The benchmark MUST recover, from repository authority rather than memory:

- exact Q1–Q7 definitions;
- exact comparator span/basis and its frozen provenance;
- exact gate order relevant to the weak-field GR realization;
- exact quotient/residual definition and numerical/analytic acceptance conditions;
- any domain/kinematic restrictions on source/Ward/contact/K2 objects.

Until these are linked below by exact path + commit/blob evidence, GR's `EXACT_COMPARATOR_IDENTITY` is provisional.

## Literal-authority table

| Object | Exact authority path | Commit/blob | Recovery state |
|---|---|---|---|
| Candidate Gravity current front | external RQIR: `candidate_gravity/recovery/CURRENT_QG_FRONT.md` | refresh on each KMQGB iteration | recovered path / moving SHA |
| Q1–Q7 definitions | NOT YET LITERALLY LOCATED | — | OPEN BLOCKER |
| Frozen comparator span | NOT YET LITERALLY LOCATED | — | OPEN BLOCKER |
| Frozen quotient/residual acceptance rule | NOT YET LITERALLY LOCATED | — | OPEN BLOCKER |
| Source/Ward/contact+K2 target contract | external RQIR moving recovery/results authority; exact canonical path to be pinned | — | OPEN |

## Search/index warning

GitHub text/code search has returned false negatives for terms known to exist in the RQIR repository. Therefore zero search hits are not evidence that an authority artifact or prior model audit is absent. Recovery must prefer recursive tree/path inspection and direct file reads.

## Read-only external-authority rule

This repository may fetch/read RQIR authority but MUST NOT write to `pppuu7-cmd/Relativity-Quantum-Interface-Reconstruction` as part of KMQGB work. If the frozen RQIR protocol itself needs a methodological change, that must be treated as a separately authorized/versioned RQIR action, never an implicit benchmark write.

## No-memory rule

If this ledger is incomplete in a future session, do not infer missing definitions from prior chat summaries. Mark the exact item unresolved and continue repository recovery until literal authority is obtained.
