# KMQGB Second-Wave Queue — frozen denominator

Updated: 2026-09-08

The historical first queue remains permanently `9/9 = 100%` terminally classified. This file freezes a separate second-wave denominator of five targets. The second-wave percentage must never be merged into, or retroactively alter, the first-wave denominator.

## Frozen second-wave queue

| S2 index | Target | Exact benchmark intent | Initial state |
|---|---|---|---|
| S2-M01 | weakly nonlocal / entire-form-factor gravity | test whether entire form factors evade the Stelle spin-2 ghost and whether any tree-level RQIR discriminator survives field-redefinition equivalence | ACTIVE |
| S2-M02 | Lorentzian asymptotic-safety spectral realization | test a concrete self-consistent graviton spectral function rather than the program label | QUEUED |
| S2-M03 | classical-channel / measurement-feedback gravity | test explicit CP channel dynamics, interaction/decoherence linkage and entanglement limitation | QUEUED |
| S2-M04 | modern postquantum classical gravity | test a specified stochastic classical-spacetime / quantum-matter realization with current consistency constraints | QUEUED |
| S2-M05 | UV-completion comparator for string-threshold M09 branch | construct a fair same-domain UV comparator so the type-II string pole-tower claim is no longer compared against out-of-domain low-energy C5 | QUEUED |

Second-wave terminal coverage starts at `0/5 = 0%` and advances only on terminal classifications.

## Mandatory rules

1. Every target must be a concrete realization, not a school/program label.
2. `FAIL_RQIR_CONSISTENCY` requires an explicit mandatory consistency-gate failure.
3. `OPERATIONALLY_DEGENERATE` requires a frozen observable and an explicit admissible comparator/nuisance map that reproduces it.
4. A theory outside a comparator's validity domain is `BLOCKED_PROTOCOL_MISMATCH`, not a victory over that comparator.
5. Tree-level equivalence does not imply full quantum equivalence unless separately proved.
6. Nonlocal theories must freeze their Lorentzian/Euclidean amplitude prescription; direct-Minkowski and Euclidean-analytic-continuation prescriptions are not silently conflated.
7. KMQGB may not dispatch heavy shared-runner work while the external RQIR heavy chain is active.

## External protocol authority

RQIR comparator C0-C6 and F0-F7 semantics remain read-only external authority. Second-wave work may cite/import them by provenance but may not write into the RQIR repository.
