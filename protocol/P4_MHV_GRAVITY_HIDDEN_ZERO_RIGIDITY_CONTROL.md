# P4 MHV Gravity Hidden-Zero Rigidity Control

**Status:** gravity-native positive control / comparator authority.  
**KMQGB iteration:** 105.  
**Purpose:** distinguish mathematical uniqueness of a graviton amplitude from novelty of the underlying quantum-gravity parent.

## 1. Control result

Tree-level MHV gravity amplitudes have nontrivial numerator zeros in spinor-helicity space. A 2024/2025 analysis conjectures that, after fixing the appropriate bi-degree, the numerator is the unique polynomial up to an overall factor that vanishes whenever

`<ij> = [ij] = 0`

for any pair `i<j`.

The authors provide computational proofs/evidence for low multiplicities (`n=5,6`) and a program for the general statement.

Thus gravity itself supplies an important positive control:

`finite algebraic data + hidden-zero loci + degree -> near/claimed unique all-n MHV numerator`.

## 2. Why this matters for KMQGB

The result demonstrates that hard graviton amplitudes can exhibit rigidity not visible from pole/factorization data alone.

This is exactly the kind of mathematical compression a future KG parent should exploit: the numerator/hard data need not be an arbitrary coefficient tower.

However the selected object is the ordinary Einstein/Hodges MHV tree amplitude.

Therefore

`RIGID_GRAVITY_AMPLITUDE != NOVEL_QUANTUM_GRAVITY_PARENT`.

## 3. Origin-law distinction

The uniqueness conditions are properties of the already known GR amplitude class. To use an analogous construction for P4, a candidate must prospectively derive

1. **why** the new zero/singularity locus is physically mandatory;
2. the allowed polynomial/transcendental degree or growth class;
3. absolute normalization;
4. extension beyond one MHV tree sector;
5. linked Lorentzian/CTP higher connected observables.

If a zero set is discovered by inspecting the desired amplitude and then declared fundamental, the construction is reverse-engineered rather than predictive.

## 4. Relation to prior gates

This control sharpens the Hadamard and inner/CDD lessons:

- zeros can indeed carry enough information to fix an amplitude;
- finite/structured zero data are therefore a legitimate possible origin channel;
- but the zero law itself must be physically derived and comparator-orthogonal.

Known strong zero-law comparators now include

- Einstein/Hodges MHV gravity hidden zeros;
- string/dual-resonant minimal-zero rigidity;
- CDD/inner-function zero data in elastic scattering.

## 5. P4 use

For any candidate claiming novelty through a new hard zero pattern, ask in order:

`Z1` Is the zero locus specified before fitting the amplitude?

`Z2` Is the locus generated by finite physical data/law?

`Z3` Does the locus plus declared growth/degree determine the hard object up to finite normalization?

`Z4` Is the resulting object distinct from Einstein/Hodges, string/dual-resonant, CDD/integrable and other registered zero-pattern comparators?

`Z5` Does the same parent generate non-MHV/higher-point and CTP observables?

Only a `Z1-Z5` survivor can use zero rigidity as P4 evidence.

## 6. Score consequence

Positive methodological control only. R4 remains 45%.