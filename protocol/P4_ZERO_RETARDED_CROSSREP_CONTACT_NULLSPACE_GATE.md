# P4 Zero–Retarded Cross-Representation Contact Null-Space Gate

**Status:** exact scoped cross-representation P4 prefilter.  
**KMQGB iteration:** 114.  
**Purpose:** test whether combining a hard hidden-zero law with causal/retarded real-time consistency can by itself eliminate the local gravitational contact freedom left by each constraint separately.

## 1. Candidate combined principle

A tempting stronger selector is

> choose the irreducible gravitational hard kernel as the unique object that simultaneously obeys a specified on-shell hidden-zero law and is the boundary value/continuation of a causal retarded/CTP kernel.

This combines S-matrix and response representations and is stronger than either condition alone.

## 2. Local contact counterfamily

Let `Z(kinematics)` be a crossing/helicity-compatible factor that vanishes on the proposed hard zero locus.

Let `P_D` be any allowed local polynomial/tensor structure through derivative order `D` such that

`Delta K4 = Z * P_D`

obeys the declared Ward, crossing and helicity constraints.

Then

1. `Delta K4` vanishes on the same zero locus;
2. as a local contact interaction it has no new physical factorization poles;
3. its Lorentzian retarded/CTP vertex is local in spacetime and admits the ordinary causal prescription;
4. it can therefore satisfy both the zero constraint and real-time causal consistency without being uniquely selected.

Hence the intersection of the two constraint sets contains the allowed local contact subspace.

## 3. Cutoff-dependent freedom

Define

`X_D = { Delta K4 : hidden-zero law + local causal/CTP continuation + all declared symmetries through order D }`.

Unless an additional physical law fixes the derivative/growth order,

`dim X_D`

generically increases as new generally covariant contact structures become available.

Therefore

`hidden zeros + causal retarded continuation`

is not by itself a finite hard-origin law.

Classification:

`CROSSREP_ZERO_PLUS_CAUSALITY__FULL_C5_CONTACT_NULLSPACE_REMAINS`.

## 4. Why this matters

KMQGB requires cross-representation rigidity, but this result shows that representation multiplicity alone does not guarantee origin uniqueness.

The same local interaction can be represented consistently as

- an on-shell polynomial/contact amplitude;
- a local 1PI vertex;
- a retarded/advanced/CTP local kernel.

Requiring agreement among these representations tests implementation and causality, not necessarily the value of the contact coefficients.

## 5. How the gate can be closed

A zero/CTP-based parent must additionally derive at least one of

1. a strict degree/growth/order law that leaves a finite candidate space;
2. a nonlocal/nonanalytic structure whose causal continuation is uniquely generated;
3. a nonperturbative contour/state law fixing the hard sector;
4. a modified quantum/factorization law with its own probability/no-signalling completion.

If a fixed degree plus zero law yields a unique object, continue immediately to A3 against GR/higher-derivative/string/CDD comparators.

## 6. Relation to prior gates

This is the cross-representation extension of

- `P4_HIDDEN_ZERO_CROSS_ORDER_NONUNIQUENESS_GATE.md`;
- `P4_SAME_FACTORIZATION_CONTACT_DIFFERENCE_THEOREM.md`;
- the retarded/Herglotz causality controls.

It closes the loophole that simply adding a CTP representation to a zero-based S-matrix selector could remove the local EFT tower.

## 7. Score consequence

No P4 credit. R4 remains 45%.