# P4 Principal-Symbol / Equivalence-Principle Operator Nonuniqueness Gate

**Status:** exact operator-origin prefilter.  
**KMQGB iteration:** 132.  
**Purpose:** test whether local covariance/equivalence-principle propagation data can uniquely select the finite microscopic operator required by the successful operator-generator blueprints.

## 1. Principal symbol controls propagation, not the full operator

For a second-order covariant operator, the principal symbol determines the high-frequency characteristic cone. Many distinct operators can share the same principal symbol.

A minimal exact witness is the scalar family

`Delta_xi = -Box + m^2 + xi R`.

For every value of `xi`, the principal symbol is

`sigma_2(Delta_xi)(x,k) = g^(mu nu)(x) k_mu k_nu`.

Therefore all members have the same local metric characteristic/light cone and satisfy the same leading local-equivalence propagation structure.

Yet they differ physically through the curvature coupling `xi R`.

## 2. Consequence for generated quantum gravity functionals

If the operator is inserted into

`Gamma_1 = 1/2 Tr log Delta_xi`,

the different `xi` values generate different curvature terms, nonlocal form factors and metric vertices.

Thus

`same causal principal symbol != same quantum gravitational effective interaction`.

The same structural issue generalizes: lower-order endomorphism/curvature terms, gauge/ghost complexes, boundary conditions and nonminimal structures are not fixed by the principal symbol alone.

## 3. A2 classification

A candidate principle of the form

> choose the unique microscopic operator compatible with local Lorentz invariance, diffeomorphism covariance and the equivalence-principle light cone

is insufficient.

Those conditions can fix the leading kinetic/characteristic structure while leaving lower-order and interaction data.

Classification:

`A2 BLOCKED__EQUIVALENCE_PRINCIPAL_SYMBOL_FIXES_CAUSAL_CONE_NOT_FULL_OPERATOR`.

## 4. Stronger symmetry may select a member but becomes extra parent data

For special matter systems, additional principles can select particular curvature couplings—for example conformal symmetry can pick a conformal Laplace-type operator in the appropriate dimension.

This does not invalidate the gate. It demonstrates the required logic:

`principal symbol + additional physical symmetry -> more operator data`.

A Candidate Gravity parent must identify and derive the **additional gravity-native law** that removes all remaining operator freedom without simply choosing a desired member.

## 5. Relation to Iter127/131

- Iter127: finite wave operator + causal boundary conditions can derive a complete non-rational `G2` spectral function.
- Iter131: finite operator + determinant can generate an all-point one-loop metric functional.

The present gate identifies the remaining primitive:

`what physically selects Delta itself?`

The answer cannot be merely “the equivalence principle”, because the equivalence principle does not determine lower-order operator data.

## 6. P4 operator-origin checklist

For an operator-based parent require

`O1` principal symbol/causal cone derived;

`O2` all lower-order curvature/endomorphism terms derived;

`O3` gauge/constraint/ghost complex derived;

`O4` state/boundary/Lorentzian contour derived;

`O5` local renormalization/null sector fixed;

`O6` interacting hard `4g`/higher observable generated with absolute normalization;

`O7` all-point + CTP completion;

`O8` comparator survival against ordinary covariant QFT, induced gravity, AS, LQG/spinfoam, nonlocal/spectral-action and string/matrix architectures.

## 7. Search consequence

The next strong candidate should supply a **new operator-selection law**, not merely a new operator ansatz. A finite generator becomes P4-relevant only when its complete operator/measure/state data follow from the same gravity-native principle.

## 8. Score consequence

No P4 credit. R4 remains 45%.
