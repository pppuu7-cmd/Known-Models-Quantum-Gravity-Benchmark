# P4 GR Bonus-Scaling Constructibility Control

**Status:** gravity-native rigidity positive control / comparator authority.  
**KMQGB iteration:** 115.  
**Purpose:** show that a physically structured large-complex-momentum growth law can genuinely close the boundary/degree freedom of a graviton tree hierarchy, while separating that mathematical rigidity from novelty.

## 1. Positive control

Under the standard two-line BCFW deformation, Einstein-gravity tree amplitudes display the well-known bonus behavior

`A_n(z) ~ 1/z^2`

for the relevant shifts.

On-shell analyses show that this improved scaling is tied to Bose symmetry rather than being merely an artifact of a Feynman-diagram representation.

When the boundary term at infinity vanishes, the tree amplitude is recursively reconstructed from finite factorization poles and lower-point amplitudes.

Thus, after the GR three-point seed and physical spectrum are fixed,

`GR lower data + bonus scaling/no boundary + factorization`

provides a genuinely rigid tree-level positive control.

## 2. Relation to hidden zeros

Hidden-zero work gives an analogous lesson: zeros can be equivalent to enhanced subset large-`z` scaling in suitable rational amplitude classes.

Therefore the combined data

`zero law + degree/growth law + factorization`

can be much more rigid than a zero locus alone.

This validates KMQGB's requirement that a future zero-based parent derive its global growth/order rule rather than merely list zeros.

## 3. Why this is not P4 novelty

If a candidate preserves

- the Einstein massless spectrum;
- the Einstein three-graviton amplitudes;
- the same bonus scaling strong enough to eliminate the boundary term;

then ordinary on-shell recursion reconstructs the Einstein tree hierarchy.

Classification:

`A2_RIGIDITY_PASS__A3_FAIL_GR_TREE_CONSTRUCTIBILITY_CONTROL`.

The mathematical uniqueness is real; the selected object is the comparator.

## 4. What a new parent would have to change

A genuinely new hard tree datum must therefore change, in a prospectively derived way, at least one of

1. lower-point physical states/residues;
2. the allowed boundary-at-infinity law;
3. the global growth class;
4. the factorization/quantum law;
5. a nonperturbative sector invisible to the rational tree recursion.

Changing none of these while retaining no boundary gives no room for a new tree-level KG amplitude.

## 5. Stronger search target

The desired zero/growth route is now

`finite gravity-native physical principle`

`-> new zero/growth/boundary law`

`-> finite unique hard amplitude`

`-> all-helicity/all-point generator`

`-> same-parent CTP/retarded hierarchy`

`-> comparator survival`.

The phrase `maximally soft` is not itself enough: why that softness is physically mandatory must be derived, and if the result is string/dual-resonant or weakly-nonlocal it is comparator-contained.

## 6. Score consequence

Positive methodological/comparator control only. R4 remains 45%.