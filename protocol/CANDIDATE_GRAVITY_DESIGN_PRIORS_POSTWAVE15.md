# Candidate Gravity Design Priors — Post-Wave-15 Supplement

**Status:** permanent design supplement. Read together with `protocol/CANDIDATE_GRAVITY_DESIGN_PRIORS.md`.  
**Authority:** KMQGB design evidence only; no external RQIR readiness promotion.

## A. Beyond-C5 escape declaration

Any future exploratory KG record must state how it exits the local analytic full-C5 class. Allowed structural doors are tracked as

- E1 nonlocal/nonanalytic same-spin-2 dynamics;
- E2 extra propagating DOF/mediator;
- E3 genuinely nonperturbative/transseries sector;
- E4 modified fundamental quantum dynamics;
- E5 modified symmetry/causal/microstructure.

A seed that cannot identify an escape door is comparator-contained by construction.

## B. Cheapest seeds already eliminated

Wave 16 eliminates as standalone KG seeds:

- local analytic metric-only deformation -> C5 EFT;
- EOM/Ricci-type entire nonlocal deformation -> scoped on-shell/tree field-redefinition degeneracy;
- state-only/squeezed deformation -> C5 state freedom;
- arbitrary quadratic CTP kernel -> generic open-system/C2/C5 structure;
- extra scalar quantum mediator -> C4/modified-gravity comparator space.

## C. Causality correction to E1

A cubic-curvature seed that changes the graviton three-point coupling carries a high-energy causality tax in the weakly coupled CEMZ regime. A minimal no-extra-tower search therefore preferentially preserves

- GR propagator;
- GR three-graviton vertex;

and moves the first exploratory departure to four-point/higher order.

This is a search-priority rule, not a theorem that all cubic deformations are impossible. Any escape must state which CEMZ assumption is changed and how causal consistency is restored.

## D. Quartic-first rigidity object

In 4D GREFT with quartic-first `C3=0`, define

`f2=C4,1-C4,2`,

`g2=C4,1+C4,2`,

`rho4=f2/g2`.

In the scoped local/polynomial positivity class, `-1<rho4<1`.

`rho4` is an IR anchor, not KG novelty. A serious parent must predict a linked tower of helicity/crossing coefficients `a_(k,j)` from few shared parameters, and must survive holdout across helicity/order/energy/angle blocks.

## E. Spectral-moment rigidity and its limit

For suitable parity-preserving local dispersive graviton eigenchannels, EFT coefficients form moments of a positive-semidefinite spectral-density matrix and associated Hankel matrices are positive semidefinite.

Use moment/Hankel constraints as cross-order consistency diagnostics.

Do **not** interpret low-rank or saturated Hankel structure as KG-specific: simple ordinary spectral support can generate it.

## F. Dispersion-class attribution

Before positivity/moment analysis, classify the amplitude growth/analyticity assumption.

- `D-local`: local/polynomially or appropriately Regge-bounded dispersive class after required graviton-pole/IR treatment.
- `D-nonlocal`: exponentially bounded/nonlocal class requiring its own modified dispersion/positivity treatment.

Applying local positivity formulas to a genuinely nonlocal candidate without proof is `BLOCKED_DISPERSION_CLASS_MISMATCH`.

## G. Kinematic projection-rank completeness

If parent response kinematics have dimension `n` and the target/native observable has dimension `m<n`, a unique local pullback requires `n-m` additional independent same-parent constraints or an equivalent prospectively frozen lift `x=gamma(s)`.

Required rank condition:

`rank([J_target; J_aux]) = n`.

Without it, target derivatives/discontinuities/root Jacobians/residuals are not uniquely defined and downstream comparator/COR is blocked.

External RQIR Iter611 is the current motivating example: three independent source invariants map to one native hard variable, so two auxiliary constraints are required.

## H. Current preferred pre-ansatz search region

The most conservative same-spin-2 branch currently is

`GR 2-point + GR 3-point + parent-fixed quartic/higher cross-regime structure`,

with

- no arbitrary form-factor fitting;
- few shared parameters;
- full Ward/crossing/contact response;
- declared dispersion class;
- four-graviton helicity/moment anchors;
- same-domain UV/nonlocal comparator quotient;
- exact kinematic lift to target observables;
- cross-order/intervention/holdout rigidity;
- COR plus global separation.

No kernel or tensor ratio is yet derived strongly enough for G0 PASS. No KG ansatz is promoted.
