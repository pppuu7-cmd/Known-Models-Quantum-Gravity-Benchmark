# P4 Celestial-Ward All-Point Gravity-Native Control

**Status:** gravity-native positive control / comparator evidence / no P4 credit.  
**KMQGB iteration:** 094.  
**Purpose:** record that an all-point graviton hierarchy can, in a controlled sector, be generated from genuinely gravitational asymptotic-symmetry/Ward data rather than from a matrix spectral curve, while identifying why this still does not provide a new Candidate Gravity parent.

## 1. Fresh positive control

Recent 2026 work revisits single-minus tree-level graviton amplitudes. In a restricted kinematic decay region, the all-`n` amplitude simplifies to a product of soft factors and, with suitable analyticity assumptions, is generated recursively by an `L w_{1+infinity}` Ward identity from the three-graviton amplitude as seed.

This is important for KMQGB because it exhibits the desired architecture

`finite low-point gravity seed + gravity-native Ward recursion -> all-point sector`.

It is therefore a stronger structural control than matrix/topological recursion for the specific question of gravity-native generators.

## 2. A1/A4 positive features

The seed and recursion are tied directly to graviton scattering and asymptotic/celestial symmetry rather than to an externally chosen scalar form factor.

In the controlled kinematic sector, nontrivial higher-point amplitudes are produced from lower-point gravitational data.

Thus the example demonstrates that a gravity-native all-point generator is not impossible in principle.

## 3. A3 — Einstein-gravity containment

The construction concerns graviton amplitudes of ordinary Einstein gravity. It therefore provides a GR/amplitude comparator, not a comparator-orthogonal Candidate Gravity hard datum.

Classification:

`A3 FAIL_FOR_KG_NOVELTY__EINSTEIN_GRAVITY_SCATTERING_SECTOR`.

## 4. A2 — symmetry alone is not a unique parent

Independent celestial analyses show that `w_{1+infinity}` invariance admits a discrete infinite family of celestial theories, including the MHV sector and quantum self-dual gravity as members.

Therefore the symmetry algebra and associated null-state constraints do not by themselves select one complete four-dimensional bulk dynamics.

Classification:

`A2 BLOCKED_IF_ONLY_W1INFINITY_SYMMETRY_IS_DECLARED__MULTIPLE_INVARIANT_THEORIES_EXIST`.

## 5. Soft-versus-hard lesson

Asymptotic/celestial Ward identities are extremely effective at transporting soft information through an amplitude hierarchy. They do not automatically determine the first comparator-orthogonal hard irreducible datum.

This parallels Iter085:

`Ward/factorization recursion = propagation of hard/soft information, not necessarily its origin`.

A future P4 parent must either

- derive a new hard seed in addition to the symmetry algebra, or
- contain an additional finite principle that selects one member of the symmetry-compatible family and fixes its hard data.

## 6. Design target sharpened

The strongest current synthesis target is therefore

`gravity-native finite seed selector`

`+ all-point Ward/causal generator`

`+ unique hard member selection`

`+ same-parent CTP/retarded realization`.

The desirable feature to copy from this control is the gravity-native all-point recursion; the GR hard seed itself is comparator-contained.

## 7. Score consequence

No score changes. `R1=92%`, `R2=89%`, `R3=24%`, `R4=45%`.