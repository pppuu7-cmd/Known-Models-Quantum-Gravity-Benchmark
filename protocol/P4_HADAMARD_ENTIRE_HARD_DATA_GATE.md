# P4 Hadamard Entire Hard-Data Gate

**Status:** permanent S2/nonlocal hard-function prefilter.  
**KMQGB iteration:** 097.  
**Purpose:** identify exactly where the independent data of a finite-order entire four-point hard function reside.

## 1. Hadamard factorization

For an entire function `F(z)` of finite order, Hadamard factorization writes schematically

`F(z)=z^m exp(Q(z)) Prod_n E_p(z/z_n)`,

where

- `{z_n}` are the nonzero zeros with multiplicity;
- `Q(z)` is a polynomial of bounded degree determined by the growth order;
- `E_p` are canonical factors required for convergence.

Thus a finite-order entire function is controlled by its zero set plus finitely many polynomial-exponential data.

## 2. Zero-free branch

If `F` has no zeros, the canonical product disappears and

`F(z)=exp(Q(z))`

up to the trivial zero-at-origin factor if present.

Therefore a zero-free finite-order entire hard dressing is not an arbitrary new class. It belongs to a finite exponential-polynomial family.

For KMQGB this immediately triggers

- weakly/nonlocal entire-form-factor comparators;
- field-redefinition/tree-equivalence audits;
- exponential-growth/causality tests.

It does not, by itself, supply a new gravity-specific parent.

## 3. Infinite-zero branch

If the hard function has infinitely many zeros, the zero set `{z_n}` is essential dynamical information.

Merely specifying

`entire + crossing + finite order`

therefore does not satisfy A2. A serious parent must derive the zero sequence/multiplicities from finite physical data.

If the zero locations are independently adjustable as resolution grows, the functional-freedom no-go applies.

## 4. Comparator pressure from rigid zero sets

Known amplitude bootstrap/string constructions demonstrate that finite global conditions can rigidly generate infinite Regge-zero/pole structures. The 2026 `Strings from Almost Nothing` positive control is particularly strong: ultrasoft Regge behavior and minimal mandatory zeros can collapse to Veneziano/Virasoro-Shapiro/string structures.

Thus an entire hard function with a beautifully generated infinite zero set is not automatically novel. Its zero-generation law must survive string/dual-resonant/q-deformed comparator containment.

## 5. Combined S2 conclusion

Together with Iter096:

- entire + polynomial bounded -> polynomial/contact -> C5;
- zero-free finite-order nonpolynomial entire -> `exp(Q)` weakly-nonlocal comparator class;
- finite-order entire with infinite zeros -> zero-set origin must be derived, with strong string/bootstrap comparator pressure;
- infinite-order entire -> even weaker growth/locality class and correspondingly stronger causality/dispersion taxes.

Therefore the word `entire` hides no free P4 escape.

## 6. Score consequence

No score changes. `R1=92%`, `R2=89%`, `R3=24%`, `R4=45%`.