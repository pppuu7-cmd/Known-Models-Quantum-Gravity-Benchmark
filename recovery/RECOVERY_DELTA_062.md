# RECOVERY_DELTA_062 — Finite-Rule QCA Triage + MISP2-QCA Target + RQIR621

**Date:** 2026-09-08  
**KMQGB iteration:** 062  
**External RQIR observed:** Iteration 621, `MODEL_READINESS=24%`.

## Stable readiness

- R1 repository readiness: **92%**.
- R2 KMQGB methodology/material readiness: **89%**.
- R3 Candidate Gravity scientific readiness: **24%**.
- R4 `MINIMAL_NOVEL_PARENT_PRINCIPLE_SEARCH`: **45%**.

No score changes in Iter062. The iteration narrows the constructive P4 search but does not yet supply the explicit interacting parent required for P4 credit.

## New permanent authority

`protocol/P4_FINITE_RULE_QCA_TRIAGE.md`.

The generic target `finite microscopic dynamical selector` is sharpened to an explicit classification problem:

`MISP2-QCA = Minimal Isotropic Self-interacting Physical-spin-2 Quantum Cellular Automaton`.

This is a research target, not a promoted model.

## Three finite-rule controls

### F1 — Chrono-Grid Dynamics

A microscopic lattice Hamiltonian/update is explicit enough for provisional A1/A2 interest, but the published architecture maps directly into Ashtekar/SU(2), spin-network, 6j, spin-foam/BF and Turaev-Viro structures.

Classification:

`A3 FAIL__DISCRETE_LQG_SPINFOAM_BF_COMPARATOR_CONTAINED`.

The complete many-tetrahedra Hamiltonian constraint remains an open problem in the published program, and no KMQGB hard four-graviton/CTP A4 object is available.

### F2 — QICT gauge-coded QCA

The worked 3+1D model uses a finite-depth local circuit with finite-dimensional quantum-link gauge registers, a constrained spin-2/tetrad register and constraint ancillas.

Classification:

- A1 `PROVISIONAL_PASS`;
- A2 `PROVISIONAL_PASS_ONLY_IF_GATES_ARE_MICROSCOPICALLY_FIXED`;
- A3 `UNRESOLVED_FOR_HARD_QG_OBJECT`;
- A4 `BLOCKED_MISSING_REQUIRED_OBJECT`.

This is the strongest audited finite-rule near-survivor architecture, but it is not P4 because the required derived hard four-graviton/CTP hierarchy is absent.

### F3 — hard-amplitude string uniqueness positive control

`Strings from Almost Nothing` shows that sufficiently strong scattering assumptions can collapse hard functional freedom onto Veneziano/Virasoro-Shapiro amplitudes.

Thus A2/A4-style selection can genuinely occur, but A3 fails because the selected amplitudes are precisely registered string/dual-resonance comparators.

## New spin-2 locality rule

A strictly local finite-range microscopic parent should not take an exact TT-only field as a local fundamental register.

The transverse projector contains a `1/k^2` factor; in position space exact TT extraction is spatially nonlocal. Therefore the S-local MISP2-QCA branch must use local gauge-redundant tensor/tetrad/connection variables plus local constraints/stabilizer ancillas (or explicitly declare nonlocal kinematics).

The physical two helicities must emerge after constraint/gauge reduction.

## MISP2-QCA A1 target

Freeze prospectively:

- a homogeneous 3D graph/Cayley lattice;
- finite local Hilbert dimension;
- finite interaction radius;
- fixed finite circuit depth or bounded local Hamiltonian rule;
- exact unitary update `U(theta)`;
- a finite parameter vector `theta` independent of perturbative order/volume;
- local gauge-redundant spin-2 variables and exact local constraints;
- exactly two gapless helicity-2 IR physical modes;
- no unwanted gapless gravitational scalar/vector modes;
- low-k Lorentzian propagation;
- universal GR self-coupling with one shared Newton scale.

## Microscopic A2 rigidity test

In addition to the ordinary hard-basis `FF_D`, define the dimension of the allowed microscopic gate manifold after unitarity/symmetry/constraint/basis redundancies:

`d_gate`.

For induced hard coefficients `c_m(theta)`, increasing hard order must create more predictions without creating one independent parameter per order.

Required behavior:

`rank(d c_1,...,d c_M / d theta) <= dim(theta)`

with fixed `dim(theta)` as `M` grows.

Growing circuit radius/depth or inserting independent higher-order gates to fit each coefficient is a functional-freedom failure.

## First real A4 target

Only after the constrained interacting update is fixed, derive from the same `U(theta)`:

1. two physical graviton branches and residues;
2. cubic GR self-coupling;
3. connected physical `2->2` helicity-2 amplitude / Floquet T-matrix;
4. first finite-rule correction beyond matched C5/GR;
5. same-parent retarded/CTP response.

The first possible novelty is a shared-parameter correction derived from the same finite update, not generic lattice dispersion.

## Exact Q1-Q5 program

1. **Q1:** choose minimal homogeneous/isotropic graph and local gauge-redundant spin-2 register.
2. **Q2:** classify the free constrained unitary symbol and require exactly two gapless helicity-2 modes.
3. **Q3:** classify the smallest interacting local update preserving the constraint/code subspace and derive the cubic GR vertex.
4. **Q4:** derive quartic/higher rigidity from the same update; do not add an independent quartic gate.
5. **Q5:** only after Q1-Q4 close, compute physical four-graviton/Floquet scattering and same-parent CTP, then run comparator profiling.

## Compute decision

No heavy computation is authorized yet. Q1-Q4 are algebraic/symbolic classification gates. Large-lattice simulation before the update rule is frozen would explore an underdetermined gate space and could not produce P4 authority.

## RQIR621 sync

New authority: `external_rqir_checks/iter621_normalization_graph_rank_audit.md`.

The source convention sector and native `Gamma3/chi2R/connection` sector form exactly two internally connected normalization components with zero frozen cross-sector bridge equations. After quotienting one common convention there is exactly one relative complex degree of freedom

`N_native in C*`.

Thus root-specific or q2-specific normalizations are not hidden allowed freedoms; they would contradict the frozen authority graph.

RQIR readiness remains 24%; heavy numerical work cannot supply the absent source-to-Gamma3 bridge.

## Exact continuation front

The next useful work is **Q1/Q2**, not another broad theory survey:

- formalize the exact local-TT obstruction;
- choose the smallest prospective local gauge-redundant spin-2 register/graph;
- solve or fail-closed the free two-helicity classification;
- only then attempt the interacting constraint-preserving gate.
