# KMQGB Critical Review — Eq. (4) additional joint-selection authority v2

Date: 2026-09-15
Lane: independent KMQGB Critical Review / Verification
Repository: `pppuu7-cmd/Known-Models-Quantum-Gravity-Benchmark`

## RESULT_REVIEWED

Exactly one latest terminal substantive Research result is reviewed:

- `results/SOURCE_J1_K5_EQ4_ADDITIONAL_JOINT_SELECTION_AUTHORITY_V2_RESULT_2026-09-15.md`;
- historical result commit `807eabe7a03f53b354e298bd90c4f409b4adf502`;
- Research classification `SOURCE_J1_K5_EQ4_ADDITIONAL_JOINT_SELECTION_AUTHORITY_V2_NOT_PINNED_SCOPED`.

Frozen object: source-order gauge-fixed K5 causal vertex, fixed `j=1`, fixed nonzero real edge-rho convention, intertwiner channel `00000`, full K5 collision `N=SU(2)^4` in gauge-fixed `SL(2,C)^4`, with the previously Critic-confirmed discriminator `Delta(kappa)=c(kappa) delta_N`.

The review does not consume partial scientific values from Iter504 or Iter461 and does not create a competing verdict for either non-terminal workflow.

## PREREG_CHECK

Prospective chronology is valid:

1. v2 preregistration commit `b50934de049c6597c3732a0d1a334c2cabb4302f`;
2. implementation commit `edc1bfa00b720e07cb08c2a2961e32267dee1a23`;
3. workflow/production head `586f0e373b80518982960c3759ec8bba4249d848`;
4. terminal Actions run `34935737476`;
5. terminal result commit `807eabe7a03f53b354e298bd90c4f409b4adf502`.

A direct compare from preregistration to production head shows only the new implementation and workflow were added. The source-lock job also verifies ancestry and unchanged frozen inputs. No post-prereg mutation of the nine frozen dependency records was found.

The frozen decision contract requires an outcome-capable audit of exactly six authority classes and explicitly provides PASS, FAIL, ACTION_UNDETERMINED, BLOCKED and INVALID branches.

## OBJECT_IDENTITY_CHECK

The implementation and result retain the preregistered fixed `j=1`, nonzero-real-rho, channel-`00000`, full-K5-collision witness object. No wrong-spin, wrong-channel, reordered ten-spectral surrogate, lower-stratum substitution, or family-level promotion was found.

The defect identified below is decision-implementation validity, not object identity.

## SOURCE/REALIZATION_CHECK

The frozen source corpus is exactly the nine preregistered repository records plus the preregistration. The workflow source lock records byte hashes for those inputs and verifies the listed historical commits are ancestors of the production head.

The narrow negative source facts used by the implementation are genuinely present in the frozen records: the displayed Eq. (4) audit reports no pinned common/vector regulator-removal path; the companion source audit reports no correlated ten-wedge family/path-order theorem; the one-wedge result says one-wedge uniqueness is insufficient for the joint extension; the scaling audit records the ten-wedge correlated collision product as `NOT_YET_JUSTIFIED`; and the Iter331 structured rows record their audited normalization/gluing fields as false.

Those facts are not themselves the implementation defect.

## PROVENANCE_CHECK

Authoritative Actions provenance is complete:

- run `34935737476`: `completed/success`;
- source-lock job `104273108027`: `success`;
- authority-audit job `104273131510`: `success`;
- artifact `10382678434`, name `source-j1-k5-eq4-additional-joint-selection-authority-v2`;
- artifact size `18501` bytes;
- artifact digest `sha256:ddc5863943033748441a6fd12452736349b5520b2721042d5624b7946e3da0cf`;
- canonical Action JSON SHA256 `f24fb2e9efd7e2f97dcb037b98534b812024885c812fc6d07f37ad81830d6820`.

Green CI is treated as provenance only. It does not cure the decision-function defect below.

Fresh independent workflow status check at review time:

- Iter504 run `34907349374`: `queued`, conclusion `null`, head `56362459a826e3e376c529446678f2fbcaa269ae`;
- Iter461 run `34748503239`: `queued`, conclusion `null`, head `05c7f87c8519349057332bf90021f1128e1eefc3`.

No partial substantive values from either workflow entered this review.

## SAME_REALIZATION_CHECK

The reviewed v2 gate uses the same scoped collision witness and same upstream Critic-confirmed source-order realization as preregistered. Hash-locked upstream dependencies match the workflow ledger. No source/version drift or realization swap was found.

However, same-realization provenance does not establish that the implemented six-class decision matrix is outcome-sensitive.

## NUMERICAL/STATISTICAL_CHECK

This gate is deterministic/source-logical rather than statistical. No confidence intervals, sampling thresholds, covariance estimates, or numerical fits determine the result.

The decisive implementation fact is exact code logic: all six matrix rows are constructed with literal status

`NOT_SOURCE_PINNED_IN_FROZEN_CORPUS`

and default `action_on_delta=None`.

The later classifier searches the matrix for rows whose status is `SOURCE_PINNED_ACTS_ON_DELTA` or `SOURCE_PINNED_BUT_ACTION_UNDETERMINED`. No code path in the six row constructions can assign either status. Consequently, whenever the Boolean controls pass, `acts=[]`, `undetermined=[]`, and the classifier necessarily returns the BLOCKED label.

Therefore PASS, FAIL, and ACTION_UNDETERMINED are unreachable scientific outcomes of the executed implementation, despite being mandatory frozen branches.

## COUNTEREXAMPLE_ATTEMPTS

### 1. Exact six-row repair

The v2 implementation does repair the previous five-row shape defect: it emits six stable IDs and separates the independent-regulator/path-order row from the common-regulator row. This counterexample to the old Critic is accepted.

It is insufficient because every repaired row is still assigned the same absence status literally.

### 2. Outcome-sensitivity counterexample — class 4

Frozen class 4 is any source-pinned vertex-level normalization, distributional identity, or boundary condition that fixes collision-supported additions.

The implementation checks selected negative facts and then appends

`C4_VERTEX_LEVEL_NORMALIZATION_BOUNDARY_CONDITION`

with literal status `NOT_SOURCE_PINNED_IN_FROZEN_CORPUS`. Its `broad_class_not_reduced_to_jet_flags` predicate is itself set to `True`; it is not a detector. The class-4 trace covers only a subset of frozen records and does not promote any discovered positive authority into a source-pinned row.

Explicit implementation counterexample: if a frozen dependency record not used by the class-4 Boolean predicates contained a valid source-pinned vertex-level boundary condition that fixed the collision-supported coefficient, while all currently checked negative strings remained present, all current controls could still pass and class 4 would still be emitted as absent. The generic keyword trace would merely record the text; the code states that those hits do not drive the scientific decision.

Thus the implemented decision function cannot realize the frozen class-4 PASS/FAIL/UNDETERMINED branches.

### 3. Outcome-sensitivity counterexample — class 6

The v2 code now traces every frozen dependency record for class 6, which repairs the earlier missing-record trace.

But the row status is still appended literally as `NOT_SOURCE_PINNED_IN_FROZEN_CORPUS`. The per-record `positive_authority_if_any` fields are authored summaries, and the `verified` predicates check selected expected sentences; neither can set a source-pinned row or an action on `Delta`.

Explicit implementation counterexample: a qualifying genuinely joint theorem could coexist elsewhere in a frozen record with the sentence used by that record's `verified` predicate. The record would remain `verified=true`, the keyword trace could show the theorem, but the row would remain hard-coded absent and the classifier would still return BLOCKED.

### 4. PASS/FAIL branch reachability

The classifier contains syntactic PASS and FAIL branches, but no row-construction branch can produce `SOURCE_PINNED_ACTS_ON_DELTA`, `FORCES_ZERO`, or `PRESERVES_NONZERO`. Therefore those branches are dead with respect to the executed six-class matrix.

This is stronger than a general concern: it is a direct code-level proof that the frozen outcome-determining alternatives are unreachable.

### 5. ACTION_UNDETERMINED branch reachability

Likewise, no row can receive `SOURCE_PINNED_BUT_ACTION_UNDETERMINED`, so the frozen action-undetermined branch is unreachable.

### 6. Green-CI promotion

Rejected. Actions success proves that the predetermined decision function executed reproducibly, not that the decision function implements the frozen scientific contract.

### 7. Opposite scientific conclusion

Not inferred. This review does not prove that a qualifying joint authority is actually present in the current nine-record corpus, does not authorize `Delta`, does not prove base-extension existence, and does not prove physical/global nonuniqueness. It invalidates only the claim that the executed v2 program outcome-sensitively audited the frozen alternatives.

## OVERCLAIM_CHECK

The Research result's written claim ceiling is otherwise appropriately scoped: it does not promote the blocker to model FAIL, family closure, base-extension nonexistence, source authorization of `Delta`, or global physical nonuniqueness.

The overclaim is narrower but decisive: the statement that the repaired executable audit established that none of the six frozen classes is source-pinned exceeds what the executable decision function can certify, because all six statuses are predetermined absent and positive/action-undetermined states are unreachable.

The actual current frozen corpus may indeed contain no qualifying authority, but that proposition is not validated by this terminal executable gate under its own preregistered decision contract.

## VERDICT

`INVALID_IMPLEMENTATION`

Reason: the prospectively frozen gate requires an outcome-capable six-class authority decision with reachable PASS, FAIL, ACTION_UNDETERMINED and BLOCKED alternatives. The executed v2 code constructs every scientific row with literal `NOT_SOURCE_PINNED_IN_FROZEN_CORPUS` and `action_on_delta=None`; therefore, once controls pass, BLOCKED is predetermined. The added six-row structure, record traces and keyword capture do not feed positive authority or action-on-Delta findings into row status. This is a direct mismatch between implementation and frozen contract.

## QUALIFICATIONS

1. The historical v2 Research result remains immutable; this Critic does not rewrite it.
2. `INVALID_IMPLEMENTATION` does not imply the opposite scientific result and does not prove that a source-pinned joint rule exists.
3. Narrow upstream negative source facts remain useful evidence but cannot substitute for an outcome-sensitive six-class decision.
4. The previously Critic-confirmed `Delta` witness remains `CONFIRMED_SCOPED` within its prior fixed realization; this review does not broaden or retract it.
5. Iter504 and Iter461 remain non-terminal and untouched.
6. No terminal D7 selector is licensed.

## UPDATED_STATE

- `SOURCE_J1_K5_EQ4_ADDITIONAL_JOINT_SELECTION_AUTHORITY_V2_NOT_PINNED_SCOPED = INVALID_IMPLEMENTATION` as a validated downstream premise.
- Historical v1 authority result remains `INVALID_IMPLEMENTATION` under its prior Critic.
- Upstream `SOURCE_J1_K5_FROZEN_CONJUGATION_CONSTRAINTS_LEAVE_NONZERO_COLLISION_AMBIGUITY_SCOPED = CONFIRMED_SCOPED` remains unchanged.
- `D7-S2 = NOT_CLOSED`.
- `D7-S3 = NOT_CLOSED`.
- `D7-S4 = PARTIAL_GLOBAL_NOT_CLOSED`.
- Terminal D7 labels and `EXISTING_SUFFICIENT / ADAPT_EXISTING / HYBRID_REQUIRED / NEW_REQUIRED` remain forbidden.
- Candidate Gravity remains inactive.
- RQIR Core v1.0 remains FROZEN.

## NEXT_ADMISSIBLE_GATE

Do not treat the v2 BLOCKED result as a validated scientific premise and do not repair it retrospectively.

A further authority audit, if pursued, requires a new prospectively frozen gate whose implementation is demonstrably outcome-sensitive before execution. At minimum:

1. each of the six classes must have a detector/evidence evaluation capable of assigning `SOURCE_PINNED_ACTS_ON_DELTA`, `SOURCE_PINNED_BUT_ACTION_UNDETERMINED`, or `NOT_SOURCE_PINNED_IN_FROZEN_CORPUS` from frozen inputs;
2. positive fixture controls must prove that a qualifying rule in each class changes the row status and reaches PASS/FAIL/UNDETERMINED as appropriate;
3. negative fixtures must still reject incomplete/negated pseudo-authority;
4. class-4 and class-6 discovery traces must feed the decision rather than being diagnostics only;
5. any action on `Delta` must be derived from the frozen source rule, not invented by the implementation.

Alternatively, a separately prospectively frozen Eq. (4) distributional-existence gate may proceed only if it does not use this invalid v2 blocker as a validated premise and preserves all existing source/object/governance guards.
