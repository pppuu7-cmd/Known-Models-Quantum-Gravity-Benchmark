# KMQGB — Restore from a New Chat

This file assumes no usable chat memory.

## Required order

1. Open `pppuu7-cmd/Known-Models-Quantum-Gravity-Benchmark`, branch `main`.
2. Read `recovery/CURRENT_BENCHMARK_FRONT.md`, `recovery/state.json`, and `recovery/CANDIDATE_GRAVITY_RESEARCH_HANDOFF.md`.
3. Read `protocol/READINESS_METRICS.md` and `protocol/MINIMAL_NOVEL_PARENT_PRINCIPLE_SEARCH_RUBRIC.md` before reporting progress.
4. Read permanent protocols named in current front/state, especially `protocol/P4_FUNCTIONAL_FREEDOM_NO_GO.md`, `protocol/COMPOSITION_GLUING_DUALITY_P4_PREFILTER.md` and `protocol/P4_CONSTRUCTIVE_SEARCH_NEGATIVE_LEDGER.md`.
5. Read `code/p4_functional_freedom_reference.py`, candidate schema/validator and highest-numbered immutable recovery delta.
6. Treat waves 1–38 as terminal historical authority.
7. Fetch external RQIR `candidate_gravity/recovery/CURRENT_QG_FRONT.md` read-only before R3/readiness/heavy-compute claims.
8. Resume from `next_actions` in `recovery/state.json`; do not reconstruct state from chat memory.

## Stable percentages at Iteration 059

- `R1 Repository readiness = 92%`;
- `R2 KMQGB methodology/material readiness = 88%`;
- `R3 Candidate Gravity scientific readiness = 24%` from external RQIR Iter616;
- `R4 current minimal-novel-parent-principle search = 45%`.

Always report all four separately. Wave38 and Iter059 do not increase R4 because no explicit constructive P4 survivor exists yet.

## Immutable coverage

- Wave 1: `9/9 = 100%`.
- Waves 2–37: each `5/5 = 100%`.
- Wave 38: `5/5 = 100%`, constructive prefilter / no R4 credit.

Coverage is not Candidate Gravity readiness.

## Current architecture

`Gamma_parent = Gamma_C5,full_matched + DeltaGamma_KG`,

with

`full matched C5 through 3-point + first candidate-specific independent 4-point/higher structure`,

and shared parameters across order/configuration/representation/background/holdout blocks.

The target parent must be a finite microscopic **dynamical selector**, not merely a gluing/composition/duality/overlap consistency scaffold.

## Functional-freedom kill test

For proposed principle `P`, define its structural-null hard-deformation space `N_P`. At hard/EFT cutoff `D`, define

`FF_D(P)=dim N_P(D)`.

If `FF_D` keeps growing with `D`, classify the proposal

`FUNCTIONAL_FREEDOM_BLOCKED`.

Such a proposal leaves an arbitrary hard function/spectral measure/Wilson tower and therefore cannot by itself satisfy P4's low-freedom requirement.

With massless four-point `s+t+u=0`, the reference scalar crossing-symmetric basis is generated by `sigma2=s^2+t^2+u^2` and `sigma3=stu`; leaving higher coefficients free provides an explicit growth witness. This is only a prefilter: candidate-specific gravity analysis still needs the exact physical helicity/tensor basis and all stated constraints.

## Strong comparator/precedent set

- major modern LQG/GFT/tensor/CFS/QFT-vector programs have scoped hard-observable audits;
- BFSS/IKKT define the benchmark standard for microscopic parent derivation into gravity observables;
- self-sourced spectral closure substantially overlaps asymptotic-safety spectral RG;
- causal-diamond projector gluing is a scaffold rather than unique dynamics selector;
- associator/nonassociative composition overlaps relative-locality and string R-flux comparator spaces;
- UV/IR/Born reciprocity overlaps metastring/T-duality comparator space;
- overlap-spectrum consistency has a close Holographic Space-Time precedent.

## Repository CI

`.github/workflows/methodology-ci.yml` runs on GitHub-hosted `ubuntu-latest`. Iter059 adds the P4 functional-freedom reference self-test. Do not claim the newest push run passed unless its workflow conclusion has been directly observed; the historically confirmed first methodology run `34266786359` completed `success`.

## External RQIR state

Latest directly observed authority: **Iteration 616**, R3 **24%**.

Iter616 closes exact source/Iter582 q2-bucket identity and exact external scalar endpoint amputation. The remaining native-binding ambiguity is exactly one common nonzero normalization/phase scalar `N_native`.

The next gate is an independent lower-order/common-field normalization identity fixing `N_native` prospectively. No matched comparator residual/ANSATZ/Fisher/resources yet. No heavy compute is required for this gate.

## Next research front

Constructive P4 only. For each proposed finite microscopic dynamical selector, apply `FF_D` first. Only a finite-freedom survivor should receive detailed hard-amplitude/CTP derivation and immediate comparator profiling.

## Minimal continuation instruction

`Продолжай KMQGB с Iter059. Восстанови current front/state/handoff/latest delta, READINESS_METRICS и P4_FUNCTIONAL_FREEDOM_NO_GO. Считай волны 1–38 терминальными. Всегда сообщай R1/R2/R3/R4. Обнови внешний RQIR read-only. Для нового P4-кандидата сначала вычисли/оцени FF_D; рост без ограничения => FUNCTIONAL_FREEDOM_BLOCKED. Ищи finite microscopic dynamical selector -> normalized hard 4pt/higher relation -> same-parent CTP/retarded block. Отрицательные prefilters R4 не повышают.`
