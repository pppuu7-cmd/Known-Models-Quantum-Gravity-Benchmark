# KMQGB — Restore from a New Chat

This file assumes no usable chat memory.

## Required order

1. Open `pppuu7-cmd/Known-Models-Quantum-Gravity-Benchmark`, branch `main`.
2. Read `recovery/CURRENT_BENCHMARK_FRONT.md`, `recovery/state.json`, and `recovery/CANDIDATE_GRAVITY_RESEARCH_HANDOFF.md`.
3. Read `protocol/READINESS_METRICS.md` and `protocol/MINIMAL_NOVEL_PARENT_PRINCIPLE_SEARCH_RUBRIC.md` before reporting progress.
4. Read both design-prior files and permanent protocols named in current front/state.
5. Read candidate schema/validator and highest-numbered immutable recovery delta.
6. Treat waves 1–32 as terminal historical authority.
7. Fetch external RQIR `candidate_gravity/recovery/CURRENT_QG_FRONT.md` read-only before R3/readiness/heavy-compute claims.
8. Resume from `next_actions` in `recovery/state.json`; do not reconstruct state from chat memory.

## Stable percentages at Iteration 052

- `R1 Repository readiness = 92%`;
- `R2 KMQGB methodology/material readiness = 87%`;
- `R3 Candidate Gravity scientific readiness = 24%` from external RQIR Iter611;
- `R4 current minimal-novel-parent-principle search = 45%`.

Always report all four separately. R4 may no longer increase from additional negative audits; the next scoreable stage is a constructive P4 survivor.

## Immutable coverage

- Wave 1: `9/9 = 100%`.
- Waves 2–32: each `5/5 = 100%`.

Coverage is not Candidate Gravity readiness.

## Current preferred search architecture

`full matched C5 through 3-point + first candidate-specific independent 4-point/higher structure`,

with shared physical parameters across order, configuration, representation, allowed backgrounds and holdout blocks.

## Current hard-origin rule

For coefficient combinations controlled by a valid local gravitational dispersive sum rule, a candidate-specific beyond-C5 shift requires changed UV spectral/Regge support. Otherwise the candidate must explicitly enter a `D-nonlocal` branch or remain blocked.

Naive forward positivity is not accepted in gravity; use the proper loop/subtraction/smearing/crossing prescription.

## Current failed kernel-selection mechanisms

No constructive P4 survivor has emerged from spectral action, zeta spectral action, diffusion/semigroup exponential, retarded Stieltjes kernels, minimal spectral support, amplitude bootstrap/string minimality, universal double copy, self-dual integrability or known dual-resonance deformations.

## Repository CI

`.github/workflows/methodology-ci.yml` runs on GitHub-hosted `ubuntu-latest`. First run `34266786359` completed `success`; it does not use the RQIR self-hosted runner.

## External RQIR state

Latest directly observed authority: **Iteration 611**, R3 **24%**. Three independent source invariants must map to one native hard coordinate; two same-parent auxiliary constraints or an equivalent prospective trajectory are still missing. No comparator quotient/ANSATZ/Fisher/resources until the bridge closes.

## Next research front

Constructive P4 only: identify one explicit low-freedom physical parent principle that derives a hard/cross-representation relation and survives immediate full-C5/string/AS/C4-C6/nonlocal/spectral-geometry containment checks. Only then create a new pre-ansatz candidate record and increase R4.

## Minimal continuation instruction

`Продолжай KMQGB и полезные наработки для будущей Candidate Gravity. Восстанови current front/state/handoff/latest delta, READINESS_METRICS и R4 rubric. Считай волны 1–32 терминальными. Всегда сообщай R1/R2/R3/R4. Обнови внешний RQIR read-only. Следующий фронт — конструктивный P4; отрицательные аудиты сами по себе R4 больше не повышают.`
