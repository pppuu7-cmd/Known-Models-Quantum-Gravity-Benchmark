# KMQGB — Restore from a New Chat

This file assumes no usable chat memory.

## Required order

1. Open `pppuu7-cmd/Known-Models-Quantum-Gravity-Benchmark`, branch `main`.
2. Read `recovery/CURRENT_BENCHMARK_FRONT.md`, `recovery/state.json`, and `recovery/CANDIDATE_GRAVITY_RESEARCH_HANDOFF.md`.
3. Read `protocol/READINESS_METRICS.md` before reporting progress percentages.
4. Read both design-prior files and every permanent protocol named by current front/state.
5. Read the candidate schema/validator and highest-numbered immutable recovery delta.
6. Treat waves 1–29 as terminal historical authority.
7. Fetch external RQIR `candidate_gravity/recovery/CURRENT_QG_FRONT.md` read-only before R3/readiness/heavy-compute claims.
8. Resume from `next_actions` in `recovery/state.json`; do not reconstruct state from chat memory.

## Stable readiness metrics at Iteration 050

- `R1 Repository readiness = 90%`;
- `R2 KMQGB methodology/material readiness = 86%`;
- `R3 Candidate Gravity scientific readiness = 24%` from external RQIR Iter611;
- always report current task `R4` separately.

Do not merge these percentages.

## Immutable coverage

- Wave 1: `9/9 = 100%`.
- Waves 2–29: each `5/5 = 100%`.

Coverage is not Candidate Gravity readiness.

## Current preferred KG search architecture

`full matched C5 through 3-point + first candidate-specific independent 4-point/higher parent structure`,

with shared parameters across

- response order;
- controlled configuration;
- physical representation (scattering / CTP / relational channel);
- allowed backgrounds;
- prospective training/holdout blocks.

A future parent must derive its tensor/kernel hierarchy, radiative closure, Lorentzian/CTP prescription, dispersion class and exact kinematic lift.

## Current mandatory comparators/gates

In addition to C0–C6/C3b and full C5:

- string/Virasoro-Shapiro and broader dual-resonance/bootstrap;
- asymptotic-safety momentum-dependent scattering/spectral structures;
- universal double-copy breadth warning;
- cross-representation comparator coverage;
- cross-background EFT consistency;
- modern state-dependent nonlocal curvature-history comparator;
- kinematic projection-rank completeness.

Missing comparator representations are `BLOCKED`, not zero.

## External RQIR state

Latest directly observed authority: **Iteration 611**, Candidate Gravity readiness **24%**. Current blocker: three independent source invariants must map to one native hard variable, so two same-parent auxiliary constraints are still required. No comparator quotient/ANSATZ/Fisher/resources until that bridge closes.

## Next research front

Search for a minimal parent principle that both selects a low-dimensional hierarchy and creates a common-domain relation outside all registered comparator manifolds. In particular, test principles that link scattering, real-time/CTP and relational or curved-background observables through one small shared parameter set.

Do not launch heavy compute or promote KG until such a structural object survives upstream gates.

## Minimal continuation instruction

`Продолжай KMQGB и полезные наработки для будущей Candidate Gravity. Восстанови current front/state/handoff/latest delta и READINESS_METRICS. Считай волны 1–29 терминальными. Всегда сообщай R1/R2/R3/R4. Обнови внешний RQIR read-only. Следующий фронт — minimal novel parent principle with cross-representation/cross-background rigidity; KG ansatz не продвигать до полного promotion gate.`
