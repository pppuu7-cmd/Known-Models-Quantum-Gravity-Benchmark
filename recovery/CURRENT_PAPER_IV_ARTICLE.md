# CURRENT PAPER IV ARTICLE — canonical cross-chat recovery authority

Updated: 2026-09-11
Repository: `pppuu7-cmd/Known-Models-Quantum-Gravity-Benchmark`
Target journal: Classical and Quantum Gravity
Article role: RQIR / KMQGB Paper IV

## Canonical working manuscript

**Current canonical manuscript:**

`paper_iv/PAPER_IV_CQG_DRAFT_v1_1.md`

Canonical manuscript commit:

`74f7a9532b817585071c7ca41177356b9a05821e`

Canonical manuscript audit:

`paper_iv/PAPER_IV_CQG_DRAFT_v1_1_AUDIT.md`

Machine-readable recovery manifest:

`recovery/PAPER_IV_ARTICLE_MANIFEST.json`

## Historical manuscript — preserve unchanged

`paper_iv/PAPER_IV_CQG_FIRST_DRAFT_v1_0.md`

v1.0 is the valid historical **Iter274 / 14-family** snapshot. Never overwrite it or silently replace its historical `1/14` values. All further manuscripts must be new files (`v1_2`, `v1_3`, ...).

## Current scientific snapshot

Current coverage authority:

`protocol/PAPER_IV_MAJOR_FRAMEWORK_COVERAGE_CONTRACT.json` — Iter275 / schema v1.3.

Current D7 readiness authority:

`paper_iv/PAPER_IV_D7_READINESS_STATE.json` — Iter276 / schema v1.1.

Current state:

- RQIR Core v1.0 = `FROZEN`;
- Tier-1 total = `15`;
- strict terminal Tier-1 rows = `1/15`;
- strict nonterminal Tier-1 rows = `14/15`;
- Tier-2 unresolved = `0`;
- internally actionable fronts = `0`;
- operational proving-ground readiness = `100%`;
- D7-S0 = `PASS`;
- D7-S1 = `PASS`;
- D7-S2 = `NOT_CLOSED`;
- D7-S3 = `NOT_CLOSED`;
- D7-S4 = `PARTIAL_GLOBAL_NOT_CLOSED`;
- D7-S5 = `NOT_AUTHORIZED`;
- D7-S6 = `INACTIVE`;
- Paper-IV global decision = `NOT_YET_AUTHORIZED`;
- Candidate Gravity remains inactive;
- `EXISTING_SUFFICIENT`, `ADAPT_EXISTING`, `HYBRID_REQUIRED`, and `NEW_REQUIRED` are not authorized.

### Validation caveat

The Iter276 D7 readiness file states that fresh Iter276 synchronization validation is pending. `recovery/CURRENT_BENCHMARK_FRONT.md` still lags at Iter274 / 14 rows. Do **not** use that stale front file to override the newer coverage contract or Iter276 D7 readiness. Before submission, freeze the article against a validated immutable release and then remove this temporary implementation caveat from the manuscript.

## Why the census is now 15 rather than 14

Iter275 promoted `RELATIONAL_QUANTUM_CAUSAL_PROCESSES` / RQCP-QG to Tier-1 under the already-existing rule

`new_concrete_independent_parent_triggers_tier1_promotion = true`.

The promotion did not modify RQIR Core or the terminal criteria. It increased the census from 14 to 15 while the strict terminal count stayed at one. This prospective denominator worsening is now an important Paper-IV result: a new independent candidate is admitted even when its inclusion makes the benchmark look less closed.

Transition authority:

`paper_iv/PAPER_IV_ITER275_ARTICLE_TRANSITION.md`

RQCP audit:

`paper_iv/P_RQCP_NEW_PARENT_PROMOTION_AUDIT_ITER275_2026-09-11.md`

## Current article thesis

Paper IV is not a review and not an exclusion census. It executes a pre-frozen cross-framework observable/comparator protocol over a declared, prospectively extensible quantum-gravity census. Framework-native results are admitted to a family-level comparison only after family scope, same-realization provenance, physical transport, normalization and comparator definition are sufficiently closed. Missing objects remain `BLOCKED` or `PARTIAL`; they are not transformed into negative residuals.

Permanent semantic guardrail:

`BLOCKED != FAIL`.

## Current principal results

### 1. Operational saturation versus scientific closure

KMQGB can be operationally saturated with respect to all currently admissible internal work while the physical theory-selection problem remains scientifically nonterminal. Current strict family-level terminality is only `1/15`.

### 2. Proposition 1 — protocol-level no-promotion

A scoped PASS or FAIL cannot entail parent-family sufficiency or exclusion while a material branch remains unresolved or a required same-realization/common-domain residual is undefined.

Do **not** market this as a new theorem of statistics or logic. The bare logical idea has close analogues in composite hypotheses and partial identification. The article's defensible contribution is its operationalization with physical realization vectors, common-domain residuals and explicit family-scope promotion across the QG census.

Prior-art audit:

`paper_iv/PAPER_IV_PROPOSITION1_PRIOR_ART_AUDIT_v0_1.md`

### 3. Precision cannot repair structural undefinedness

More numerical precision in an adjacent calculation cannot replace a missing physical ancestry map, normalization identity, observable or comparator definition.

### 4. Current five-class primary obstruction projection

Classes:

- C1 — family / branch scope;
- C2 — same-realization transport;
- C3 — amplitude / comparator completeness;
- C4 — explicit beyond-baseline correction object;
- C5 — physical-state / relational-observable closure.

Iter275 deterministic baseline over the 14 nonterminal families:

`(5,6,1,1,1)`.

Bounded alternatives:

- transport-first: `(2,9,1,1,1)`;
- scope-maximal: `(6,5,1,1,1)`.

The exact C1/C2 split is projection-dependent. The robust quantitative result is

`C1 + C2 = 11/14 = 78.6%`.

Therefore v1.1 should emphasize that most current nonterminality occurs upstream of a terminal numerical residual at family-scope and/or same-realization transport. Do not call the five classes universal quantum-gravity failure modes.

Machine authority:

`paper_iv/PAPER_IV_OBSTRUCTION_TAXONOMY_v1_1.json`

### 5. Prospective frozen-judge stress tests

- Iter273 LQG/spinfoam: new Lorentzian fixed-point/continuum evidence narrows the UV-side blocker without family terminal promotion.
- Iter274 Asymptotic Safety: Lorentzian contact-term progress attacks the frozen amplitude blocker but still lacks the stable public contact-complete terminal package.
- Iter275 RQCP: a new independent parent is added under a pre-existing coverage rule, worsening the terminal fraction from `1/14` to `1/15` without changing the judge.

## LQG equation attribution lock

Use:

`paper_iv/PAPER_IV_LQG_EQUATION_ATTRIBUTION_AUDIT_v0_1.md`

Direct external relations:

- Bianchi & Rincon-Ramirez: `2 f_GB/f_CS = gamma - 1/gamma` and the ideal inflationary `1/gamma - gamma = (pi/8)(r+8n_T)/Pi` relation in their chosen gamma-dual model;
- Dittrich 2026 area-metric realization: `sinh(2xi)=1/gamma` and `psi=-(1/2) arctan(tanh xi)`.

KMQGB definitions / algebraic reductions:

- `Delta_gamma := rho-(gamma-1/gamma)`;
- generalized `q=1/gamma-gamma-Delta_gamma`;
- `q=-rho` and `beta_q=-beta_rho` under the common residual convention;
- `beta_Delta=beta_rho-(1+1/gamma^2)beta_gamma`;
- `gamma=-cot(4psi)`;
- `Delta_gamma=2cot(8psi)-q`;
- `beta_Delta=-16csc^2(8psi)beta_psi-beta_q`.

The apparent sign mismatch is resolved; algebraic consistency passes. The physical EPRL→area-metric→EFT same-realization bridge remains open.

## Article support files

Use the following current files, not their older versions:

- canonical manuscript: `paper_iv/PAPER_IV_CQG_DRAFT_v1_1.md`;
- audit: `paper_iv/PAPER_IV_CQG_DRAFT_v1_1_AUDIT.md`;
- claim/evidence control: `paper_iv/PAPER_IV_CQG_CLAIM_EVIDENCE_MATRIX_v0_2.md`;
- 15-family primary literature map: `paper_iv/PAPER_IV_PRIMARY_LITERATURE_EVIDENCE_TABLE_v0_2.md`;
- bibliography: `paper_iv/references_cqg.bib`;
- obstruction taxonomy: `paper_iv/PAPER_IV_OBSTRUCTION_TAXONOMY_v1_1.json`;
- Proposition prior-art audit: `paper_iv/PAPER_IV_PROPOSITION1_PRIOR_ART_AUDIT_v0_1.md`;
- LQG attribution audit: `paper_iv/PAPER_IV_LQG_EQUATION_ATTRIBUTION_AUDIT_v0_1.md`;
- Iter275 transition: `paper_iv/PAPER_IV_ITER275_ARTICLE_TRANSITION.md`;
- machine recovery manifest: `recovery/PAPER_IV_ARTICLE_MANIFEST.json`.

Historical files (`v1_0`, primary-table v0.1, obstruction taxonomy v1) remain provenance records and must not override the newer current files.

## Resume instructions for any future chat

If the user says “continue Paper IV” or “continue article 4”, do this without asking them to repeat context:

1. Read `recovery/CURRENT_PAPER_IV_ARTICLE.md`.
2. Read `recovery/PAPER_IV_ARTICLE_MANIFEST.json`.
3. Read the newest `paper_iv/PAPER_IV_D7_READINESS_STATE.json` and `protocol/PAPER_IV_MAJOR_FRAMEWORK_COVERAGE_CONTRACT.json` in case the scientific state has advanced.
4. Read canonical `paper_iv/PAPER_IV_CQG_DRAFT_v1_1.md` and its audit.
5. Read current claim/evidence, primary-literature, obstruction, LQG-attribution and Proposition prior-art files listed above.
6. If a new scientific iteration changes the census or a family status, freeze a transition audit first; never silently edit historical versions.
7. Create the next full manuscript as a new version (`PAPER_IV_CQG_DRAFT_v1_2.md`, etc.).
8. Update this recovery file and the machine manifest whenever a newer manuscript becomes canonical.

## Current preparation readiness

From the v1.1 audit:

- full v1.1 manuscript assembly = `100%`;
- scientific content toward submission quality ≈ `60%`;
- CQG structural fit ≈ `85%`;
- family-level primary anchor coverage = `15/15` first-pass;
- exact blocker-sentence citation/provenance completion ≈ `45%`;
- obstruction robustness layer ≈ `80%`;
- LQG equation attribution layer ≈ `80%`;
- figures rendered = `0%`;
- IOP/CQG LaTeX clean compile = `0%`;
- immutable article release/DOI = `0%`;
- overall submission readiness ≈ `40%`.

These manuscript-readiness percentages are independent of D7 scientific authorization.

## Next work — do not restart completed tasks

1. Build direct-primary-source support for every exact terminal-blocker sentence and material branch distinction across the 14 nonterminal rows.
2. Use that audit to assemble `PAPER_IV_CQG_DRAFT_v1_2.md` with a citation-complete Results matrix.
3. Render Figures 1–4, especially the obstruction topology and prospective 14→15 census-expansion figure.
4. Convert the stabilized manuscript to IOP/CQG LaTeX and compile it.
5. Freeze a validated immutable article release/DOI and perform the final line-by-line claim/evidence audit.

## Recovery rule

This file and `recovery/PAPER_IV_ARTICLE_MANIFEST.json` together are the authoritative cross-chat recovery entry point for Paper IV. Always preserve historical manuscripts and advance by versioned files only.
