# KMQGB Critical Review — SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_REGULARIZATION_V7 same-contract repair

Date: 2026-09-16
Lane: independent KMQGB Critical Review / Verification
Review-start main: `3b369be9fd3769768fb1dcd1a8dc0076f8ae6ff1`

## RESULT_REVIEWED

Exactly one clearly bounded terminal substantive execution was reviewed:

- gate: `SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_REGULARIZATION_V7`;
- same-contract implementation-repair commit: `3b369be9fd3769768fb1dcd1a8dc0076f8ae6ff1`;
- authoritative Actions run: `35061567730`;
- run state: `completed/success`;
- historical Research aggregate classification: `TRIANGLE_DELTA2_CONTACT_REGULARIZATION_SOURCE_BLOCKED`.

The historical Research classification is not rewritten by this review. During final synchronization a separate newer Eq. (4) common-group-lift implementation commit appeared concurrently; it is outside this bounded review and no substantive value from that newer lane is consumed here.

## VERDICT

`INVALID_IMPLEMENTATION`

The repair genuinely fixes the earlier unbound derivative-identity control, but two decision-relevant frozen-contract problems remain. First, the run is falsely classified source-blocked because source authority is locked to mutable current-front prose rather than durable V5 authority. Second, the repaired transverse control still does not exercise the correlated three-variable Wick moment used by the decision-producing target calculation; an explicit coefficient mutation changes all substantive K values while the frozen transverse and other implemented controls remain green.

This is neither scientific FAIL nor provenance failure. The terminal K values remain exact diagnostic outputs only and are not promoted here to validated V7 scientific authority.

## PREREG_CHECK

PASS for chronology and frozen scientific contract.

The original V7 preregistration predates the original implementation/result and remains the governing contract. The current repair changes implementation/control wiring only; it does not change the frozen local conormal map, derivative order, schemes A/B/C/A4, statistic K, PASS/FAIL/BLOCKED/INVALID semantics, or interpretation ceiling.

The preregistration explicitly freezes:

- local map `B12=x`, `B23=y`, `B13=x+y`;
- exact second-derivative Gaussian object `D2_eps^a`;
- correlated Wick identity with the term `8 Cxy Cxz Cyz` in `<X^2Y^2Z^2>`;
- V5 exact context `channel 00000 = 11/24`;
- transverse two-contact normalization through the same Gaussian-moment machinery;
- source BLOCKED only when the repaired V4 authority, V5/V6 realization chain, or exact target conormal/derivative identity cannot actually be source-locked at execution head.

## OBJECT_IDENTITY_CHECK

QUALIFIED / implementation binding failure.

The production object remains the frozen derivative-contact triangle, and the repair now routes production derivative coefficients and derivative controls through one executable `d2_factor()` representation. The old derivative-identity tautology is therefore genuinely repaired.

However, the decision-critical correlated target calculation uses a separate correlated branch of `gaussian_engine()` containing

`mxyz = Vx Vy Vz + 2 Cxy^2 Vz + 2 Cxz^2 Vy + 2 Cyz^2 Vx + 8 Cxy Cxz Cyz`,

while the transverse fixture invokes `gaussian_engine(..., c=None)`, an independent-covariance branch that never executes `mxyz` or any nonzero cross covariance. Thus use of the same function name does not by itself bind the frozen correlated Wick identity to the mandatory replay control.

## SOURCE / REALIZATION CHECK

FAIL at implementation source-lock layer; durable authority itself is present.

The current source lock implements V5 authority as a substring test against mutable navigation prose:

`v5_channel00000_lock = "00000=11/24" in front_txt or "00000 = 11/24" in front_txt`.

That is not a durable scientific source lock. On the repair head, current-front wording no longer contains the literal substring, so the authoritative source-lock artifact records `v5_channel00000_lock=false` and `source_ok=false`, even though the underlying V5 authority has not disappeared.

Independent durable checks establish the authority:

- the V7 preregistration itself records V5 exact `channel 00000 = 11/24` as the frozen nonzero angular-channel context;
- `recovery/CRITICAL_REVIEW_SOURCE_J1_K5_CHANNEL_COVERAGE_V5_2026-09-16.md` records the same exact datum;
- V5 aggregate artifact `10425727109`, ZIP SHA256 `a8775abfca4f5059eb55a72d1ed58a1ee4464fecc211e122ca15a51850666e7d`, contains exact `channel_00000 = 11/24`, complete-map SHA256 `2bceeb84db17ef0f01df4aa3350f8800d9e8e531ed68ed9ef10ca10cecfc9d0d`, and classification `CHANNEL_COVERAGE_NONZERO_SCOPED`;
- the earlier V7 source-lock execution had the same scientific ancestry and passed this lock only because then-current navigation prose happened to contain equivalent literal wording.

Therefore `TRIANGLE_DELTA2_CONTACT_REGULARIZATION_SOURCE_BLOCKED` in run `35061567730` is a false block caused by a brittle mutable-prose implementation lock, not a genuine absence of source authority.

## PROVENANCE_CHECK

PASS.

Actions run `35061567730` is terminal `completed/success` on head `3b369be9fd3769768fb1dcd1a8dc0076f8ae6ff1`. Source-lock, Python 3.11, Python 3.13 and aggregate jobs all completed successfully.

Artifacts and Actions ZIP digests, independently rechecked after download:

- source lock `10432368834`: `sha256:6ee4e14149805f2ff064901c408d749de5af3ba27a1ff66f66a6f3cfaf109e5c`;
- Python 3.11 `10432144784`: `sha256:6b4e3630d673b4b9091bb4ccffb53d860b18a2099e206f87c75ed34710c35757`;
- Python 3.13 `10432815881`: `sha256:4ed49cebe36e98bd41399a187283c0163c228ddb1cc5cf3fbcfaf61c6700775c`;
- aggregate `10432687425`: `sha256:0aafc4821ca3a04eb6e149e27de3efaa7e2e068550fa814edcb6ecda0b32632d`.

The aggregate JSON itself has internal SHA256 `d2af5edd1bfe9708c336ba165a83b7c27635c1b8fc9d148846f8deea23125fee`; the source-lock JSON has internal SHA256 `8f276f50381bace863b2a9b03781475a7c70a63cd29b2aa40b22269d394361a7`. There is no `INVALID_PROVENANCE` basis.

## SAME_REALIZATION_CHECK

QUALIFIED.

The repair is on the same preregistered local derivative-contact realization and preserves the same scientific object. The V5 source authority is also the same durable authority; only the navigation-text wording changed. The false source block therefore cannot be interpreted as a realization loss.

The remaining correlated-Wick control defect is an implementation verification gap inside this same realization.

## NUMERICAL / STATISTICAL CHECK

Exact, nonstatistical arithmetic. The terminal aggregate reports diagnostic values

- `K(A)=1600/531441`;
- `K(B)=204800/1162261467`;
- `K(C)=14400/19487171`;
- `K(A4)=K(A)` exactly;
- Python 3.11 and 3.13 agree on decision-critical fields.

An independent exact algebra replay reproduced these values for the implemented coefficient `8` in the frozen correlated Wick identity. This supports arithmetic coherence but does not repair the mandatory-control mismatch, so the values remain diagnostic rather than validated V7 PASS authority.

## COUNTEREXAMPLE_ATTEMPTS

### 1. Mutable-front source-lock counterexample — SUCCESS

Keep all durable V5 scientific authority fixed and change only the human navigation wording in `CURRENT_BENCHMARK_FRONT.md` so the literal `00000=11/24` / `00000 = 11/24` substring disappears. The implementation flips `v5_channel00000_lock` from true to false and emits `SOURCE_BLOCKED` although the V5 aggregate artifact, Critic record, preregistered datum and realization chain are unchanged.

Hence current source blocking is text-format dependent rather than authority dependent.

### 2. Correlated Wick mutation counterexample — SUCCESS

Change only the production coefficient in the frozen correlated sixth moment from

`8*Cxy*Cxz*Cyz`

to the incorrect

`7*Cxy*Cxz*Cyz`.

The repaired transverse control still equals `4` because its `c=None` branch has zero cross covariance and never executes the correlated `mxyz` formula. Derivative identity, one-factor mass and x^2 controls, wrong-sign rejection, permutation symmetry, common-rescaling equality and the structural object fixtures remain green.

Yet an independent exact replay gives changed decision values:

- correct-8 A: `1600/531441`; wrong-7 A: `1369/531441`;
- correct-8 B: `204800/1162261467`; wrong-7 B: `15273701/10125391872`;
- correct-8 C: `14400/19487171`; wrong-7 C: `15973321/21046144608`.

Thus the mandatory transverse replay does not bind a decision-critical component of the production Gaussian/Wick machinery. This is an explicit implementation counterexample, not a general suspicion.

## OVERCLAIM_CHECK

PASS after qualification.

No scientific FAIL, model/family failure, D7 closure, selector conclusion, Candidate Gravity activation or global quantum-gravity claim follows. The historical Research source-block label is preserved as history but is not accepted as a genuine scientific/source blocker. The exact K values are not promoted to a validated V7 PASS while the frozen implementation controls remain inadequately bound.

`BLOCKED != FAIL`; green CI is provenance, not science; finite/local evidence is not family closure.

## QUALIFICATIONS

1. The derivative-control repair is real and should be retained.
2. The production coefficient `8` is independently consistent with the frozen Wick identity; the mutation counterexample tests control sensitivity, not evidence that the actual coefficient is wrong.
3. Durable V5 source authority is present; only the implementation lock against mutable navigation prose is wrong.
4. No historical result is rewritten.

## UPDATED_STATE

- V7 original run `35054748495`: historical Research `TRIANGLE_DELTA2_CONTACT_REGULARIZATION_DEPENDENT_SCOPED`; independent Critic `INVALID_IMPLEMENTATION`.
- V7 same-contract repair run `35061567730`: historical Research `TRIANGLE_DELTA2_CONTACT_REGULARIZATION_SOURCE_BLOCKED`; independent Critic `INVALID_IMPLEMENTATION`.
- The second verdict is caused by false source locking plus incomplete correlated-moment control binding, not by scientific falsification or missing provenance.
- RQIR Core v1.0 remains FROZEN; D7 required subgates remain unclosed; terminal selectors remain forbidden; Candidate Gravity remains inactive.
- Iter504 run `34907349374` remains non-terminal `in_progress / conclusion=null`; Iter461 run `34748503239` remains non-terminal `queued / conclusion=null`. Their partial substantive values were not consumed.

## NEXT_ADMISSIBLE_GATE

Only another same-contract V7 implementation repair is admissible without a new scientific preregistration:

1. source-lock V5 context directly against durable frozen authority — e.g. exact V5 Critic/result identity plus artifact ID/digest and parsed `channel_00000 = 11/24` field — never against mutable current-front prose;
2. route production and mandatory controls through a genuinely shared generic multivariate Gaussian/Wick expectation implementation, or add an exact correlated-moment negative/mutation fixture that necessarily detects an `8 -> 7` corruption of the frozen `Cxy Cxz Cyz` term;
3. preserve the frozen object, conormal map, derivative order, A/B/C/A4 schemes, exact K statistic, source dependency semantics, PASS/FAIL/BLOCKED/INVALID meanings and interpretation ceiling;
4. rerun exact Python 3.11 and 3.13 lanes plus aggregate with fresh artifacts and digests.

Any change to the scientific object, source authority meaning, target map, schemes, decision statistic, or decision/interpretation semantics requires a new prospectively frozen gate.