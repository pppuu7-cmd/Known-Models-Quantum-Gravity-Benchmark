# Recovery Delta 075 — O3 Semigroup Exponential Uniqueness Control

**Date:** 2026-09-09  
**KMQGB iteration:** 075  
**Score change:** none.

Created `protocol/O3_SEMIGROUP_EXPONENTIAL_UNIQUENESS_CONTROL.md`.

## Exact A2 positive control

For analytic/entire `F` with

`F(0)=1`,

`F(z+w)=F(z)F(w)`,

one has no zeros and, differentiating at `w=0`,

`F'(z)=F'(0)F(z)`.

Therefore the unique solution is

`F(z)=exp(c z)`

with one free constant `c`.

This proves that a finite functional law can genuinely collapse an infinite function space to finite parent data. O3 is therefore not blocked in principle by the functional-freedom theorem.

## Comparator result

Exponential/entire weakly nonlocal gravity is already a registered comparator architecture. In a known stable/unitary EOM-related subclass, all tree-level graviton amplitudes coincide with Einstein gravity.

Thus the control passes A2 but fails A3 and does not supply a new hard residual.

## Next O3 target

Search for a finite law whose unique solution is

- non-rational;
- not an exponential/EOM dressing of GR;
- parent-fixed in retarded/CTP prescription;
- tied to a nontrivial hard four-graviton or cross-representation invariant;
- outside q-string/string/AS/discrete/nonlocal comparator containment.

Readiness remains R1=92, R2=89, R3=24, R4=45.
