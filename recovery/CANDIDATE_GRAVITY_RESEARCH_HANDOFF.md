# Candidate Gravity Design Research — Handoff / Recovery Method

This is the continuation entrypoint for KMQGB as a design/red-team layer for a future Candidate Gravity (KG) model. It never replaces external RQIR Candidate Gravity authority.

## Restoration order

1. Open `pppuu7-cmd/Known-Models-Quantum-Gravity-Benchmark`, branch `main`.
2. Read `recovery/RESTORE_FROM_NEW_CHAT.md`.
3. Read `recovery/CURRENT_BENCHMARK_FRONT.md`.
4. Read `recovery/state.json`.
5. Read completely:
   - `protocol/CANDIDATE_GRAVITY_DESIGN_PRIORS.md`;
   - `protocol/CANDIDATE_GRAVITY_DESIGN_PRIORS_POSTWAVE15.md`;
   - `protocol/CANDIDATE_GRAVITY_PROMOTION_GATE.md`;
   - `protocol/PARENT_KERNEL_RESPONSE_COMPLETENESS.md`;
   - `protocol/RESIDUAL_SPACE_GEOMETRY.md`;
   - `protocol/OPTIMAL_COMPARATOR_CONTRASTS.md`;
   - `protocol/CROSS_ORDER_RIGIDITY.md`;
   - `protocol/INTERVENTION_CONFIGURATION_RIGIDITY.md`;
   - `protocol/PREDICTIVE_HOLDOUT_RIGIDITY.md`;
   - `protocol/MINIMAL_DISCRIMINATING_TEST_SUITE.md`;
   - `protocol/MACHINE_READABLE_KG_PIPELINE.md`;
   - `protocol/BEYOND_C5_ESCAPE_TAXONOMY.md`;
   - `protocol/ESCAPE_DOOR_PRIORITY_MATRIX.md`;
   - `protocol/QUARTIC_FIRST_E1_SEARCH_SPECIFICATION.md`;
   - `protocol/QUARTIC_HELICITY_MOMENT_RIGIDITY.md`;
   - `protocol/KINEMATIC_PROJECTION_RANK_COMPLETENESS.md`.
6. Read the candidate schema/validator and highest-numbered immutable recovery delta.
7. Treat waves 1–21 as terminal historical authority.
8. Refresh external RQIR `candidate_gravity/recovery/CURRENT_QG_FRONT.md` read-only before readiness/heavy-compute statements.
9. Resume from `next_actions` in `recovery/state.json`, not chat memory.

## Historical coverage

- Wave 1: `9/9 = 100%`.
- Waves 2–21: each `5/5 = 100%`.
- Robust unique-QG residuals authorized by KMQGB: `0`.
- KMQGB-promoted KG ansatz: none.

Coverage is not Candidate Gravity readiness.

## Current preferred architecture

The conservative same-spin-2 search front is

`GR 2-point + GR 3-point + parent-fixed quartic/higher cross-regime structure`.

Requirements:

- no arbitrary form-factor/tensor-ratio fitting;
- few shared parent parameters;
- full contact/exchange/Ward response;
- declared dispersion/growth class;
- linked helicity/moment/cross-order predictions;
- exact source-to-native invariant-rank completion;
- same-domain full-C5/nonlocal/UV comparator quotient;
- intervention/holdout rigidity;
- nonzero COR and global separation.

No exact parent kernel currently passes G0. No ansatz is promoted.

## New stable rules after Wave 15

1. Every new seed must state its Beyond-C5 escape door `E1..E5`.
2. Local analytic metric-only deformations are C5 EFT directions in their low-energy domain.
3. A cubic graviton deformation carries the CEMZ high-energy causality tax unless the completion explains the escape; quartic-first is the preferred minimal no-extra-tower front.
4. For quartic-first 4D GREFT, `rho4=(C4,1-C4,2)/(C4,1+C4,2)` is an IR helicity anchor, not novelty.
5. Higher-order graviton moment/Hankel constraints are useful cross-order consistency tests, not KG attribution.
6. Declare `D-local` versus `D-nonlocal` dispersion/growth class before applying positivity/moment bounds.
7. A native observable with fewer invariants than its parent response requires enough prospectively frozen auxiliary same-parent constraints to define a unique lift.

## Kinematic projection-rank rule

If parent invariant dimension is `n` and target dimension is `m<n`, require `n-m` independent auxiliary constraints or an equivalent prospectively frozen trajectory `x=gamma(s)` such that

`rank([J_target;J_aux])=n`.

Without this, derivatives/cuts/root Jacobians/residuals in target coordinates are `BLOCKED`.

## External RQIR authority

Latest directly observed authority: **Iteration 611**, Candidate Gravity readiness **24%**.

Since Iter590, RQIR has closed K3/K1^3 origin accounting, assembled the complete 13-family source object, passed full source-level nonlinear Ward consistency at Iter605 and froze the source-to-native mapping contract at Iter606.

Current blocker: the full source tree depends on three independent invariants `(u,a_s,a_a)` while native `D_s` supplies one scalar `s`. Two additional same-parent invariant constraints (or one equivalent trajectory `(u(s),a_s(s),a_a(s))`) are required before pole support/Jacobians and native mapping are unique.

Until then: no Source/Born subtraction, comparator quotient, ANSATZ-003, Fisher or resources.

## Exact next KMQGB research front

Search for a **parent principle** that fixes the quartic-first tensor/kernel hierarchy with few parameters.

Reject as insufficient by themselves:

- ghost freedom;
- positive spectral density;
- choosing an entire function by taste;
- one quartic Wilson ratio;
- low-rank moment/Hankel structure.

Any candidate parent rule must also provide its Lorentzian/CTP prescription and dispersion/growth class, and must predict multiple helicity/order/cross-regime blocks without independent retuning.

## Minimal continuation instruction

`Продолжай KMQGB и наработки для будущей Candidate Gravity. Восстанови current front/state/handoff и прочитай оба design-prior файла, quartic-first, quartic moment-rigidity и kinematic projection-rank protocols. Считай волны 1–21 терминальными. Обнови внешний RQIR read-only. Следующий фронт — parent principle для low-parameter quartic/higher kernel; KG ansatz не продвигать до полного promotion gate.`
