# Candidate Gravity Design Research — Handoff / Recovery Method

This is the continuation entrypoint for using KMQGB as a design/red-team layer for a future Candidate Gravity (KG) model. It never replaces external RQIR Candidate Gravity authority.

## A. Required restoration order

1. Open `pppuu7-cmd/Known-Models-Quantum-Gravity-Benchmark`, branch `main`.
2. Read `recovery/RESTORE_FROM_NEW_CHAT.md`.
3. Read `recovery/CURRENT_BENCHMARK_FRONT.md`.
4. Read `recovery/state.json`.
5. Read completely:
   - `protocol/CANDIDATE_GRAVITY_DESIGN_PRIORS.md`;
   - `protocol/CANDIDATE_GRAVITY_PROMOTION_GATE.md`;
   - `protocol/RESIDUAL_SPACE_GEOMETRY.md`;
   - `protocol/OPTIMAL_COMPARATOR_CONTRASTS.md`;
   - `protocol/PARENT_KERNEL_RESPONSE_COMPLETENESS.md`.
6. Read the highest-numbered immutable `recovery/RECOVERY_DELTA_NNN.md`.
7. Read wave summaries needed by the current front; waves 1–10 are historical terminal authority.
8. Refresh external RQIR `candidate_gravity/recovery/CURRENT_QG_FRONT.md` read-only before readiness or heavy-compute decisions.
9. Resume from `next_actions` in `recovery/state.json`, not chat memory.

## B. Historical benchmark coverage

- Wave 1: `9/9 = 100%`, immutable.
- Waves 2–10: each `5/5 = 100%`, immutable.
- Globally authorized robust unique-QG residuals: `0`.
- KMQGB-promoted Candidate Gravity ansatz: none.

Coverage is not Candidate Gravity readiness.

## C. Core parent decomposition

Never define novelty relative only to a Gaussian or tree truncation.

Use

`Gamma_parent = Gamma_C5,full_matched + DeltaGamma_KG`.

`DeltaGamma_KG` means only what survives the largest applicable same-domain/same-order C5 nonlinear/loop/EFT/state/contact parent plus all other allowed comparators.

Wave 7 established that higher-point, squeezed-state, memory, parity/chirality and nonanalyticity labels are all insufficient by themselves.

## D. Candidate Gravity promotion order

The authoritative checklist is `protocol/CANDIDATE_GRAVITY_PROMOTION_GATE.md`.

Short form:

`one parent dynamics`

`-> complete same-parent response`

`-> exact Ward/Bianchi/contact/constraint physical basis`

`-> common-domain comparator definition`

`-> attribution stack`

`-> full C5/C0-C6/C3b quotient`

`-> nonzero local COR`

`-> global/nonlinear separation`

`-> projected identifiability / optimal observable design`

`-> rigidity`

`-> only then ansatz promotion / Fisher / resources`.

## E. Attribution stack

A KG-specific residual must survive, where relevant:

1. quantum attribution;
2. mediator attribution;
3. gravity / massless-spin-2 attribution;
4. locality / microcausality attribution;
5. relational / QRF attribution;
6. geometry attribution for causal/process claims;
7. detector/backaction attribution;
8. regulator/continuum attribution;
9. state-sector attribution;
10. full-C5 order matching;
11. rigidity/identifiability.

## F. Residual-space geometry

After exact physical constraints, for residual `r`, covariance `Sigma`, union comparator Jacobian `J_union`:

`A=Sigma^(-1/2)J_union`,

`Pi_perp=I-AA^+`,

`COR=Pi_perp Sigma^(-1/2)r`.

A nonzero COR is only a local prefilter; finite/global comparator profiling is mandatory.

For future KG Jacobian:

`B_KG=Pi_perp Sigma^(-1/2)J_KG`.

Only robust nonzero singular directions of `B_KG` are locally identifiable after comparator profiling.

## G. Observable design rules from wave 9

Authoritative protocol: `protocol/OPTIMAL_COMPARATOR_CONTRASTS.md`.

- exact comparator-null contrasts satisfy `J_union^T w=0`;
- covariance-optimal contrast for candidate signal `s`:
  `w_opt proportional to Sigma^(-1/2)Pi_perp Sigma^(-1/2)s`;
- maximum local post-comparator SNR:
  `||Pi_perp Sigma^(-1/2)s||`;
- adding `k` physical observables gives
  `Delta d_perp = k - Delta rank(J_union)`.

Thus future KG observables are chosen by post-comparator dimension/conditioning/SNR, not raw sensitivity or qualitative novelty.

## H. Same-parent response completeness from wave 10

Authoritative protocol: `protocol/PARENT_KERNEL_RESPONSE_COMPLETENESS.md`.

For `G=K^-1` and derivative label set `S`:

`D_S G = sum_k (-1)^k sum_(B1,...,Bk in OP(S,k)) G K_B1 G ... K_Bk G`.

Every ordered set partition appears once.

Term counts: `1,3,13,75,541` for orders `1..5`.

Required order:

`complete response -> origin/cut classification -> Ward/contact physical reduction -> matched observable -> COR/comparator quotient`.

Never hand-select response families or call a subset complete without an exact null/origin proof.

## I. Current external RQIR authority

Last directly observed scientific front: **Iteration 590**.

- Candidate Gravity `MODEL_READINESS = 24%`;
- Iter424 `5/5 PASS`;
- Iter581 exact15 PASS;
- Iter582 q2-resolved operator coordinate PASS but non-residual;
- Iter583–589 close K2/K1 routing, normalization and fixture bridges;
- Iter590 proves the cubic source response needs `K3 + six K1/K2 + six K1^3` families and that omitted `K3` and `K1^3` have nonzero same-parent support.

Exact next RQIR gate:

1. prove/classify hard-channel discontinuity origin of local `K3`;
2. classify `K1^3` under the frozen linked-cut/origin protocol;
3. only then close complete nonlinear source Ward block and apply comparator quotient.

KMQGB must not treat Iter582 buckets as residual data before this closes.

## J. RQIR COR pre-registration

If the final matched residual remains only a three-bucket physical vector,

`d_perp = 3 - rank(A_union)`.

If union comparator rank is 3, no amount of precision creates a local residual direction.

Add a new linked observable/order only when

`Delta d_perp = Delta m_phys - Delta rank(J_union) > 0`

or projected conditioning/SNR materially improves.

## K. Next research front — cross-order rigidity

The next wave should stack multiple response orders from the same parent dynamics.

Goals:

1. define stacked multi-order observable/covariance/Jacobian notation;
2. preserve shared parent parameters across orders — no artificial independent retuning;
3. derive cross-order comparator-null contrasts;
4. include cross-order covariance/common theory/systematic modes;
5. define a projected-rank/singular-value cross-order rigidity certificate.

The scientific rationale is that physical data dimension can grow across response orders while shared comparator parameter rank may grow more slowly.

## L. Reference code

- `code/residual_space_geometry_reference.py`;
- `code/optimal_comparator_contrasts_reference.py`;
- `code/inverse_kernel_ordered_partitions.py`.

## M. Minimal continuation instruction

`Продолжай KMQGB и наработки для будущей Candidate Gravity. Восстанови current front/state, прочитай Candidate Gravity promotion gate, residual-space geometry, optimal contrasts и parent-kernel response completeness. Считай волны 1–10 терминальными. Внешний RQIR только read-only. Следующий фронт — cross-order rigidity; не продвигай KG ansatz до полного promotion gate и robust comparator-subtracted residual.`
