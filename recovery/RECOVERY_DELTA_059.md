# RECOVERY_DELTA_059 — P4 Functional-Freedom No-Go

**Date:** 2026-09-08  
**KMQGB iteration:** 059  
**External RQIR observed:** Iteration 616, `MODEL_READINESS=24%`.

## Stable readiness

- R1 repository readiness: **92%**.
- R2 KMQGB methodology/material readiness: **88%**.
- R3 Candidate Gravity scientific readiness: **24%**.
- R4 `MINIMAL_NOVEL_PARENT_PRINCIPLE_SEARCH`: **45%**.

No percentage changes in Iter059. The new result is a permanent necessary-condition filter and executable prefilter; it does not itself supply the explicit P4 parent survivor required for R4 credit.

## New permanent result — Functional-Freedom No-Go

Authority: `protocol/P4_FUNCTIONAL_FREEDOM_NO_GO.md`.

For a proposed parent principle `P`, define the structural-null hard-deformation space

`N_P = {delta A4 != 0 | every stated P-constraint remains satisfied by A4 + delta A4}`.

If `N_P` contains an infinite linearly independent tower of admissible hard deformations, then `P` cannot by itself satisfy P4.

The reason is direct: P4 requires finite/low-dimensional parent data. Infinite-dimensional `N_P` means that an arbitrary hard function, spectral measure or independent per-order coefficient remains to be chosen rather than derived.

A finite nonzero `dim N_P` is allowed only if all remaining directions are explicitly declared as low-dimensional parent data and remain shared across the required order/representation/background blocks.

## Concrete crossing-symmetric witness

For massless four-point kinematics with `s+t+u=0`, fully symmetric scalar polynomials are generated by

`sigma2 = s^2+t^2+u^2`,

`sigma3 = s*t*u`.

Thus the monomials

`sigma2^a sigma3^b`

have degree `2a+3b` and provide an unbounded tower as the hard/EFT cutoff is increased.

If a structural principle fixes only finitely many low-degree coefficients, higher-degree symmetric contact directions can remain while preserving crossing and all finite-order low-energy constraints. In a real graviton analysis these scalar coefficient directions must be combined with the exact allowed physical tensor/helicity contact basis; the scalar ring is a constructive lower-level witness, not a substitute for that full basis.

Representative four-graviton EFT literature confirms that higher-derivative gravitational amplitudes carry independent Wilson/contact data constrained by crossing/unitarity/dispersion without those generic structural conditions automatically selecting one unique complete tower.

## Executable reference

New file:

`code/p4_functional_freedom_reference.py`.

It enumerates the crossing-symmetric scalar monomial basis and provides the finite-cutoff diagnostic

`FF_D(P) = dim N_P(D)`

for the minimal case where all coefficients through a declared finite degree are frozen but higher crossing-symmetric directions remain free.

Self-test authority for `fixed_through_degree=3`:

- cutoffs `D = [4,6,8,10,12,18,24,30]`;
- freedom counts `FF_D = [1,4,7,11,16,34,58,88]`;
- classification `FUNCTIONAL_FREEDOM_GROWTH_WITNESS`.

A second delayed-growth check freezes through degree 11 and still gives increasing freedom `[3,21,45,75]` at `D=[12,18,24,30]`.

The reference test has been added to `.github/workflows/methodology-ci.yml`.

## Scientific interpretation

The key distinction is now frozen:

`infinitely many predicted terms` is acceptable,

but

`infinitely many independent choices` is not a low-freedom P4 parent.

This sharply separates microscopic theories such as string/matrix constructions, where one parent can fix an infinite EFT tower, from structural ansätze that merely permit an arbitrary function/tower.

## Immediate P4 workflow update

Before substantial derivation or heavy compute for any future parent proposal:

1. define its physical hard basis at increasing cutoff/order;
2. impose the complete claimed structural constraints;
3. estimate or compute `FF_D(P)`;
4. if surviving freedom keeps growing with `D`, classify `FUNCTIONAL_FREEDOM_BLOCKED` and stop;
5. only if the freedom stabilizes to a small declared finite dimension proceed to the explicit P4 hard-relation derivation;
6. then perform P5 comparator survival and P6 pre-ansatz record construction.

This turns the Wave38 lesson `structural consistency -> many dynamics` into a reproducible kill test.

## External RQIR

No newer external authority than Iter616 was observed during this iteration. RQIR remains at 24%, with the one-dimensional `N_native` normalization/phase blocker and no need for heavy compute before the independent lower-order/common-field anchor is fixed.

## Exact continuation front

Search for a finite microscopic dynamical selector and apply the new `FF_D` filter **before** detailed candidate development. A useful next candidate must first demonstrate that its hard freedom stabilizes to finite low dimension, then derive a uniquely normalized 4-point/higher or cross-representation relation and same-parent CTP/retarded block.
