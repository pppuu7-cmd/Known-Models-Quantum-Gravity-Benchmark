# Current Benchmark Front
Updated: 2026-09-17

Fresh repository `main` and fresh Actions state always outrank this navigation document if they diverge.

## Latest independently closed authority result

Gate:

`ITER504T_REPAIR_RERUN_TERMINAL_AUTHORITY_REVIEW_DECISION_CANONICALIZATION_REPAIR_GATE`

Terminal classification:

`ITER504T_REPAIR_RERUN_EXACT_RUN_AUTHORITY_VALIDATED_SCOPED`

Independent Critical Review:

- file `recovery/CRITICAL_REVIEW_ITER504T_REPAIR_RERUN_TERMINAL_AUTHORITY_REVIEW_DECISION_CANONICALIZATION_REPAIR_2026-09-17.md`;
- commit `8624532254981fbea6f8fcfac09cd538e2066aa4`;
- verdict `CONFIRMED_SCOPED`.

The independent review freshly downloaded all four canonicalization-run artifacts, rehashed their ZIP bytes, reconstructed the path-free canonical manifest/decision independently from the downloaded lane JSON, rechecked exact upstream artifact IDs/digests and confirmed real missing-artifact `BLOCKED` semantics.

Confirmed canonical identities:

- canonical manifest SHA256 `1188f3cce3280035373074b68358a65cedf420616bfca76fe8d67ffe6e723766`;
- lane canonical decision SHA256 `8969d8746ca56f6999b02b3ff04055d91d72a19f404d8e125bae3d049ec00a9e`;
- aggregate decision SHA256 `233444879a2f052a8e6706800750e2a4940414eb61ee00eebe5cbf4f3be4f1ee`.

The inherited exact completed-run science remains authority-valid only at its original bounded development scope:

`ITER504T_LOCAL_D_CERTIFIES_THREE_ROOT_CONTINUOUS_DRIFT_SCOPED`

Development cohort:

`causal=0to5`, `block=0`, `path=2`, boxes `13,14,15`.

Retained exact facts:

- 12 terminal leaves per environment;
- zero recomputed unresolved leaves per environment;
- C1 = 18 records / 70,056 components / zero false per environment;
- exact inherited semantics controls pass;
- leaf/per-rho certification is coherent in the exact completed-run payload.

This is not all-Iter504 closure.

## Active scientific frontier — Iter504U held-out generalization firewall

Gate:

`ITER504U_HELDOUT_LOCAL_D_GENERALIZATION_FIREWALL`

Purpose: test whether the validated local-D repair generalizes outside development roots 13–15 without refit before any broader/full-domain campaign is authorized.

Prospective chain:

- outcome-blind successor toolkit `81e0e94649a50cccc510dc2630456af45fd98e46`;
- held-out preregistration `05b8e9354c9a7805f0fce18b904346a986a0787f`;
- pre-execution source audit `264cad630c380276dfa23c3d933f4519bdc91cc8`, verdict `ITER504U_PREEXECUTION_METHODOLOGY_VALID_SCOPED`;
- methodology CI run `35246378450`, terminal `completed/success`;
- single execution authority `7b446398c97dc849f6f4ecbcd493058d9889d0cf`;
- prepared workflow commit `65246c144675d656ab434b306816fd99459f753d`;
- single launch marker `102c7f9cafec956f3bc7bed4384ae755c98f761a`;
- scientific Actions run `35246605860`, run number `1`.

The scientific run source-lock has completed successfully. The 12 held-out producer jobs (6 frozen cases x Python 3.11/3.13) are authorized to run in parallel. No duplicate execution is authorized.

## Prospectively frozen held-out cohort

No held-out science value was inspected before selection. The six exact cases are:

1. `H0_AMP_LOW = (0to5, block 0, path 2, box 0)`;
2. `H1_AMP_MID = (0to5, block 0, path 2, box 8)`;
3. `H2_CAUSAL_1 = (1to4, block 0, path 2, box 8)`;
4. `H3_CAUSAL_2 = (2to3, block 0, path 2, box 8)`;
5. `H4_DIRECTION = (0to5, block 3, path 2, box 8)`;
6. `H5_SIGN = (0to5, block 0, path 3, box 8)`.

Development boxes `13,14,15` are forbidden as held-out test cases.

This cohort tests amplitude transfer, both alternate causal families, a different block/direction and an explicit sign flip.

## Frozen Iter504U scientific contract

No refit is allowed:

- same base source realization as exact repair head `10ae6bcc8447d14cecc6e550065504b23f792953`;
- Arb/Acb precision `384` bits;
- all `243` channels, no pruning;
- R `[6,8,10,12]`;
- rho `[0.35,0.9,1.6,2.7]`;
- exact drift threshold `1/20`;
- exact robust slope floor `1`;
- deterministic rational dyadic midpoint subdivision;
- `MAX_DEPTH=3`;
- validated local `D(J)` recomputed on every visited interval;
- producer exact-Arb decision booleans, display floats non-authoritative;
- explicit C4 binding `leaf.certified = all(rho.certified)`;
- exact dyadic identity/full cover;
- independent Python 3.11 and 3.13 lanes, independent assembly, cross-environment aggregate and adversarial Critic.

The historical reusable C4 validator is not used as Iter504U authority.

## Frozen Iter504U terminal labels

PASS:

`ITER504U_HELDOUT_LOCAL_D_GENERALIZES_SCOPED`

only if all six valid held-out cases have zero unresolved leaves and both environments agree on the exact scientific projection.

INCONCLUSIVE:

`ITER504U_HELDOUT_LOCAL_D_INCONCLUSIVE_SCOPED`

if the gate is valid and cross-environment exact decisions agree but one or more leaves remain unresolved at frozen depth 3.

INVALID:

`ITER504U_INVALID`

for provenance/cohort/source/threshold/depth/partition/exact-decision/C4/cross-environment implementation defects.

There is no scientific FAIL label in this bounded certificate gate. Failure to certify at depth 3 is INCONCLUSIVE, not model falsification.

## Historical qualifications retained

- canonicalization run `35245037403` is exact-run scoped PASS and independently `CONFIRMED_SCOPED`;
- prior artifact-boundary run `35238357310` remains historical `INVALID_IMPLEMENTATION`, independently confirmed;
- semantics-repair run `35232311780` remains historical `INVALID_IMPLEMENTATION`;
- authority-review run `35225818354` remains historical `INVALID_IMPLEMENTATION`;
- historical reusable-validator C4 defect remains independently confirmed for that old reusable path;
- original Iter504T run `35181094204` remains historical `INVALID_IMPLEMENTATION`, not scientific FAIL;
- C1 VERIFIED, C2 REFUTED, C3 VERIFIED remain immutable history;
- no Iter504T/Iter504U result is promoted directly to all 1888 states.

## Parallel D7-S2 retained state

- raw K5 group-variable tangent pushforward = `CLOSED_SCOPED`;
- group-only simultaneous contact-covector restriction/conditioning = `BLOCKED_SCOPED`;
- physical transverse quotient/projection = `BLOCKED_SCOPED`;
- measure/Haar/contact normalization = open;
- observable/distributional final pushforward = open.

Do not repeat blocked K5 source audits without genuinely new authorized primary material.

## Governance lock

- `RQIR Core v1.0 = FROZEN`.
- `D7-S2 = NOT_CLOSED`; `D7-S3 = NOT_CLOSED`; `D7-S4 = PARTIAL_GLOBAL_NOT_CLOSED`.
- terminal D7 selectors and `EXISTING_SUFFICIENT / ADAPT_EXISTING / HYBRID_REQUIRED / NEW_REQUIRED` remain forbidden until required subgates close.
- Candidate Gravity remains inactive; Paper IV remains `NOT_YET_AUTHORIZED`.
- `INCONCLUSIVE != FAIL`; `BLOCKED != FAIL`; `INVALID_IMPLEMENTATION != SCIENTIFIC_FAIL`.
- missing object/rank/certificate != zero; scoped child result != family closure; green CI != science.
- no authority exists for `QUANTUM_GRAVITY_SOLVED`, `ALL_KNOWN_MODELS_FAIL`, `NEW_THEORY_REQUIRED`, or `NEW_PHYSICS_FOUND`.
