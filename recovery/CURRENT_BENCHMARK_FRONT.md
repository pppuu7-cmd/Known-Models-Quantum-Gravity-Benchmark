# Current Benchmark Front
Updated: 2026-09-16

Fresh repository `main` and fresh Actions state always outrank this navigation document if they diverge.

## Latest terminal Research execution

`SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_REGULARIZATION_V7`

Frozen chain:

- preregistration `764cd9f915cef15b6294f5f72329f12cb145c08e`;
- implementation `44da1edd39debce755f9ae7d48c4e7abd375604c`;
- workflow head `e5021974d02f7cd52c540e80f5b3a14fcf0e5cdf`;
- authoritative run `35054748495`, terminal `completed/success`.

Jobs:

- source lock `104662442178`, success;
- exact Python 3.13 `104667121284`, success;
- exact Python 3.11 `104667121315`, success;
- aggregate `104671916886`, success.

Artifacts:

- source lock `10430711890`, digest `sha256:fb97f0f48802026f0cc8fcc38bd8749e307f9f091cb3e74af1bca11cc660a1d3`;
- Python 3.11 `10431855371`, digest `sha256:952d9f95aaddf363d82444c5c8cfc0cc2d52e47376a988cee734a3ad689d7348`;
- Python 3.13 `10431023217`, digest `sha256:ae345963ee9733711f95cb619864c9ecb031cdb42ec0edd0e46187df7e67edc0`;
- aggregate `10431741758`, digest `sha256:4933e65ca18f316be51508d0481449f2debc546126d9cac6ee791ddda4217f2c`.

Historical Research aggregate output:

`TRIANGLE_DELTA2_CONTACT_REGULARIZATION_DEPENDENT_SCOPED`

Diagnostic exact values from the historical aggregate:

- `K(A)=1600/531441`;
- `K(B)=204800/1162261467`;
- `K(C)=14400/19487171`;
- `K(A4)=K(A)` exactly;
- exact Python lanes agree on decision-critical fields.

## Latest independent Critical Review

`recovery/CRITICAL_REVIEW_SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_REGULARIZATION_V7_2026-09-16.md`

Critic commit:

`55757762a0a371e8885db2aa5b437e6435308cc2`

Verdict:

`INVALID_IMPLEMENTATION`

The V7 production object is statically coherent with the frozen local `delta''` triangle realization and provenance is complete. The Critic verdict is implementation invalidity, not scientific FAIL.

Two mandatory frozen positive controls were not genuinely implemented:

1. `derivative_identity` is a tautological self-comparison and is disconnected from the derivative representation used by the decision-producing `gaussian_data()` path;
2. the preregistered **independent transverse two-contact replay through the same Gaussian-moment machinery** is implemented only as `x21*x24`, a product of two one-factor controls, so it does not exercise the multivariate covariance/Wick machinery used by production.

Explicit outcome-independent counterexamples show that production derivative or multivariate-moment errors could be introduced while both controls remain green. Therefore green CI and lane agreement cannot validate the historical Research PASS label under the frozen contract.

Historical Actions values remain immutable diagnostic output but are not validated downstream V7 scientific authority until a same-contract implementation repair passes the mandatory controls genuinely.

## Prior terminal Research / Critic state

V6 run `35050058294` remains terminal with Research classification `TRIANGLE_CONTACT_REGULARIZATION_DEPENDENT_SCOPED` and independent Critic verdict `QUALIFIED`. It establishes only the prospectively frozen local plain-delta Gaussian triangle result and is not a substitute for the V7 derivative-contact object.

Repaired V4 run `35033194283` remains `CONFIRMED_SCOPED` for the highest `delta''` contact leading homogeneous tensor in the frozen `j=1`, fixed-tangent, channel-`00000` realization. V5 remains `QUALIFIED` for its exact finite channel census.

## Independent registered workflows

Do not consume partial substantive values:

- Iter504 run `34907349374` is freshly `in_progress / conclusion=null`;
- Iter461 run `34748503239` remains `queued / conclusion=null`.

Neither is authority for the reviewed V7 gate.

## Next admissible work

Perform only a **same-contract implementation repair** of V7:

1. define one executable derivative-factor representation and use that identical representation in production, derivative identity positive control and wrong-sign adversarial replay;
2. independently compute the frozen transverse two-contact integral with the same Gaussian covariance/moment engine used by the target calculation, rather than multiplying one-factor outputs;
3. preserve source locks, local conormal map, width schemes A/B/C/A4, exact statistic `K`, PASS/FAIL/BLOCKED/INVALID semantics and interpretation ceiling;
4. rerun two exact Python lanes and aggregate with fresh Actions artifacts/digests.

A control-wiring-only repair does not change the scientific contract. Any change to object, map, schemes, statistic, decision semantics or interpretation ceiling requires a new prospectively frozen gate and must not rewrite run `35054748495`.

## Governance lock

- `RQIR Core v1.0 = FROZEN`.
- `BLOCKED != FAIL`.
- finite certificate != universal theorem.
- missing object != zero residual.
- scoped child result != family closure.
- numerical-method/implementation blocker != scientific falsification.
- `D7-S2 = NOT_CLOSED`.
- `D7-S3 = NOT_CLOSED`.
- `D7-S4 = PARTIAL_GLOBAL_NOT_CLOSED`.
- terminal D7 labels and `EXISTING_SUFFICIENT / ADAPT_EXISTING / HYBRID_REQUIRED / NEW_REQUIRED` remain forbidden until required subgates close.
- Candidate Gravity remains inactive.
- Green CI is provenance, not science.
- No authority exists for `QUANTUM_GRAVITY_SOLVED`, `ALL_KNOWN_MODELS_FAIL`, `NEW_THEORY_REQUIRED`, or `NEW_PHYSICS_FOUND`.
