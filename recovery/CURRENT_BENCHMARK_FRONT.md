# KMQGB Current Benchmark Front

**Updated:** 2026-09-08  
**KMQGB iteration:** 038  
**Repository:** `pppuu7-cmd/Known-Models-Quantum-Gravity-Benchmark`  
**Branch:** `main`  
**Phase:** WAVES 1–15 TERMINALLY CLASSIFIED / FAIL-CLOSED MACHINE-READABLE KG PIPELINE FROZEN / MINIMAL DELTAGAMMA SCREENING NEXT

## Coverage

- Wave 1: **9/9 = 100%** — immutable.
- Waves 2–15: **each 5/5 = 100%** — immutable.
- Globally authorized robust unique-QG residuals: **0**.
- KMQGB-promoted Candidate Gravity ansatz: **none**.
- External Candidate Gravity readiness: **24%** at the latest directly observed RQIR authority.

Wave coverage is finite benchmark/methodology queue completion, not theory probability and not Candidate Gravity readiness.

## Permanent Candidate Gravity protocols

- `protocol/CANDIDATE_GRAVITY_DESIGN_PRIORS.md`;
- `protocol/CANDIDATE_GRAVITY_PROMOTION_GATE.md`;
- `protocol/PARENT_KERNEL_RESPONSE_COMPLETENESS.md`;
- `protocol/RESIDUAL_SPACE_GEOMETRY.md`;
- `protocol/OPTIMAL_COMPARATOR_CONTRASTS.md`;
- `protocol/CROSS_ORDER_RIGIDITY.md`;
- `protocol/INTERVENTION_CONFIGURATION_RIGIDITY.md`;
- `protocol/PREDICTIVE_HOLDOUT_RIGIDITY.md`;
- `protocol/MINIMAL_DISCRIMINATING_TEST_SUITE.md`;
- `protocol/MACHINE_READABLE_KG_PIPELINE.md`.

Reference code is maintained under `code/` for each algebraic/executable layer.

## Wave 15 — machine-readable fail-closed pipeline

Terminal `5/5 = 100%`, all methodology `PASS_RQIR_GATE`.

Canonical objects:

- `schema/KG_CANDIDATE_RECORD_v1.json`;
- `schema/KG_CANDIDATE_RECORD_PREANSATZ_EXAMPLE.json`;
- `code/kg_candidate_record_validator.py`;
- `protocol/MACHINE_READABLE_KG_PIPELINE.md`;
- `fifteenth_wave/README.md` and `fifteenth_wave/result.json`.

### Critical executable rule

`scientifically BLOCKED != structurally invalid`.

A record may be valid while a required physical object is `BLOCKED`.  It becomes invalid only when its internal state contradicts the missing/failed object, for example

- `G1=PASS` with an incomplete response block;
- `G0=PASS` without parent authority;
- `G3/G5=PASS` without a comparator registry;
- `G6=PASS` without a covariance object;
- ansatz promotion with any mandatory non-PASS gate;
- Fisher/resources promotion without a robust global comparator-subtracted residual.

Missing required physics is never converted to zero.

## Current Candidate Gravity architecture target

A serious future KG candidate should contain

1. one explicit parent dynamics with exact provenance;
2. few shared dynamics parameters/functions;
3. complete ordered-partition response for every used order;
4. exact origin/cut and Ward/Bianchi/contact/constraint reduction;
5. several linked response orders and controlled configurations;
6. full same-domain C0–C6/C3b + stronger comparator/full-C5 matching;
7. comparator-orthogonal residual and finite/global separation;
8. optimal comparator-null observable design;
9. cross-order and intervention rigidity;
10. prospective holdout prediction without shared-parameter retuning;
11. a compact redundant minimal discriminating test suite;
12. a machine-readable record that passes the full promotion state machine.

This is the operational implementation of **rigidity by overconstraint**.

## External RQIR authority

Latest directly observed scientific authority before this recovery update: **Iteration 590**, `MODEL_READINESS=24%`.

Iter590 proves the cubic MSSC source response requires local `K3`, six ordered `K1/K2`, and six ordered `K1^3` families.  External next gate remains

1. hard-channel origin of local `K3`;
2. linked-cut/origin classification of `K1^3`;
3. only then complete nonlinear source Ward closure and comparator subtraction.

KMQGB has a read-only algebraic K3 analyticity pre-check, but it is not RQIR authority.

## Promotion gate

No KG ansatz is promoted before `protocol/CANDIDATE_GRAVITY_PROMOTION_GATE.md` is satisfied. Fisher/resources remain forbidden until a real robust comparator-subtracted residual exists.

## Exact next KMQGB front — prospective minimal DeltaGamma screening

Do **not** promote a theory.  Freeze a small set of prospective minimal parent deformations and run each through the fail-closed record before any numerical optimization.

For every exploratory template:

1. define the parent/CTP deformation and its validity domain;
2. identify the lowest complete response order/configuration where it differs from full matched C5;
3. test field-redefinition/EFT/state/mediator absorption first;
4. require spin-2/Ward/locality/relational attribution;
5. compute only structural/tangent predictions that are justified without missing objects;
6. record all missing objects as `BLOCKED`;
7. reject a template immediately if it is comparator-contained or requires independent per-block retuning.

The first screening wave should compare several **minimal structural ansatz classes**, not claim a final Candidate Gravity model.
