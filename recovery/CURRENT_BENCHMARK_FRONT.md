# Current Benchmark Front
Updated: 2026-09-16

Fresh repository `main` and fresh Actions state always outrank this navigation document if they diverge.

## Latest terminal Research execution

`SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_FINITE_RENORMALIZATION_AUTHORITY_GATE`

Terminal Research classification:

`SOURCE_FINITE_RENORMALIZATION_AUTHORITY_BLOCKED_SCOPED`

This is **BLOCKED, not FAIL**.

Authority / execution chain:

- V9 prospective preregistration + immutable structured-source authority ledger commit `065d0a84d7f3cb96eac0fcd767f462f8c19be64d`;
- implementation/workflow head `aabb451f6e4efa50f23bb51d4bb3242d537699d7`;
- terminal result commit `7d3d4de295f3d5b3b5968621ffcb8fd35bfd2bd6`;
- authoritative Actions run `35083607676`, terminal `completed/success`.

Jobs:

- source lock `104753141925`;
- exact Python 3.11 `104753190321`;
- exact Python 3.13 `104753190342`;
- aggregate `104753240889`.

Artifacts:

- source lock `10440839290`, digest `sha256:82f0d2d9cc508d477673eb51e1b0a8619186efceecca478a69daaadc3b07f101`;
- Python 3.11 `10441182892`, digest `sha256:49ead4d2853c0403ec4be9c0a0dabc182dd1775055b99b1a5d3f892bcd91efb9`;
- Python 3.13 `10441402193`, digest `sha256:4abd55108c605d44a155e4275d0101df8583bb584cac8f5501298b7cfbad8047`;
- aggregate `10441477025`, digest `sha256:56fdc47ad9bf7b6d48b480b51ebd77ffa587beab1069d363069f6b69b104403b`.

Artifact-internal hashes:

- canonical aggregate JSON SHA256 `cb016e2902d868e37c78c67ab1c9fbbe0d65409ad8051117d1d49700478e63a1`;
- raw aggregate log SHA256 `219a6e6dda7a6d2d8c71dace9207acf4746c0859af0f88e5a7d907f8cbc04614`;
- aggregate decision-projection SHA256 `70369d7e0c78e4378f3c257519e2f7a32ed69f42414d3379cb35ac13967b759d`.

Green CI is provenance only; the terminal classification comes from the prospectively frozen exact authority classifier.

## Exact V9 result

V9 consumed exactly the terminal V8 local highest-contact `delta''` triangle and reconstructed its finite-renormalization state before auditing source selection authority:

- V8 classification `SOURCE_UNFIXED_FINITE_LOCAL_FREEDOM_SCOPED` reproduced;
- invariant basis count `8`;
- V8 rank `2`, augmented rank `2`;
- V8 affine nullity `6`;
- all six exact nullspace vectors reproduced.

The complete durable structured `sources/*.json` corpus frozen before result consists of exactly three Git-locked records:

- `arXiv:2202.04360` SL(2,C) Haar/Cartan snapshot, scoped to Iter486 radial escape;
- `arXiv:2601.23162v1` causal-vertex equations/definitions snapshot, scoped to source-object qualification;
- `arXiv:2604.24945v1` Toller/Cartan snapshot, scoped to Iter484 one-edge reconstruction.

Under the prospectively frozen categories `FINITE_COEFFICIENTS`, `FINITE_NORMALIZATION`, `GLUING_COMPOSITION`, `RG_SCALE`, `AUTHORIZED_QUOTIENT`:

- actionable same-realization selector records = `0`;
- selector rank = `0`;
- selector augmented rank = `0`;
- common finite normalization fixed = `false`;
- remaining V8 affine nullity = `6`.

The lexical audit reports seven mention-only hits. They do not reduce nullity: the Haar `normalization_note` is explicitly a radial-slope normalization note, the causal snapshot's `decomposition` is the Cartan magnetic decomposition, and several `rg` matches are only lexical substrings in URLs/other words. Both exact Python lanes agree on the same decision payload and classification.

Outcome-sensitive UNIQUE / QUOTIENT / BLOCKED / INCONSISTENT synthetic controls all pass, and a wrong-realization selector is rejected. Therefore the BLOCKED result is not a hard-wired classifier outcome.

## Scientific meaning

New durable fact: the current **durable structured source corpus** does not encode an actionable same-realization condition that uniquely fixes or physically quotients the six-dimensional V8 finite-renormalization freedom.

This does not prove that the full cited papers contain no additional condition outside the repository snapshots. It means the present GitHub authority corpus is insufficient for a unique finite extension/normalization claim. Missing selection authority is neither coefficient zero nor model falsification.

## Latest independent Critical Review

No independent Critic review of terminal V9 is recorded on current main yet.

The latest relevant historical reviews remain V7/V5. Historical V7 `INVALID_IMPLEMENTATION` for run `35054748495` stays preserved; V8 and V9 consume only the separately repaired V7 chain. V5 remains `QUALIFIED`, with exact `00000=11/24` and same-realization/source identity confirmed scoped while broad-support promotion remains forbidden.

## Prior terminal state retained

- repaired V4: highest `delta''` contact leading homogeneous tensor confirmed scoped in the frozen `j=1`, fixed-tangent, channel-`00000` realization;
- V5: exact channel map and `00000=11/24` confirmed scoped, broader support claim qualified;
- V6: local plain-delta triangle remains a distinct qualified object;
- repaired V7 run `35067251385`: `TRIANGLE_DELTA2_CONTACT_REGULARIZATION_DEPENDENT_SCOPED`;
- V8 run `35072993488`: `SOURCE_UNFIXED_FINITE_LOCAL_FREEDOM_SCOPED`, exact local counterterm absorbability with nullity `6`.

## Next admissible work

Highest-information next gate: prospectively freeze a same-realization **source-expansion / finite-renormalization authority ingestion gate**.

It should enlarge the durable GitHub source corpus, before classification, with immutable exact excerpts or structured records from the cited primary causal-vertex/Toller sources specifically covering finite normalization, gluing/composition, RG/scale, distributional-extension prescriptions, or explicit equivalence/quotient statements. It must then rerun the V9 selector question without weakening V9 criteria.

If the expanded source corpus still contains no actionable selector, preserve the source-authority blocker. Do not infer model failure.

## Governance lock

- `RQIR Core v1.0 = FROZEN`.
- `BLOCKED != FAIL`.
- missing authority != coefficient zero.
- finite certificate != universal theorem.
- scoped child result != family closure.
- numerical/implementation blocker != scientific falsification.
- `D7-S2 = NOT_CLOSED`.
- `D7-S3 = NOT_CLOSED`.
- `D7-S4 = PARTIAL_GLOBAL_NOT_CLOSED`.
- terminal D7 labels and `EXISTING_SUFFICIENT / ADAPT_EXISTING / HYBRID_REQUIRED / NEW_REQUIRED` remain forbidden until required subgates close.
- Candidate Gravity remains inactive.
- Green CI is provenance, not science.
- No authority exists for `QUANTUM_GRAVITY_SOLVED`, `ALL_KNOWN_MODELS_FAIL`, `NEW_THEORY_REQUIRED`, or `NEW_PHYSICS_FOUND`.
