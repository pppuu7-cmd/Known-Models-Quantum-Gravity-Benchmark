# KMQGB Current Benchmark Front

**Updated:** 2026-09-09  
**KMQGB iteration:** 097  
**Repository:** `pppuu7-cmd/Known-Models-Quantum-Gravity-Benchmark`  
**Branch:** `main`  
**Phase:** FIRST HARD DATUM CORRIDOR NARROWED / GRAVITY-NATIVE ORIGIN LAW REQUIRED

## Stable readiness metrics

Authority: `protocol/READINESS_METRICS.md`.

- **R1 — Repository readiness: 92%.**
- **R2 — KMQGB methodology/material readiness: 89%.**
- **R3 — Candidate Gravity scientific readiness: 24%** at fresh external RQIR Iteration 649.
- **R4 — `MINIMAL_NOVEL_PARENT_PRINCIPLE_SEARCH`: 45%.**

Last score change remains Iter060: R2 `88 -> 89`. Iter061–097 are score-neutral under the frozen rubrics because no explicit novel P4 survivor has passed A1–A4/A3 containment.

No robust unique-QG residual is authorized and no KMQGB Candidate Gravity ansatz is promoted.

## Construction authority

Use

- `protocol/CANDIDATE_GRAVITY_CONSTRUCTION_PLAYBOOK.md`;
- `protocol/P4_CONSTRUCTIVE_SURVIVOR_MATRIX.md`;
- `protocol/P4_FUNCTIONAL_FREEDOM_NO_GO.md`;
- the new first-hard-datum gates through Iter097.

Mandatory route:

`A1 explicit parent -> A2 finite freedom -> A3 comparator survival -> A4 normalized hard/cross-representation relation -> P5 -> P6 -> G0-G10`.

Heavy compute remains forbidden for structural/origin/authority blockers.

## What Iter082–097 established

### Modified-quantum-dynamics squeeze

`protocol/O4_GISIN_STINESPRING_SQUEEZE.md` freezes the O4 boundary:

- linear CPTP reduced dynamics is environment/Stinespring-degenerate at the reduced-channel level;
- generic deterministic nonlinear state dynamics faces Gisin/Polchinski no-signalling problems;
- novelty therefore has to live in a joint gravity+matter process law with hard spin-2/CTP consequences.

### First-hard-datum theorem

`protocol/P4_SAME_FACTORIZATION_CONTACT_DIFFERENCE_THEOREM.md` shows:

`same spectrum + same GR factorization poles/residues + local analytic difference -> full C5 contact space`.

Thus ordinary factorization/recursion cannot manufacture KG novelty from the already-matched GR three-point data.

### Failed synthesis controls

- `SYNTHESIS-001`: `F=1+XF^3` is finite-freedom but exactly melonic/Fuss-Catalan random-tensor architecture -> A3 FAIL.
- `SYNTHESIS-002`: physical-helicity parquet closure is gravity-sensitive but does not select its irreducible `K4`; Einstein-only input is ordinary GR QFT resummation and inherits C5 renormalization freedom.
- `SYNTHESIS-003`: subtraction-free dispersion moves freedom into `Im a_l(s)`/discontinuity functions.
- `SYNTHESIS-004`: finite 4↔5 soft/splitting loci leave null contact directions such as high-derivative five-graviton contacts.
- `SYNTHESIS-005`: Schwarz-Pick extremality collapses a causal Herglotz response to a rational Möbius map -> finite mediator class.
- `SYNTHESIS-006`: trace-anomaly origin fixes a particular nonlocal action but leaves arbitrary Weyl-invariant homogeneous functional freedom.

### All-point positive controls

Two strong controls show that finite all-point generation is realistic:

1. topological recursion: finite spectral-curve data -> all `(g,n)` hierarchy, but matrix/JT/string/dynamical-triangulation comparator;
2. celestial `Lw_{1+infinity}` Ward recursion: gravity-native three-graviton seed -> all-point single-minus sector in restricted kinematics, but ordinary Einstein-gravity comparator and symmetry alone is not unique.

Hence **generator completeness is required but not sufficient**; the hard seed itself must have a new physical origin.

### Fresh comparator upgrades

`protocol/FRESH_LQG_UV_FIXED_POINT_COMPARATOR_2026.md` upgrades covariant LQG/spinfoam: a 2026 candidate UV fixed point can reduce infinite triangulation ambiguity to finite boundary coefficients. Future candidates cannot beat LQG by merely claiming finite boundary data or triangulation independence.

BFSS/matrix hard-amplitude authority remains a strong microscopic-parent comparator.

## Exact same-spectrum corridor after Iter095–097

### Ordinary new physical cuts

`protocol/P4_STANDARD_UNITARITY_NEW_CUT_ATTRIBUTION_GATE.md`:

with standard unitarity, physical discontinuities are determined by on-shell intermediate states and lower transition amplitudes. A new cut therefore requires new/modified states, changed lower amplitudes, a genuinely missing nonperturbative sector, or modified quantum/factorization law.

A cut cannot be inserted as independent hard data while the complete comparator transition data remain unchanged.

### Entire hard functions

`protocol/P4_ENTIRE_POLYNOMIAL_BOUNDED_COLLAPSE_GATE.md`:

`entire + finite global polynomial bound -> polynomial`.

Therefore, conditionally within the ordinary polynomially-bounded/local analytic class, a pole-free entire same-spectrum correction collapses to C5 contact data.

`protocol/P4_HADAMARD_ENTIRE_HARD_DATA_GATE.md` further shows:

- zero-free finite-order entire -> `exp(Q)` with polynomial `Q`, under known weakly-nonlocal/form-factor comparator pressure;
- infinite-zero finite-order entire -> the zero set is essential dynamical data and must itself be derived from finite physical law;
- strong known zero-generation laws face string/dual-resonant comparator pressure.

## Current scientific bottleneck

The preferred target is no longer “find an interesting kernel”. It is:

`finite gravity-native origin law`

`-> first comparator-orthogonal irreducible hard datum with fixed normalization/growth/singularity class`

`-> complete or explicitly generatable higher-point hierarchy`

`-> same-parent Lorentzian retarded/CTP block`

`-> immediate comparator survival`.

For a same-spectrum branch, the surviving corridor is now primarily

1. a derived genuinely nonlocal/exponentially-bounded irreducible hard structure with explicit causality/growth class;
2. an intrinsic gravitational nonperturbative/transseries hard structure with parent-fixed contour, weight and phase;
3. a controlled modified factorization/quantum law satisfying probability, composition and no-signalling.

New states/poles/cuts remain possible but route immediately to E2/string/tower/mediator comparators.

## External RQIR authority — Iter649

RQIR remains **24%**.

For

`A_cand=lambda_native*a`, `A_Born=lambda_source*b`, `rho=lambda_source/lambda_native`,

`A_matched=lambda_native*(a-rho*b)`.

Projectivization removes the common `lambda_native` but not the independent `rho`; the historical open response is not a valid one-factor proxy. Source/Born subtraction and comparator residual remain blocked.

Exact external next gate: derive or locate the same-parent Source/Born contribution inside the closed retarded gravitational `Gamma3` observable with frozen CTP/Legendre normalization and pole/cut origin.

## Exact next KMQGB front

Do **not** begin with another free propagator, arbitrary spectral density, bootstrap checklist, Ward recursion with an unspecified seed, or hand-selected form factor.

Search for the **origin law of the first irreducible hard datum itself**. Require prospectively:

- finite parent data;
- fixed absolute normalization;
- declared growth/singularity class;
- gravity-specific attribution;
- all-point generator completeness or a direct route to it;
- same-parent CTP/retarded structure;
- immediate survival against full C5, string/q-string, BFSS/matrix, fresh LQG/spinfoam, AS, nonlocal, mediator/KL and environment comparators.
