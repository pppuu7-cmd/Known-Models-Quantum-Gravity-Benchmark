# KMQGB Current Benchmark Front

**Updated:** 2026-09-08  
**KMQGB iteration:** 035  
**Repository:** `pppuu7-cmd/Known-Models-Quantum-Gravity-Benchmark`  
**Branch:** `main`  
**Phase:** WAVES 1–12 TERMINALLY CLASSIFIED / INTERVENTION RIGIDITY FROZEN / PREDICTIVE HOLDOUT RIGIDITY NEXT

## Coverage

- Wave 1: **9/9 = 100%** — immutable.
- Waves 2–12: **each 5/5 = 100%** — immutable.
- Globally authorized robust unique-QG residuals: **0**.
- KMQGB-promoted Candidate Gravity ansatz: **none**.
- External Candidate Gravity readiness: **24%**.

## Permanent Candidate Gravity construction stack

Authoritative files:

- `protocol/CANDIDATE_GRAVITY_DESIGN_PRIORS.md`;
- `protocol/CANDIDATE_GRAVITY_PROMOTION_GATE.md`;
- `protocol/PARENT_KERNEL_RESPONSE_COMPLETENESS.md`;
- `protocol/RESIDUAL_SPACE_GEOMETRY.md`;
- `protocol/OPTIMAL_COMPARATOR_CONTRASTS.md`;
- `protocol/CROSS_ORDER_RIGIDITY.md`;
- `protocol/INTERVENTION_CONFIGURATION_RIGIDITY.md`.

Reference code:

- `code/inverse_kernel_ordered_partitions.py`;
- `code/residual_space_geometry_reference.py`;
- `code/optimal_comparator_contrasts_reference.py`;
- `code/cross_order_rigidity_reference.py`;
- `code/intervention_design_reference.py`.

## Wave 11 — cross-order rigidity

Terminal `5/5`, methodology PASS.

For shared parent parameters across response blocks:

`R_shared = sum_ell rank(J_ell) - rank(J_stack) >= 0`.

`R_shared>0` quantifies extra consistency directions generated by shared cross-order parameters.

Exact monomial scaling can yield global nuisance-free invariants from the left nullspace of the exponent matrix.

## Wave 12 — multi-configuration/intervention rigidity

Terminal `5/5`, methodology PASS.

Controlled source/detector variables are design variables when known, not free nuisance parameters.

Shared dynamics parameters remain shared across configurations; only genuinely local nuisances may vary independently.

Adding a configuration with `k` physical coordinates obeys

`Delta d_perp = k - Delta rank(J_union)`.

Interventions are selected prospectively by post-comparator rank, projected singular values or projected SNR.

Attribution-oriented controls include source composition, orientation/polarization, distance/time/frequency sweeps, probe-state swaps and null/reversal geometries. Mass/distance scaling alone is not gravity attribution.

## Candidate Gravity promotion gate retained

No ansatz is promoted before

`one parent dynamics -> complete responses -> exact constraints -> common-domain comparators -> attribution stack -> full C5 quotient -> nonzero COR -> global separation -> projected identifiability -> rigidity`.

Only afterward may model readiness/Fisher/resources advance.

## External RQIR read-only authority

Latest directly observed scientific authority: **Iteration 590**, `MODEL_READINESS=24%`.

Iter590 proves the complete cubic MSSC source response contains

- local `K3`;
- six ordered `K1/K2` placements;
- six ordered `K1^3` chains;

with nonzero support for both omitted families on the frozen fixture.

External next gate remains:

1. classify hard-channel discontinuity origin of local `K3`;
2. classify `K1^3` under the frozen linked-cut protocol;
3. only then close complete nonlinear source Ward block and map to Iter582/comparator quotient.

No active or queued RQIR Actions were observed at the last direct check.

## KMQGB external K3 analyticity pre-check

Created

`external_rqir_checks/iter590_k3_local_analyticity_precheck.md`.

Independent algebra shows that the isolated local MSSC K3 coefficient is built from a local polynomial expansion of `sqrt(-g)` and `g^-1` with no internal propagator/nonanalytic form factor. Therefore it should have zero **standalone** hard-channel branch discontinuity, subject to exact RQIR routing/continuation verification.

This is not RQIR authority and does not delete the larger `G K3 G` composite or classify K1^3.

## Exact next KMQGB front — predictive/holdout rigidity

Freeze a prediction-before-retuning protocol:

1. split configurations/orders prospectively into calibration/training and held-out prediction blocks;
2. fit shared comparator/KG parameters only on training blocks;
3. forbid holdout-specific retuning unless the parent theory contains a declared local nuisance;
4. propagate full cross-covariance and parameter uncertainty into the held-out predictive covariance;
5. use leave-one-configuration/order-out validation as a rigidity diagnostic.

Goal: distinguish one genuinely predictive parent dynamics from a flexible family that only succeeds by retuning each new observable/configuration.
