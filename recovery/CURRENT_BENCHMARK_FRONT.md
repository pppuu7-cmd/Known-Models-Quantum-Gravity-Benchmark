# Current Benchmark Front
Updated: 2026-09-16

Fresh repository `main` and fresh Actions state always outrank this navigation document if they diverge.

## Current active Research / Closure gate

`SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_REGULARIZATION_V7`

Prospective/frozen chain:

- V7 preregistration `764cd9f915cef15b6294f5f72329f12cb145c08e`;
- implementation `44da1edd39debce755f9ae7d48c4e7abd375604c`;
- workflow head `e5021974d02f7cd52c540e80f5b3a14fcf0e5cdf`;
- authoritative run `35054748495`.

Fresh Actions state at this reconciliation:

- source-lock job `104662442178` = `completed/success`;
- exact Python 3.13 job `104667121284` = `completed/success`;
- exact Python 3.11 job `104667121315` = `completed/success`;
- aggregate job `104671916886` = `queued / conclusion=null`;
- workflow run remains nonterminal; no aggregate scientific classification is consumed.

No partial `K(A/B/C)` values or nonterminal lane artifacts are used as science.

## Latest independent Critical Review

Latest Critic authority is the preterminal V7 audit:

`recovery/CRITICAL_PRETERMINAL_AUDIT_SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_REGULARIZATION_V7_2026-09-16.md`

Critic commit:

`e08f086ec39df5b311c928945590e2f4fd17f2e9`

Status:

`PRETERMINAL_ONLY — NO SCIENTIFIC VERDICT`

The audit confirms prospective chronology and static V7 object identity: local conormal map `B12=x`, `B23=y`, `B13=x+y`; normalized Gaussian `delta''` factors; frozen exact rational statistic `K = pi*C^2/(a+b+c)^7`; schemes A/B/C plus common-rescaling A4.

Critic also records two mandatory frozen-control implementation defect candidates that must be applied in terminal review if the source remains unchanged:

1. derivative-identity control is tautological/disconnected from the production derivative representation;
2. transverse two-contact control is computed as a product of one-factor controls instead of an independent replay through the same multivariate Gaussian/Wick machinery used by production.

Therefore green exact lanes cannot by themselves validate a PASS/FAIL classification. If the aggregate terminalizes under unchanged source, the Research output must still be audited against these control-binding defects.

## Latest terminal Research execution

The latest terminal Research result remains V6:

`SOURCE_J1_K5_TRIANGLE_CONTACT_REGULARIZATION_V6`

Authoritative run `35050058294`, terminal `completed/success`.
Research classification:

`TRIANGLE_CONTACT_REGULARIZATION_DEPENDENT_SCOPED`

Independent Critic verdict remains `QUALIFIED`: V6 is a plain-delta local triangle result and cannot be promoted to the repaired V4 highest-contact `delta''` realization.

## V7 scientific contract

Exact object:

`D2_eps^a(x) D2_eps^b(y) D2_eps^c(x+y)`

with normalized Gaussian second derivatives and frozen schemes:

- A `(1,1,1)`;
- B `(1,1,4)`;
- C `(1,2,3)`;
- common-rescaling A4 `(4,4,4)`.

Frozen decisions:

- PASS: `TRIANGLE_DELTA2_CONTACT_REGULARIZATION_DEPENDENT_SCOPED`;
- FAIL: `TRIANGLE_DELTA2_CONTACT_REGULARIZATION_INVARIANT_SCOPED`;
- BLOCKED: `TRIANGLE_DELTA2_CONTACT_REGULARIZATION_SOURCE_BLOCKED`;
- INVALID: `INVALID_IMPLEMENTATION`.

No frozen criterion may be changed after result.

## Scope / interpretation ceiling

V7, even if terminally valid, is only a local normalized-Gaussian derivative-contact triangle statement. It does not establish:

- a theorem for all mollifier classes;
- distributional extension nonexistence;
- full ten-contact K5 product failure;
- complete K5 vertex convergence/divergence;
- model/family failure;
- D7 closure;
- terminal selectors;
- Candidate Gravity activation.

Finite local certificate != universal theorem; scoped child result != family closure.

## Independent registered workflows

Do not consume partial substantive values:

- Iter504 run `34907349374` is nonterminal; latest Critic noted it had advanced to `in_progress / conclusion=null`;
- Iter461 run `34748503239` remains nonterminal / `queued / conclusion=null` in the latest Critic snapshot.

These are not authority for the active V7 gate.

## Next admissible work

Do not launch a competing V7 scientific gate while run `35054748495` is nonterminal.

First consume only the terminal aggregate/artifacts/digests of this same run. Then apply the independent Critic audit. If terminal review finds the mandatory frozen controls were not genuinely implemented, classify the historical Research output as implementation-invalid on its scientific claim path and perform only the contract-preserving control-wiring repair identified by Critic:

1. use one executable production derivative representation for production and derivative positive/negative controls;
2. independently replay the frozen transverse two-contact integral through the same multivariate Gaussian/Wick machinery;
3. preserve object, conormal map, schemes, statistic, PASS/FAIL/BLOCKED/INVALID semantics and interpretation ceiling.

Changing those scientific fields requires a new prospectively frozen gate.

## Governance lock

- `RQIR Core v1.0 = FROZEN`.
- `BLOCKED != FAIL`.
- finite certificate != universal theorem.
- missing object != zero residual.
- scoped child result != family closure.
- numerical-method blocker != scientific falsification.
- `D7-S2 = NOT_CLOSED`.
- `D7-S3 = NOT_CLOSED`.
- `D7-S4 = PARTIAL_GLOBAL_NOT_CLOSED`.
- terminal D7 labels and `EXISTING_SUFFICIENT / ADAPT_EXISTING / HYBRID_REQUIRED / NEW_REQUIRED` remain forbidden until required subgates close.
- Candidate Gravity remains inactive.
- Green CI is provenance, not science.
- No authority exists for `QUANTUM_GRAVITY_SOLVED`, `ALL_KNOWN_MODELS_FAIL`, `NEW_THEORY_REQUIRED`, or `NEW_PHYSICS_FOUND`.
