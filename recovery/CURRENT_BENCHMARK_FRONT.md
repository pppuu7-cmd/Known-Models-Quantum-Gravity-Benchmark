# Current Benchmark Front
Updated: 2026-09-16

Fresh repository `main` and fresh Actions state always outrank this navigation document if they diverge.

## Latest terminal Research execution

`SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_COUNTERTERM_EQUIVALENCE_GATE`

Terminal Research classification:

`SOURCE_UNFIXED_FINITE_LOCAL_FREEDOM_SCOPED`

Authority / execution chain:

- prospective V8 preregistration commit `38054879237dd03e9d368ceb47356e669c02b516`;
- pre-implementation executable protocol + immutable authority ledger commit `f5b4caa7e439262baee55a890b786d016101dbfc`;
- implementation/workflow head `6c4f286f3d7db91f9646788a54535486bde88585`;
- terminal result commit `3e6e069d728ccef2724c26f374c298f29d591043`;
- authoritative Actions run `35072993488`, terminal `completed/success`.

Jobs:

- source lock `104718668996`;
- exact Python 3.11 `104718712236`;
- exact Python 3.13 `104718712407`;
- aggregate `104718759457`.

Artifacts:

- source lock `10437017515`, digest `sha256:650ab9296f78dd6fab5ac4b95babc8be21b9a59559652a6f8f52af669d79f558`;
- Python 3.11 `10437400162`, digest `sha256:02d4da0f7cd248c8471c8bf5ab2101cfdcf105df0ae705475e8da87884ecb89e`;
- Python 3.13 `10436962644`, digest `sha256:eaafd3c9d7d462d5d47f3f85ba92ea7b32efd8eb11ee9b0aa4ccfea53a79460e`;
- aggregate `10436264023`, digest `sha256:a4b6b867d3cb3b29c119e342f6f399e0b37ec7b82797f3e5d2e3c4bb09d1b314`.

Canonical aggregate JSON SHA256 `7746fed4183fb1ada48db412218785299e5762da151ef1c9334db89197039cd6`; raw aggregate log SHA256 `944f452139be6fcacd83ed015235622110f0cf0d0828c68a98346ee6be1eccca`.

Green CI is execution provenance only; the classification comes from the prospectively frozen exact rational classifier.

## Exact V8 result

The same repaired-V7 local highest-contact `delta''` triangle is retained:

- `B12=x`, `B23=y`, `B13=x+y`;
- exact V4/V5 fixed source/channel realization;
- local point-supported jet freedom through derivative order `7` from scaling degree `9` on the two-dimensional normal space.

The implementation enumerated the complete jet class through order 7 and projected it mechanically to the exact triangle-permutation (`S3`) invariant subspace. Invariant dimensions by derivative degree `0..7` are

`{1,0,1,1,1,1,2,1}`,

for total invariant dimension `8`.

The repaired-V7 diagnostics reproduce exactly:

- `K(A)=1600/531441`;
- `K(A4)=1600/531441`;
- `K(B)=204800/1162261467`;
- `K(C)=14400/19487171`.

The prospectively frozen finite-counterterm equivalence system

`(M_s-M_A)c = K_A-K_s`, `s in {A4,B,C}`

has exact rank `2`, augmented rank `2`, and nullity `6`. It is therefore solvable but non-unique. One canonical RREF representative equalizes all four corrected diagnostics exactly to

`726034269538880/22649187954139857`.

The synthetic in-span positive control solves exactly; the prospectively frozen outside-span RHS `(1,0,0)` is rejected with rank `2`, augmented rank `3`. Both Python lanes produce identical full decision payloads.

## Source-authority interpretation

The pre-implementation immutable authority ledger locks the relevant V4 repaired terminal result, V5 independent Critic source/realization audit, repaired-V7 canonical/result files, and V8 preregistration by Git blob SHA. Runtime reproduced every lock and required positive scope fact.

That corpus authorizes testing the finite local collision-supported class but does not fix all finite coefficients and does not fix a common finite normalization. Hence the exact solvability result is `SOURCE_UNFIXED_FINITE_LOCAL_FREEDOM_SCOPED`, not source-fixed uniqueness.

New durable fact: on the finite repaired-V7 Gaussian scheme set, the observed A/B/C relative-width dependence lies exactly in the admissible S3-invariant local-jet action span through order 7. V7 regulator dependence therefore cannot by itself be promoted to distributional nonexistence or model/family failure. The active blocker is now finite-renormalization/source-normalization selection authority.

## Latest independent Critical Review

No independent Critic review of terminal V8 is yet recorded on current main.

The latest relevant Critic records remain the earlier V7/V5 reviews. Historical V7 `INVALID_IMPLEMENTATION` applies to historical run `35054748495` and remains preserved; its implementation defects were repaired before the separately terminal V7 repair consumed by V8. V5's `QUALIFIED` review still confirms the exact `00000=11/24` source/realization fact scoped while forbidding promotion of the finite census to an unfrozen broad-support claim.

## Prior terminal state retained

- repaired V4 run `35033194283`: highest `delta''` contact leading homogeneous tensor confirmed scoped in the frozen `j=1`, fixed-tangent, channel-`00000` realization;
- V5 channel census: `QUALIFIED`, exact channel map and `00000=11/24` confirmed scoped;
- V6 local plain-delta triangle remains a distinct qualified object and does not substitute for the derivative-contact object;
- repaired V7 run `35067251385`: `TRIANGLE_DELTA2_CONTACT_REGULARIZATION_DEPENDENT_SCOPED`, retained historically and now further localized by V8 rather than overwritten.

## Independent registered workflows

Do not consume partial substantive values from unrelated workflows. Iter504 / Iter461 remain outside V8 authority unless and until separately terminalized and explicitly admitted by a prospective dependency contract.

## Next admissible work

Highest-information next gate: prospectively freeze a same-realization finite-renormalization authority/normalization gate, e.g.

`SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_FINITE_RENORMALIZATION_AUTHORITY_GATE`.

It should ask whether source normalization, gluing/composition identities, RG/scale identities, or another already-authorized same-realization condition fixes the six-dimensional V8 finite freedom (or a prospectively justified quotient of it), while preserving the off-collision object and V4/V5 source/channel realization.

If the source supplies no such selector, record a source-authority blocker/free-renormalization fact. Missing selection authority is not scientific/model FAIL.

## Governance lock

- `RQIR Core v1.0 = FROZEN`.
- `BLOCKED != FAIL`.
- finite certificate != universal theorem.
- missing object != zero residual.
- scoped child result != family closure.
- numerical-method/implementation blocker != scientific falsification.
- `D7-S2 = NOT_CLOSED`.
- `D7-S3 = NOT_CLOSED`.
- `D7-S4 = PARTIAL_GLOBAL_NOT_CLOSED`.
- terminal D7 labels and `EXISTING_SUFFICIENT / ADAPT_EXISTING / HYBRID_REQUIRED / NEW_REQUIRED` remain forbidden until required subgates close.
- Candidate Gravity remains inactive.
- Green CI is provenance, not science.
- No authority exists for `QUANTUM_GRAVITY_SOLVED`, `ALL_KNOWN_MODELS_FAIL`, `NEW_THEORY_REQUIRED`, or `NEW_PHYSICS_FOUND`.
