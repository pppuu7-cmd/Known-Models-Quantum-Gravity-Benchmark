# O-LQG Subleading Entropy Comparator Gate — 2026-09-10

**KMQGB iteration:** 159  
**RQIR standard:** Core v1.0 FROZEN  
**Closure-wave target:** CW2-02 / O-LQG.

## Question

Can a logarithmic correction to the black-hole/spinfoam entropy serve as the comparator-orthogonal LQG observable required after Iter155?

## Answer

**Not by its functional form alone.**

A correction of the schematic form

`S = A/(4G) + alpha log(A/G) + ...`

is widely generated by distinct physical mechanisms and frameworks. Recent authorities include:

- ordinary Einstein–Maxwell / supergravity gravitational path integrals with regulated extremal zero modes;
- Einstein–Gauss–Bonnet one-loop fluctuations;
- loop-quantum-gravity microstate counting;
- string/supersymmetric microscopic constructions;
- thermal/statistical fluctuation methods;
- even minimum-assumption Planck-cell discretization models.

Therefore `log A` is a **comparator-degenerate basis function**, not an LQG signature.

## Frozen-RQIR gate

A subleading-entropy claim can contribute to CW2-02 closure only through a same-realization **rigidity vector**, not a single term:

`I_entropy = {alpha_log, beta_const, gamma_1/A, parameter sharing, charge/spin/background dependence, correlation/replica structure, regulator/continuum behavior}`.

At minimum the candidate LQG realization must freeze:

1. the coefficient `alpha_log` from the same dynamics that generated the leading Lorentzian entropy;
2. its dependence, or proven independence, on Barbero–Immirzi parameter, boundary coefficients, 2-complex family and regulator;
3. the same-realization constant and next subleading terms where defined;
4. a prediction across more than one controlled geometry/charge/spin/background sector or an equivalent independent observable;
5. identical-domain GR/QFT one-loop, string/supergravity and other applicable quantum-gravity comparator coefficients;
6. a statement of which parameter is shared across all blocks rather than refit per geometry.

## Nullspace argument

If only the existence of a logarithm is specified, the comparator span already contains the same functional direction. A nonzero raw correction therefore satisfies

`Delta_raw != 0`

but can still have

`R_comparator = 0`

once ordinary fluctuation/microstate mechanisms are admitted.

A coefficient difference is also insufficient if each framework is allowed independent field-content, ensemble, regulator or horizon-state choices. The discriminant must be a **cross-condition relation** with shared parent parameters.

## Classification

`GENERIC_LOG_CORRECTION__OPERATIONALLY_COMPARATOR_DEGENERATE`.

CW2-02 remains open.

## Exact LQG target after Iter159

`SAME_REALIZATION_SUBLEADING_ENTROPY_RIGIDITY_VECTOR_OR_OTHER_COMPARATOR_ORTHOGONAL_LORENTZIAN_OBSERVABLE`.

The strongest next entropy-based evidence would be a multi-sector coefficient relation fixed by the same covariant-LQG amplitude/continuum prescription that produced the Iter155 Lorentzian area law.

## Paper-IV consequence

A generic log correction must never be used as evidence for either LQG uniqueness or `NEW_REQUIRED`. It is useful only after the full comparator/parameter-sharing quotient is performed.
