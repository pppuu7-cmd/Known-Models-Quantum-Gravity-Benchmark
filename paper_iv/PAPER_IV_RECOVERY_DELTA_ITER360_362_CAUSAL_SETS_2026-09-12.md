# Iter360–362 Causal Sets recovery delta

## Validated authority

- Scientific workflow: `paper-iv-iter360-362-causal-set-layered-suppression`
- Scientific run: `34657780142` — **SUCCESS**
- Methodology CI: `34657780127` — **SUCCESS**
- Reproducibility release: `34657870332` — **SUCCESS**
- Summary artifact: `10285788989`
- Summary ZIP SHA-256: `f6bf73101e84a4fdb3526d0e6d596a3ce6eb3f4e349340a0b3049fd20f590296`

## Scientific delta

Six independent asymptotic stress regimes reproduce the source hierarchy in which an `n^3` non-link-interval penalty eventually dominates the `n^2` layered-set entropy. The largest stressed crossover was `n = 6896756.393652842`; all six regimes are on the suppression side well above their crossover.

The source-quoted discreteness threshold is handled fail-closed: in `d=4`, only `ell/ell_p > 1.136` is certified from the quoted approximate `ell_0 ~ 1.136 ell_p`; from the quoted general bound, `ell/ell_p > 2.33` is sufficient across the stated dimension range. This does not reinterpret the threshold as a necessary family-wide bound.

## Frozen classification

`CAUSAL_SETS = PARTIAL_SUBFAMILY_ONLY`

The entropically dominant typical `K`-layered sector is now a scoped closed obstacle: the BDG action reduces to the link action at leading order and strongly suppresses that sector above the source threshold. Family closure remains blocked by the sparse layered measure-zero class, non-layered non-continuumlike causal sets, dimension selection, a source-defined full measure-to-emergent-manifold trajectory, and a normalized genuinely causal-set gravitational observable with same-domain comparator/error ledger.

No layered-sector result is promoted to full emergent-manifold closure, no BLOCKED state is converted to FAIL, and no D7 promotion is authorized.
