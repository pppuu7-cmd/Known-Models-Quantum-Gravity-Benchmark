# O-LQG γ Cross-Representation Rigidity Vector — 2026-09-10

**KMQGB iteration:** 166  
**RQIR standard:** Core v1.0 FROZEN  
**Closure-wave target:** CW2-02 / O-LQG.

## Exact relations from γ-duality

Bianchi–Rincon-Ramirez derive a nontrivial relation between parity-even and parity-odd curvature couplings in a gamma-dual effective theory.

For the Gauss–Bonnet and Chern–Simons functions,

`2 f_GB(phi) / f_CS(phi) = gamma - 1/gamma`.

In their slow-roll inflationary example, the same `gamma` obeys

`1/gamma - gamma = (pi/8) (r + 8 n_T) / Pi`,

where `Pi` is the primordial tensor circular-polarization asymmetry, `r` is the tensor-to-scalar ratio and `n_T` is the tensor tilt.

For positive `gamma`, defining

`q := (pi/8) (r+8 n_T)/Pi`,

the observable block determines

`gamma = (sqrt(q^2+4) - q)/2`.

The same microscopic Barbero–Immirzi parameter also sets the LQG elementary-area scale

`a_* = 4 sqrt(3) pi gamma l_P^2`.

## Proposed frozen-RQIR rigidity vector

A stronger Paper-IV test than any single cosmological or entropy observable is

`I_gamma = {2 f_GB/f_CS, Pi, r, n_T, a_*, gamma-dependent spinfoam/entropy data}`

with **one shared gamma** and no per-block refit.

This is attractive because different components live in different representations:

- continuum parity-even/odd EFT coefficients;
- inflationary tensor observables;
- microscopic quantum-geometry area scale;
- potentially the covariant-spinfoam stack/entropy coupling relation.

## What is already rigid

In a generic parity-violating gravitational EFT the parity-even and parity-odd functions are independent. Gamma-duality removes one functional direction by imposing a one-parameter relation.

Likewise, `Pi`, `r` and `n_T` are not fitted independently in the gamma-dual example: their consistency combination reconstructs the same gamma.

This is genuine cross-observable rigidity and is stronger than a raw parity-violating signal.

## What is not yet derived

The source paper explicitly states that a top-down derivation of the effective action directly from the non-perturbative spinfoam dynamics `W_gamma` is still missing. It assumes exact realization of gamma-duality in the semiclassical effective action and notes that possible breaking effects require study.

Therefore the key missing map is

`complete EPRL/covariant-LQG amplitude -> renormalized gamma-dual EFT -> I_gamma`.

The current rigidity vector is **conditional on that bridge**.

## Comparator requirements

A terminal RQIR test must compare against

- generic Gauss–Bonnet + Chern–Simons inflation with independent functions;
- axion/Chern–Simons and other parity-violating gravity EFTs;
- string-inspired parity-violating operators;
- loop-quantum-cosmology/spinfoam-cosmology alternatives;
- inflationary nuisance/background freedom.

The discriminant is not `Pi != 0`. It is the shared relation

`Q_gamma := 1/gamma - gamma - (pi/8)(r+8 n_T)/Pi = 0`

combined with the same gamma in an independent microscopic/geometry block.

## Classification

`PROMISING_ADAPT_EXISTING_CROSS_REPRESENTATION_RIGIDITY__TOP_DOWN_BRIDGE_MISSING`.

CW2-02 remains open, but this vector is now its preferred closure route over generic logarithmic entropy corrections.

## Exact next gate

Derive or locate authority for the survival of gamma-duality under the continuum/coarse-graining map of a complete covariant-LQG realization. If that map fixes the same gamma-dependent EFT relation prospectively, build an executable comparator/identifiability test for `I_gamma` rather than continuing the entropy-only route.
