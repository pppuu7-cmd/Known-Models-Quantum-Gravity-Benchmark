# Iter356–359 CDT recovery delta

## Validated authority

- Scientific workflow: `paper-iv-iter356-359-cdt-continuum-identifiability`
- Scientific run: `34657454153` — **SUCCESS**
- Methodology CI: `34657454078` — **SUCCESS**
- Reproducibility release: `34657546235` — **SUCCESS**
- Summary artifact: `10285632409`
- Summary ZIP SHA-256: `8889e93f07cdb6a4d04f03f7d78d6a8616a5a49a151d2db0344669c32ecf27d6`

## Scientific delta

The 2026 CDT UV product-scaling estimate `delta = 0.54 +/- 0.04` is exactly one quoted standard deviation from the `delta=1/2` scaling required for constant `Lambda G`. The central exponent implies a coupling-proxy ratio `0.6918309709189363` across an `N4` ratio of `10^4`, i.e. a fractional drift `-0.3081690290810637` if interpreted literally rather than as an uncertain estimate.

More importantly, writing `omega ~ N4^p` and `Gamma ~ N4^q`, the product result constrains only `2p+q=delta`. The same product exponent admits both `p>-1/4`, for which `omega N4^(1/4)` grows, and `p<=-1/4`, for which it fails to grow. Therefore product scaling alone does not identify the UV time-extension condition highlighted by the source.

The spectral-dimension fit is independently reproduced with `D_S(0)=1.8162962962962959` and large-diffusion limit `4.02`, but remains a scoped child observable rather than a terminal continuum certificate.

## Frozen classification

`CDT_EDT = PARTIAL_SUBFAMILY_ONLY`

Refined blocker:

`CDT_SEPARATE_OMEGA_GAMMA_CRITICAL_SCALING_PLUS_SOURCE_DEFINED_4D_CONTINUUM_TRAJECTORY_AND_LATTICE_SPACING_MAP_WITH_PROPAGATED_FINITE_SIZE_DISCRETIZATION_NUMERICAL_ERRORS_PLUS_INVARIANT_PHYSICAL_OBSERVABLE_AND_SAME_DOMAIN_COMPARATOR`

No one-sigma compatibility is promoted to exact closure; no product scaling is promoted to separate exponent identification; no child observable terminalizes the parent family; no D7 promotion is authorized.
