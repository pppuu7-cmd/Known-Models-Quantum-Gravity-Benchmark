# External RQIR Iter659 Soft-Gamma3 Topology Sync

**KMQGB iteration:** 099  
**Observed external authority:** RQIR Iter659  
**Observed model readiness:** 24%  
**Status:** read-only external-authority synchronization / no KMQGB scientific promotion.

## 1. What changed externally

The external Candidate Gravity authority advanced from the previously recorded Iter649 to Iter659 while keeping `MODEL_READINESS=24%` and no active promotable ansatz.

The principal structural advance is no longer the old unmatched Source/Born relative normalization alone. RQIR now has a normalized same-parent closed-SK Gamma2/Gamma3 parent (Iter653), a prospective soft `T_cut` kinematic completion, an exact null-domain/gauge-overlap analysis, and a quotient-safe plus-TT measurement. Iter659 applies that measurement structurally to the normalized soft-Gamma3 pre-cut object.

## 2. Iter659 topology census

All six third-variation terms are retained with one common `i/2` parent prefactor and coefficients

`[+1,-1,-1,-1,+1,+1]`.

The topology split is

- one `K3` contact/tadpole term with one propagator;
- three `K1 K2` bubble terms with two propagators;
- two `K1^3` triangle orientations with three propagators.

`K3` has no external two-line cut from denominator topology. The bubble terms remain routing-dependent ordinary-cut candidates. The triangle terms retain ordinary two-line / possible leading-Landau support pending the soft-family routing audit.

No unsupported sector is zero-filled.

## 3. Scientific firewall

Despite this progress, RQIR still authorizes

- no Source/Born subtraction;
- no native numerical soft `T_cut`;
- no fixed comparator quotient;
- no robust unique residual;
- no ANSATZ-003;
- no Fisher/resource phase.

Therefore KMQGB R3 remains exactly 24%.

## 4. New external next gate

RQIR Iter660 is a fixed-epsilon soft-family routing / threshold / leading-Landau gate for the three bubbles and two triangles. The valid next question is support/routing, not parameter fitting or residual construction.

## 5. KMQGB lesson

The external branch is a useful positive control for the KMQGB requirement that a candidate parent carry a single prospective Lorentzian/CTP normalization and preserve topology before cuts/projections.

It does **not** currently supply a comparator-orthogonal hard datum for KMQGB P4.

## 6. Score consequence

- R1 unchanged;
- R2 unchanged;
- R3 remains 24%;
- R4 unchanged.

This is an authority synchronization, not a readiness event.