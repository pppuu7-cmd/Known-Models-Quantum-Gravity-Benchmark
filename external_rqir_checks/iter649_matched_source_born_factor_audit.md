# External RQIR Iter649 — Matched Source/Born Normalization Audit

**KMQGB iteration:** 072  
**Observed external authority:** RQIR Iteration 649, 2026-09-09  
**Role:** read-only Candidate Gravity firewall synchronization.

## Stable scientific score

External RQIR `MODEL_READINESS` remains **24%**. No robust comparator-subtracted residual and no promotable ansatz are authorized.

## Iter649 exact obstruction

The frozen normalization graph separates a source normalization class and a native closed-gravity class. For a candidate and Born contribution write

`A_cand = lambda_native * a`,

`A_Born = lambda_source * b`,

and define the independent relative factor

`rho = lambda_source / lambda_native`.

Then the matched object is

`A_matched = lambda_native * (a - rho b)`.

Projectivization can remove the overall `lambda_native` but cannot remove `rho`:

`P[A_matched] = P[a - rho b]`.

Therefore Source/Born subtraction is not made normalization-free by passing to a projective observable.

## Independent proxy rejection

External RQIR Iter629 had already shown that the historical open scalar response and the closed gravitational 1PI object have family-total coefficient vectors

`[-1, 6, -6]` and `[1, -3, 2]`,

with componentwise ratios `[-1,-2,-3]`.

Thus the open response cannot be converted to the missing closed gravitational Born proxy by one common scalar normalization.

## Fail-closed KMQGB lesson

The general rule is stronger than the earlier one-common-normalization guardrail:

> Projective cancellation is valid only for a factor common to every term entering the observable. If subtraction combines two independently normalized sectors, an unknown relative factor survives projectivization and absolute or projective residual claims remain blocked.

Never infer `rho` from the same Candidate/comparator values later used to claim a residual.

## Current external next gate

RQIR Iter650 must derive or locate an explicit same-parent Source/Born contribution **inside the closed retarded gravitational Gamma3 matched observable**, with CTP/Legendre normalization and pole/cut origin frozen before subtraction. If no committed definition exists, the prerequisite remains terminal operational `BLOCKED`.

Heavy numerical work cannot determine this missing cross-sector definition.

## KMQGB score consequence

- R1 unchanged: 92%.
- R2 unchanged: 89%.
- R3 remains externally controlled at 24%.
- R4 unchanged: 45%.
