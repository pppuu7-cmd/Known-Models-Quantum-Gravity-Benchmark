name: iter456-reduced-toller-appendixb
on:
  push:
    paths:
      - 'code/iter456_reduced_toller_appendixb.py'
      - '.github/workflows/iter456-reduced-toller-appendixb.yml'
  workflow_dispatch:

jobs:
  lane:
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        lane: [L0, L1, L2, L3]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install mpmath
      - name: Frozen Iter456 lane audit
        run: |
          mkdir -p artifacts
          python code/iter456_reduced_toller_appendixb.py --lane ${{ matrix.lane }} --out artifacts/${{ matrix.lane }}.json
      - uses: actions/upload-artifact@v4
        with:
          name: iter456-${{ matrix.lane }}
          path: artifacts/${{ matrix.lane }}.json

  aggregate:
    needs: lane
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: iter456-*
          path: raw
          merge-multiple: true
      - name: Frozen aggregate
        run: |
          python - <<'PY'
          import glob,json,os
          files=sorted(glob.glob('raw/*.json'))
          rec=[json.load(open(f)) for f in files]
          valid=len(rec)==4 and all(x.get('valid') for x in rec)
          passed=valid and all(x.get('pass') for x in rec)
          cls=('ITER456_REDUCED_TOLLER_APPENDIXB_RUHL_PHASE_QUALIFIED_SCOPED' if passed else
               'SCIENTIFIC_FAIL_ITER456_REDUCED_TOLLER_APPENDIXB' if valid else
               'INFRASTRUCTURE_OR_NUMERICAL_FAIL')
          summary={'n_artifacts':len(rec),'valid_lanes':sum(bool(x.get('valid')) for x in rec),
                   'pass_lanes':sum(bool(x.get('pass')) for x in rec),'classification':cls,'lanes':rec}
          os.makedirs('summary',exist_ok=True)
          json.dump(summary,open('summary/iter456_summary.json','w'),indent=2)
          print(json.dumps(summary,indent=2))
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: iter456-summary
          path: summary/iter456_summary.json
