name: iter299-recovery-integration-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    steps:
      - name: Archived one-shot recovery integration
        run: |
          echo 'Iter299 recovery integration is already incorporated into the authoritative benchmark front.'
          echo 'This legacy workflow is retained only as an archival manual marker and no longer writes to main.'
