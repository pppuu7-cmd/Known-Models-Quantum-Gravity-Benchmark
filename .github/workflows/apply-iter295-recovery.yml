name: apply-iter295-recovery

on:
  push:
    branches: [main]
    paths:
      - 'code/apply_iter295_recovery.py'
      - '.github/workflows/apply-iter295-recovery.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: main
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12.11'
      - name: Apply frozen interpreted Iter295 recovery update
        run: python code/apply_iter295_recovery.py
      - name: Commit only if changed
        run: |
          FILES='paper_iv/P_LQG_STACK_CAUSAL_DOMAIN_OVERLAP_AUDIT_ITER295_2026-09-11.md paper_iv/PAPER_IV_DECISION_LEDGER_DELTA_295.json recovery/RECOVERY_DELTA_295.md recovery/PUBLICATION_IMPACT_LEDGER.md recovery/CURRENT_BENCHMARK_FRONT.md'
          if git diff --quiet -- $FILES; then
            echo 'No recovery changes required.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add $FILES
          git commit -m 'Iter295: integrate LQG stack causal overlap recovery state'
          git push origin HEAD:main
