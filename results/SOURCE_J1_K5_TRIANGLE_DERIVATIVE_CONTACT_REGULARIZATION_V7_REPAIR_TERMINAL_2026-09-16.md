# SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_REGULARIZATION_V7 — same-contract repair terminal result

Date: 2026-09-16
Status: TERMINAL RESEARCH RESULT

## Authority

- Gate: `SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_REGULARIZATION_V7`.
- Frozen preregistration: `research/SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_REGULARIZATION_V7_PREREG_2026-09-16.md`.
- Frozen preregistration commit: `764cd9f915cef15b6294f5f72329f12cb145c08e`.
- Initial implementation commit: `44da1edd39debce755f9ae7d48c4e7abd375604c`.
- Historical run `35054748495` remains preserved with independent Critic verdict `INVALID_IMPLEMENTATION` and is not consumed as validated science.
- Same-contract production-control repair commit: `3b369be9fd3769768fb1dcd1a8dc0076f8ae6ff1`.
- Source-authority lock repair commit: `b6f78151443dfc78c6ae07ceadf9977964a36df3`.
- The second repair only replaced a brittle current-front string lookup with the already-frozen, independent Critic-confirmed V5 channel/source-realization authority. It did not change the frozen object, schemes, statistic, controls, PASS/FAIL/BLOCKED semantics, or interpretation ceiling.

## Authoritative Actions execution

Run `35067251385`, terminal `completed/success`, head `b6f78151443dfc78c6ae07ceadf9977964a36df3`.

Jobs:

- source-lock `104700239717`, success;
- exact Python 3.13 `104700275681`, success;
- exact Python 3.11 `104700275717`, success;
- aggregate `104700314512`, success.

Artifacts:

- `v7-source-lock` ID `10433814639`, digest `sha256:6f0b93237fa00e295099cba62a9efa1192c0a4f7d140593e006406d2c85b5907`;
- `v7-python-3.13` ID `10435096047`, digest `sha256:03465ab60d701497d173b4be3f3bedcac5d0308b4da8019adb54badea6ab46f3`;
- `v7-python-3.11` ID `10435011447`, digest `sha256:c3f4edd548c0cd55f2cdf81d93f9d731ead27acd5ec0b8922f980d880455ea10`;
- `v7-aggregate` ID `10434348700`, digest `sha256:cc8a7175007e7589d2a9ce2ee9346e796c0b8f5bae770d48e37483502c8073cd`.

Canonical aggregate JSON SHA256: `3906b6f76adc7469617550c14ecfc0165dcaefbede578318de55be1e569b9f77`.
Raw aggregate log SHA256: `8f51a3a999da54481707ccd4a200753fc524458d694455dac4925a7e5e4ff013`.

## Frozen terminal classification

`TRIANGLE_DELTA2_CONTACT_REGULARIZATION_DEPENDENT_SCOPED`

This is a scoped scientific PASS for the prospectively frozen relative-width-dependence hypothesis. It is not a model/family FAIL and it is not an extension-nonexistence theorem.

## Source / realization authority

All execution-head source locks pass.

The V5 dependency is now locked to the independent V5 Critic record rather than to navigation prose. That Critic independently confirmed, on the same frozen fixed-tangent `j=1` channel realization:

- exact `channel 00000 = 11/24`;
- `SOURCE/REALIZATION_CHECK = PASS_SCOPED`;
- `SAME_REALIZATION_CHECK = PASS_SCOPED`.

Its `QUALIFIED` verdict limits only promotion of the exact channel census to an unfrozen claim of "broad support"; it does not invalidate the exact `00000 = 11/24` fact consumed by this V7 preregistration.

## Exact result

The frozen local derivative-contact object uses

- `B12=x`,
- `B23=y`,
- `B13=x+y`,
- exact Gaussian `delta''` factors,
- scale-free statistic `K = pi * (eps^7 I_eps)^2 / (a+b+c)^7`.

Exact aggregate values are:

| scheme | widths `(a,b,c)` | `D` | `E` | `K` |
|---|---|---:|---:|---:|
| A | `(1,1,1)` | `3` | `-5/9` | `1600/531441` |
| A4 | `(4,4,4)` | `48` | `-5/9` | `1600/531441` |
| B | `(1,1,4)` | `9` | `-80/243` | `204800/1162261467` |
| C | `(1,2,3)` | `11` | `-540/1331` | `14400/19487171` |

Thus common rescaling is removed exactly (`K(A4)=K(A)`), while the three frozen relative-width schemes are not equal. Both exact Python lanes return the same classification and agree on all decision-critical fields.

## Controls

All frozen production/object controls pass:

- source authority = true;
- derivative identity is wired to the executable production derivative-factor representation;
- independent transverse two-contact normalization is replayed through the shared multivariate Gaussian/Wick engine and equals `4`;
- one-factor zero mass and `x^2` normalization pass for frozen widths;
- permutation symmetry passes exactly;
- common-rescaling invariance passes exactly;
- target determinant is positive;
- wrong-sign derivative fixture is rejected;
- wrong-conormal fixture is rejected;
- plain-delta fixture is distinguished from the derivative-contact statistic.

The two implementation defects identified by the independent Critic for historical run `35054748495` are therefore repaired on the decision-producing machinery rather than merely bypassed.

## New scientific fact

For the exact frozen local K5 triangle highest-contact `delta''` realization, within the prospectively frozen normalized Gaussian regulator family, quotienting out common-width scaling does **not** remove dependence on relative regulator widths: schemes A, B, and C yield distinct exact rational values of `K`.

This establishes regulator-scheme dependence for this scoped local Gaussian derivative-contact diagnostic. It does not by itself show that every admissible mollifier behaves this way, that no distributional extension exists, or that a source-authorized local counterterm cannot identify the schemes.

## Historical preservation

- Historical V7 run `35054748495` remains preserved with Critic verdict `INVALID_IMPLEMENTATION`.
- Repair run `35061567730` remains preserved as an implementation/provenance history in which the classifier returned a source block because the V5 lock searched stale navigation prose rather than the independent validated V5 authority.
- No historical FAIL/BLOCKED/INVALID record is rewritten or deleted.

## Claim ceiling

Local parent-derivative-order Gaussian triangle only. No all-mollifier claim, no extension-nonexistence claim, no full-K5 product theorem, no model/family failure, no D7 closure, no terminal selector, and no Candidate Gravity activation.

`RQIR Core v1.0 = FROZEN`; `D7-S2 = NOT_CLOSED`; `D7-S3 = NOT_CLOSED`; `D7-S4 = PARTIAL_GLOBAL_NOT_CLOSED`.

## Next recommended gate

Prospectively freeze `SOURCE_J1_K5_TRIANGLE_DERIVATIVE_CONTACT_COUNTERTERM_EQUIVALENCE_GATE` on the same local `delta''` triangle realization. Test whether the exact A/B/C scheme differences can be absorbed by a source-authorized collision-supported local counterterm while preserving the off-collision object, fixed channel/realization authority, permutation constraints, and normalization. Separate clearly:

- source-fixed unique subtraction;
- finite but source-unfixed local freedom;
- inability of the frozen local counterterm class to identify the regulator schemes;
- missing source authority.

That gate must not be inferred from the present Gaussian regulator-dependence result and is not executed here.
