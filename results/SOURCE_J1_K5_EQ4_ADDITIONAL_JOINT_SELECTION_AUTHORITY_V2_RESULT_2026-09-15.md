# Terminal result — source j=1 K5 Eq. (4) additional joint-selection authority v2 repair

Date: 2026-09-15
Lane: KMQGB Research / Closure
Status: `TERMINAL_SCOPED`

## Authority

This result is governed by the prospective v2 preregistration:

- `research/SOURCE_J1_K5_EQ4_ADDITIONAL_JOINT_SELECTION_AUTHORITY_V2_PREREG_2026-09-15.md`;
- prereg commit `b50934de049c6597c3732a0d1a334c2cabb4302f`;
- status `PROSPECTIVE_FROZEN` before implementation and result-producing execution.

The gate repairs the implementation defect identified by the latest independent Critic:

- `recovery/CRITICAL_REVIEW_SOURCE_J1_K5_EQ4_ADDITIONAL_JOINT_SELECTION_AUTHORITY_2026-09-15.md`;
- Critic commit `fcc22dcf4f186b2de3557507242d5186e4e96caa`;
- verdict on the historical v1 terminal classification: `INVALID_IMPLEMENTATION`.

The historical v1 result is preserved and is not silently repaired in place.

## Exact object

The same Critic-confirmed scoped object is retained:

- source-order gauge-fixed K5 causal vertex;
- fixed `j=1`;
- fixed nonzero real edge-`rho` convention;
- intertwiner channel `00000`;
- full K5 collision stratum `N=SU(2)^4` inside gauge-fixed `SL(2,C)^4`;
- homogeneous collision-supported witness `Delta(kappa)=c(kappa) delta_N`, with `c=+i` on the 16 constrained causal edge-sign patterns, `c=-i` on their 16 global negatives, and `c=0` on the other 992 independent sign patterns.

The witness is used only as a discriminator for joint selection authority. This gate does not assume or prove existence of a base joint Eq. (4) extension.

## Production provenance

Implementation:

- `code/source_j1_k5_eq4_additional_joint_selection_authority_v2.py`;
- code commit `edc1bfa00b720e07cb08c2a2961e32267dee1a23`.

Workflow:

- `.github/workflows/source-j1-k5-eq4-additional-joint-selection-authority-v2.yml`;
- production/head commit `586f0e373b80518982960c3759ec8bba4249d848`.

Authoritative GitHub Actions execution:

- run `34935737476`, terminal `completed/success`;
- source-lock job `104273108027`, `success`;
- authority-audit job `104273131510`, `success`;
- source-lock verified the prospective freeze, ancestry, and unchanged frozen dependency files;
- green CI is provenance only, not the scientific classification.

## Raw / canonical artifacts and hashes

GitHub Actions artifact:

- artifact ID `10382678434`;
- artifact name `source-j1-k5-eq4-additional-joint-selection-authority-v2`;
- size `18501` bytes;
- artifact ZIP digest `sha256:ddc5863943033748441a6fd12452736349b5520b2721042d5624b7946e3da0cf`.

The artifact contains the byte-exact full canonical JSON, raw log, SHA256 record, and provenance text.

Byte-exact Action hashes:

- canonical JSON SHA256 `f24fb2e9efd7e2f97dcb037b98534b812024885c812fc6d07f37ad81830d6820`;
- raw log SHA256 `aaa85cc2465d40bab9a23055e94e37765fd03d36b1d393e5a2438b024609d96f`;
- provenance text SHA256 `d42438d73f1b5b0127a1c12871d430b18ee84babcde3d4d76af63a9b17ca0547`.

Repository copies/ledgers:

- `results/source_j1_k5_eq4_additional_joint_selection_authority_v2_2026-09-15.json` — decision-preserving projection of the full Action JSON, repository projection SHA256 `27306667862713fdba4b8981ca46f6de1d093d617955c54e31ae20ff453afee7`;
- `results/source_j1_k5_eq4_additional_joint_selection_authority_v2_2026-09-15.log` — raw decision log;
- `results/source_j1_k5_eq4_additional_joint_selection_authority_v2_2026-09-15.provenance.txt`;
- `results/source_j1_k5_eq4_additional_joint_selection_authority_v2_2026-09-15.sha256`.

The full 65,751-byte canonical Action JSON remains authoritative in artifact `10382678434`; the repository JSON deliberately records a smaller decision-preserving projection and points back to the byte-exact artifact hash instead of pretending to be the same byte stream.

## Six-class repaired audit

The v2 implementation realizes the Critic-required one-to-one six-class matrix with stable IDs and explicit per-row evidence predicates.

1. `C1_CORRELATED_COMMON_REGULATOR_REMOVAL`
   - status: `NOT_SOURCE_PINNED_IN_FROZEN_CORPUS`;
   - the published source-scope audit records no common/vector Eq. (4) regulator-removal path and the structured Iter331 source rows contain no common multiwedge `i epsilon` limit.

2. `C2_INDEPENDENT_REGULATOR_PATH_ORDER_THEOREM`
   - status: `NOT_SOURCE_PINNED_IN_FROZEN_CORPUS`;
   - this class is tested separately from C1;
   - the companion source audit explicitly found no regulator path/order-independence theorem resolving the K5 collision product, while the scaling audit states such path/order independence would have to be proved rather than assumed.

3. `C3_MICROLOCAL_PRODUCT_PULLBACK_EXTENSION_THEOREM`
   - status: `NOT_SOURCE_PINNED_IN_FROZEN_CORPUS`;
   - the frozen corpus records the ten-wedge correlated collision product as `NOT_YET_JUSTIFIED`, and the microlocal theorem is presented only as a possible future route rather than an established source theorem.

4. `C4_VERTEX_LEVEL_NORMALIZATION_BOUNDARY_CONDITION`
   - status: `NOT_SOURCE_PINNED_IN_FROZEN_CORPUS`;
   - this broad class is not reduced to the Iter331 K4/K3 jet flags;
   - the Iter331 jet flags are used only as supporting source-audit evidence, while the complete frozen records are separately traced for vertex-level identities, normalizations, and boundary conditions.

5. `C5_NESTED_FOREST_GLUING`
   - status: `NOT_SOURCE_PINNED_IN_FROZEN_CORPUS`;
   - the Iter331 source rows and aggregate contain no source-defined forest-compatible gluing normalization, and the scaling audit treats nested compatibility as an open possibility rather than an established rule.

6. `C6_OTHER_GENUINELY_JOINT_SOURCE_THEOREM`
   - status: `NOT_SOURCE_PINNED_IN_FROZEN_CORPUS`;
   - every one of the nine frozen dependency records is explicitly traced;
   - `all_dependency_records_traced=true`, `no_unclassified_dependency_record=true`;
   - no unconditional hard-coded absence predicate is used.

All six rows have `action_on_delta = null` because no source-pinned genuinely joint authority satisfying the frozen same-object contract is present strongly enough to define an action on `Delta`.

Counts:

- `source_pinned_acts_on_delta_count = 0`;
- `source_pinned_action_undetermined_count = 0`.

## Controls

All prospectively frozen positive and negative controls passed.

Key repair controls include:

- exact six rows with unique stable IDs;
- separate independent-regulator/path-order predicate;
- broad vertex-level normalization class not reduced to jet flags;
- exhaustive class-6 trace across every frozen dependency record;
- one-wedge `i epsilon` not promoted to joint authority;
- one-wedge uniqueness not promoted to joint authority;
- generic extension theory not promoted to a coefficient-selection rule;
- Iter331 detector/rank statements not promoted to physical normalization;
- synthetic common-regulator text without a removal rule rejected;
- synthetic independent-regulator text without a path/order theorem rejected;
- negated normalization text rejected.

`controls_ok = true`.

## Independent workflow status control

Fresh status was recorded inside the terminal Action artifact without consuming partial scientific values:

- Iter504 run `34907349374`: `queued`, conclusion `null`, head `56362459a826e3e376c529446678f2fbcaa269ae`;
- Iter461 run `34748503239`: `queued`, conclusion `null`, head `05c7f87c8519349057332bf90021f1128e1eefc3`.

No job output, artifact value, or partial substantive value from either non-terminal workflow entered the six-class decision.

## CLASSIFICATION

`SOURCE_J1_K5_EQ4_ADDITIONAL_JOINT_SELECTION_AUTHORITY_V2_NOT_PINNED_SCOPED`

This is the prospectively frozen **BLOCKED/source-authority** outcome. It is not a scientific/model FAIL.

## NEW FACT

Within the exact nine-record frozen validated repository corpus, a repaired one-to-one audit of all six prospectively frozen classes finds **no genuinely joint Eq. (4) source authority pinned strongly enough to act on the confirmed full-collision `Delta` witness**.

This closes the specific implementation gap identified by the latest Critic: the conclusion no longer depends on a five-row surrogate, no longer merges the independent-regulator/path-order class, no longer treats K4/K3 jet flags as the exhaustive definition of vertex-level normalization, and no longer hard-codes the catch-all class absent.

The result localizes the current high-DAG blocker to the absence, in this frozen source corpus, of an explicit joint product/limit/normalization/gluing theorem capable of selecting the collision-supported coefficient.

## CLAIM CEILING

Do **not** infer from this result:

- existence or nonexistence of a base Eq. (4) joint extension;
- unconditional physical or global nonuniqueness of the published causal vertex;
- source authorization of `Delta`;
- absence of qualifying authority outside the exact frozen repository corpus;
- impossibility of constructing a future source-compatible common/independent regulator, microlocal product theorem, normalization, boundary condition, or gluing theorem;
- any result for other spins, channels, families, lower collision strata, or arbitrary boundary recouplings;
- closure of `D7-S2`, `D7-S3`, or `D7-S4`;
- any terminal D7 selector, including `EXISTING_SUFFICIENT / ADAPT_EXISTING / HYBRID_REQUIRED / NEW_REQUIRED`;
- Candidate Gravity activation;
- `QUANTUM_GRAVITY_SOLVED`, `ALL_KNOWN_MODELS_FAIL`, `NEW_THEORY_REQUIRED`, or `NEW_PHYSICS_FOUND`.

Historical FAIL/BLOCKED/INVALID classifications remain preserved. In particular, the historical v1 authority result remains Critic-qualified `INVALID_IMPLEMENTATION`; this v2 result is a new prospectively frozen gate, not a rewrite of history.

## Consequence

The source-authority repair is now terminal for the exact frozen corpus. The highest-information next analytical question is logically distinct from uniqueness/selection:

`SOURCE_J1_K5_EQ4_IMPROPER_DISTRIBUTIONAL_EXISTENCE_GATE`.

That future gate should prospectively test whether the published Eq. (4), using only the already source-defined one-wedge Toller distributions, exists on the full K5 collision as a mathematically controlled distribution without silently adding new model data. It must not treat this v2 authority blocker as proof of nonexistence or nonuniqueness.

No such second substantive gate is executed in this run.

## Governance

- `RQIR Core v1.0 = FROZEN`.
- `D7-S2 = NOT_CLOSED`.
- `D7-S3 = NOT_CLOSED`.
- `D7-S4 = PARTIAL_GLOBAL_NOT_CLOSED`.
- Terminal D7 labels and `EXISTING_SUFFICIENT / ADAPT_EXISTING / HYBRID_REQUIRED / NEW_REQUIRED` remain forbidden.
- Candidate Gravity remains inactive.
- A scoped source-authority blocker is not family closure and not scientific falsification.
